<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.helloai.core.agent.mapper.AgentDutyLeaseMapper">

    <!--
        AgentHub V1 T3：查询 Agent 当前有效的值班租约。
        条件：agent_id 匹配 + status='ACTIVE' + 未删除，
        按 create_time 倒序取最新一条（防御性：理论上只有一条 ACTIVE）。
    -->
    <select id="selectActiveByAgentId" resultType="com.helloai.core.agent.entity.AgentDutyLease">
        SELECT * FROM agent_duty_lease
        WHERE agent_id = #{agentId}
          AND status = 'ACTIVE'
          AND deleted = 0
        ORDER BY create_time DESC
        LIMIT 1
    </select>

    <!--
        AgentHub V1 T3：关闭 Agent 当前所有有效租约。
        将指定 agent 的所有 ACTIVE 租约改为目标状态（CLOSED/EXPIRED）并写入关闭原因。
        返回值 = 影响行数；0 表示没有需要关闭的 ACTIVE 租约。
    -->
    <update id="closeActiveLeases">
        UPDATE agent_duty_lease
        SET status = #{newStatus},
            close_reason = #{closeReason},
            update_time = #{now}
        WHERE agent_id = #{agentId}
          AND status = 'ACTIVE'
          AND deleted = 0
    </update>

    <!--
        AgentHub V1 T3：扫描超时未续约的 ACTIVE 租约。
        条件：status='ACTIVE' + expires_at &lt; cutoff + 未删除。
        由定时任务调用，将超时租约标记为 EXPIRED。
    -->
    <select id="selectExpiredLeases" resultType="com.helloai.core.agent.entity.AgentDutyLease">
        SELECT * FROM agent_duty_lease
        WHERE status = 'ACTIVE'
          AND expires_at &lt; #{cutoff}
          AND deleted = 0
        ORDER BY expires_at ASC
        LIMIT #{limit}
    </select>

</mapper>
