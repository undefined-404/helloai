# HelloAI 架构演进日志 — 2026-09

## 2026-09-07 文档治理与架构主线收敛

正式将后续主线冻结为：

```text
Event Stream
→ Dual Executor Migration
→ AgentRuntime
→ Skill Capability
→ Sandbox Provider
```

并明确：

```text
Harness = 参考架构
HelloAI = Distributed Heterogeneous Agent Platform
```

## 2026-09-07 文档体系补口（现状基线 / 回流规则 / 协作规约落库）

针对文档治理重构后的四处收尾（零代码改动）：

1. **实施计划补现状基线**：P0-A 标注 A1~A5 已落地（验证 `verify-c3-events.ps1`），动作从 A6（Timeline 消费迁移）起步；P0-B 标注契约层已单轨（`LocalExecutionCommandConsumer` / `MqExecutionCommandConsumer` 统一经 `AgentRuntime#execute`，唯一实现 `LegacyExecutorAdapter` 转发旧链），实际工作是 Runtime 长真身而非建轨；P0-C 标注第一阶段已落地，动作从第二阶段（ToolRegistry / ToolExecutor）起步；P1 三项（Skill / Sandbox / Event Consumers）各补一行现状。
2. **差距表补 §7**：孤儿项回流规则（设计文档内「推迟 / 划远期 / 降级」决定必须同步登记或记 WONTFIX）+ N-013~N-019 ↔ G-003~G-009 历史编号映射。
3. **协作规约落库**：`doc/HelloAI_AI开发协作规约.md`（AI 开发生命周期 / 验证体系 / 红线），并入 doc/README.md 权威表。
4. **`.trae/rules/执行规则.md` 改薄指针**：原为基线文档逐字复制（名实不符且必然失同步），改为指向协作规约与必读清单的指针，保留硬红线内联。

