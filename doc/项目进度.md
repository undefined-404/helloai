# 项目进度（结构重构 + 字段命名规范化）

> 本文件供会话中断后接续，每完成一个 Step 追加：已完成项 / 当前分支 / 下一待办。

## 阶段一：core 业务域分包（分支 refactor/core-domain-package）

### 前置基线
- 已完成：建分支 refactor/core-domain-package；基线 `mvn compile` + `mvn test`（187+25，0 失败）全绿。
- 说明：`mvn clean` 因 helloai-mq\target 被占用无法删除，改用不带 clean 的编译/测试，结果等价可信。

### Step 1 — 迁移 28 个 Service（已完成）
- 已完成项：
  - 28 个 Service 由 `com.helloai.core.service` 迁至 `agent.service`(11) / `task.service`(7) / `system.service`(10)，逐个改 package 声明。
  - 全项目（core/api/job/start）60 个文件的 import / FQN 引用同步改为新包。
  - 跨域兄弟引用补 import：AgentService、SubTaskService、RewardService、AgentExecutionConnectivityService、AttachmentService、SubTaskDispatchService、McpToolService（保留在 core.service）。
  - 3 个测试类随主类迁移到对应域测试包：AgentInboxServiceTest、ExternalAgentFailureTrackerTest → agent.service；SubTaskDispatchServiceTest → task.service。
  - McpToolService 本步保留在 `core.service`（Step 3 随 mcp 迁入 agent/mcp）。
  - PatrolRecordService：无任何外部注入点（仅自身 @Service 空实现），按文档"在用就迁、本次不删"处理——已随 system.service 迁移，未删除。
- 验证闸：`com.helloai.core.service.`（排除 McpToolService）残留 grep = 0；`mvn compile` + `mvn test` 全绿。
- 当前分支：refactor/core-domain-package
- 下一待办：Step 2 — 迁移 24 Entity + 24 Mapper + 启动类 @MapperScan + 5 个 XML 全限定名。
