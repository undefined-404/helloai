# 项目进度

> 本文件供会话中断后接续，每完成一个里程碑追加：已完成项 / 验证证据 / 下一待办。
> 历史重构明细见 archive/HelloAI_core结构重构执行清单.md、archive/V23_字段命名规范化迁移与同步清单.md。
> 各里程碑完整实施细节与验证证据见 doc/log/HelloAI_迭代执行记录.md（§6.x 小节）。

## 已完成里程碑

### M1：core 业务域分包重构（2026-07-18 完成并合并）
- core 统一为 agent / task / system / shared 四域分包；98 个文件迁移，5 个 mapper XML 同步；
  @MapperScan 改为三域包；CODE_STYLE 回写分包规则。
- 验证：mvn compile + test（187+25）全绿；旧包名 grep 残留为零。

### M2：V23 字段命名规范化（2026-07-18 完成）
- 20 列改名（16 个 _at 时间字段 + 3 个 agent 引用补 _id + trigger→trigger_type）；
  entity/XML/AgentHealthCheckTask 裸列名同步；scripts/ 8 个验证脚本的旧列名后续补修。
- 验证：启动 Flyway 命中数=20；冒烟通过。

### M3：规范文档 V1.4（2026-07-18）
- 启动类示例、资源位置、包命名、Flyway 规范、6.3 Controller 分层红线（含 6 个待收口
  Controller 清单）、8.5 残留检查范围（Java+XML+scripts+doc）全部对齐代码事实。

### M3.5：代码规范零欠账收口（2026-07-18 完成）
- P0：6 个 Controller（Activity/AdminDashboard/AgentDutyLease/Attachment/Dashboard/Feed）直注 Mapper 全部收口至 Service，查询逻辑下移，mapper import 零残留；
- P1：com.helloai.config 并入 com.helloai.start.config，启动模块配置包统一；
- P2：DeepSeekProviderChatClientFactory 归位 core/agent/chat/provider/，与接口同域；
- CODE_STYLE §6.3 待收口清单与 §3.x start 配置待收口段落已同步移除，规范与代码一致。
- 验证：mvn compile 全绿；grep "private final.*Mapper" helloai-api Controller 目录 = 0。

### M4：外部 AI Agent 接入联调（2026-07-18 打卡链路全通）
- qoder、trae 两个外部 AI Agent 通过一键生成 skills 内容一次注册并连接成功；
- MCP 初始化测试通过；上班打卡（checkIn）成功；
- 租约超时后使用"一键上班"提示词可正常重连。
- 备注：脚本化五步闭环已于 2026-07-17 实测通过（verify-onboarding-*.ps1 系列 ALL PASSED）。

### M4.5：调度链加固 + 派发控制台（2026-07-20）
- N15 Controller 分层红线收口（6 个违规 Controller 收口至 Service）+ 两项包归位；
- 调度链缺陷修复：心跳阈值统一接入 `AgentHealthProperties`（消除 Java/SQL 漂移）+ 二次选人兜底；
- 重分配熔断（V24）：`reassign_attempt_count` + 阈值熔断，打破"Agent 全掉线→无限重分配"死循环；
- 派发控制台：`POST /api/sub-tasks/batch` 批量创建 + `GET /api/sub-tasks/{id}/timeline` 执行时间线 + 前端快速派发/时间线卡片。
- 验证：mvn reactor 7 模块全 SUCCESS（helloai-core 216 + helloai-job 38 全绿）。
- 详见：迭代记录 §6.9/§6.10、差距表 N15。

### M5：真实 AI 子任务闭环联调（进行中）
- qoder/trae 打卡链路已通（2026-07-18，M4）；脚本化五步闭环已实测通过；
- 场景矩阵（1~5）继续推进中：happy path / blocked path / 超时替补 / 附件 path / 双 Agent 值班；
- 原"场景 1~3 全绿前 planner 不启动"门禁已于 2026-07-28 按用户决策解除（草案态硬隔离，
  见 M6 与差距表 §5 第 7b 条）。
- 详见：差距表 N14。

### M6：PLANNER 任务自动拆解（2026-07-28 交付，门禁提前解除）
- V26"需求 → LLM 自动拆解 → 草案 `PENDING_PLAN_REVIEW` → 确认/拒绝 → 进入既有分发链"闭环；
  `PlannerAnalysisService` 五方法 + `TaskController` 四薄入口 + planner SKILL.md 升级；
- 2026-07-29 前端规划确认页补全（PlanReviewDialog）；真实环境 e2e 12 步全绿（verify-planner-decompose.sh）；
- 2026-07-30 增量：`orderByDependency` 拓扑正序展示。
- 详见：差距表 N16、迭代记录 §6.16/§6.18/§6.27。

### M7：对话式需求澄清（2026-07-29 ~ 08-01 交付）
- V29"模糊想法 → 多轮追问 → 终稿 → 建任务 → 顺路拆解"闭环（requirement_conversation/message 两张新表）；
- V31 Planner 钉住 + 失败重试；V33 结构化选项式追问 + progress 进度条；V34 联网搜索开关（默认博查）；
- 验证：verify-requirement-clarify.sh 真实环境 10 步全绿 + 浏览器闭环 8 步全过 +
  verify-requirement-clarify-structured.ps1 PASSED + verify-websearch-e2e.sh 三路径。
- 详见：差距表 N17、迭代记录 §6.19/§6.29/§6.32/§6.38。

### M8：任务管理收口 + 产出物化 + 执行链可观测（2026-07-28 ~ 08-02 交付）
- 任务-子任务关联打通 + 真分页；任务 CRUD 收口 + 级联删除（竞态免疫）；值班租约 Agent 维度展示；
- 子任务依赖可视化：DAG 分层流水线视图 + 传递归约 + 拓扑序号徽标（6.33~6.37）；
- 执行产出物化（方案2）+ 交付物实时聚合 zip 下载 + V32 任务最终整合报告（自动/手动双触发）；
- V35 执行链依赖上下文注入（执行 Agent 参考直接前置产出）；
- V38 LLM 对话流可视化（user prompt 落库）+ reviewHistory 多轮累积；Snowflake ID 全链路字符串化；
- 验证：mvn 343 测试全绿 + vue-tsc 0 错 + npm run build 通过。
- 详见：迭代记录 §6.11~§6.13/§6.27/§6.30/§6.31/§6.33~§6.37/§6.39/§6.40/§6.41。

### M9：Planner 双模对话 + 执行链双轨上下文 + 调度/报告兜底（2026-08-03 ~ 08-04 交付）
- 依赖感知双轨上下文注入（§6.43）：下游按 `depends_on` 声明顺序同时获得直接前置的**结构化摘要**（EXECUTION_RECORD 精确取单条）与**完成内容本体**（物化附件优先、`context.lastExecution.output` 回退），渲染于 `## 依赖产出参考（直接前置）` 章节，多前置全量收集不覆盖；Phase A JSONB 回填加 taskId 粒度分段锁防并发互覆；顺带补记 Task Running Spec 全貌（V35~V37）与 §6.39 编号勘误；
- Planner 对话双模式 V39~V40.2（§6.44~§6.46）：CHAT 自由对话 + CLARIFY 方案澄清双模（单会话 mode 字段）；意图词二次确认（V40，去「转为方案」按钮）+ 口语化意图词扩展 + 澄清首轮强制 structured（V40.1）；`/planner` 斜杠命令直达 + CHAT 轮 LLM 追问推荐卡片（V40.2）；切换端点按 §8.2 整改为 `toClarifyById`/`toChatById`；
- 子任务分发失败快速兜底（V41）：孤儿 PENDING 巡检阈值 30→5 分钟（isReady 守卫不误伤）+ 依赖解锁失败写 `sub_task_dispatch_deferred` timeline 事件（异常不再静默）；
- 整合报告生成状态防重（V41）：`FinalReportStatus` 四态（NONE/GENERATING/DONE/FAILED）+ CAS 防重入（手动/自动并发互斥）+ 失败可重试 + 前端「报告生成中」tag/按钮禁用/5s 轮询；
- 验证：helloai-core 全量 397/397 全绿 + vue-tsc 0 错 + `verify-deps-context-e2e.sh`（双前置场景 PASS）+ `verify-planner-chat-dual-mode.sh`（8 步全流程 PASS）+ 报告并发防重全链路冒烟 PASS。
- 详见：迭代记录 §6.43~§6.47、差距表 N6/N17。

## 当前待办（下一里程碑）

- **M5 场景矩阵继续推进**：真实外部 AI 自主按 SKILL.md 完成"收铃 → 拉任务 → 认领 → 执行 → 提交"全环
  （差距表 N14）。
- **REVIEWER 自动审查补强**：当前主流程仍依赖显式 review 提交（基线文档 §9 状态"部分支持"）。
- **差距表遗留项**：N2 工作流模板、N8 浏览器型 Agent、N10 credential_vault 迁移收口、
  N12 A2 第 2 段（动态 TTL）+ 第 3 段（concurrency 预扣）、产出物化方案3（LLM manifest）、
  moonshot/minimax/dashscope Provider Factory。

## 历史分支状态
- refactor/core-domain-package、refactor/db-field-naming：均已合并主分支，可删除。
