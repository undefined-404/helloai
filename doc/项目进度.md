# 项目进度（结构重构 + 字段命名规范化）

> 本文件供会话中断后接续，每完成一个 Step 追加：已完成项 / 当前分支 / 下一待办。

## 阶段一：core 业务域分包（分支 refactor/core-domain-package）

### 前置基线
- 已完成：建分支 refactor/core-domain-package；基线 `mvn compile` + `mvn test`（187+25，0 失败）全绿。
- 说明：`mvn clean` 因 helloai-mq\target 被占用无法删除，改用不带 clean 的编译/测试，结果等价可信。

### Step 1 — 迁移 28 个 Service（已完成）
- 已完成项：
  - 28 个 Service 由 `com.helloai.core.service` 迁至 `agent.service`(11) / `task.service`(7) / `system.service`(10)，逐个改 package 声明。
  - 全项目（core/api/job/start）60 个文件的 import / FQN 引用同步改为新包。
  - 跨域兄弟引用补 import：AgentService、SubTaskService、RewardService、AgentExecutionConnectivityService、AttachmentService、SubTaskDispatchService、McpToolService（保留在 core.service）。
  - 3 个测试类随主类迁移到对应域测试包：AgentInboxServiceTest、ExternalAgentFailureTrackerTest → agent.service；SubTaskDispatchServiceTest → task.service。
  - McpToolService 本步保留在 `core.service`（Step 3 随 mcp 迁入 agent/mcp）。
  - PatrolRecordService：无任何外部注入点（仅自身 @Service 空实现），按文档"在用就迁、本次不删"处理——已随 system.service 迁移，未删除。
- 验证闸：`com.helloai.core.service.`（排除 McpToolService）残留 grep = 0；`mvn compile` + `mvn test` 全绿。
- 当前分支：refactor/core-domain-package
- 下一待办：Step 2 — 迁移 24 Entity + 24 Mapper + 启动类 @MapperScan + 5 个 XML 全限定名。

### Step 2 — 迁移 24 Entity + 24 Mapper + @MapperScan + 5 XML（已完成）
- 已完成项：
  - 24 Entity 由 `core.entity` 迁至 `agent.entity`(9)/`task.entity`(6)/`system.entity`(9)；24 Mapper 由 `core.mapper` 迁至对应 `agent/task/system.mapper`(9/6/9)，逐个改 package 声明。
  - 全项目（core/api/job/start）124 个 java 文件的 entity/mapper import 与 FQN 引用同步改为新包。
  - `AgentService`、`McpToolService` 的 `core.entity.*` / `core.mapper.*` 通配 import 展开为三域通配 import。
  - `HelloAIApplication` 的 `@MapperScan` 由单包改为 agent/task/system.mapper 三包数组。
  - 手改 5 个 mapper XML 的 namespace/parameterType/resultType 全限定名（AgentMapper 5、AgentDutyLease 3、AgentOutboxEvent 2、SubTask 4、TaskTimeline 2）。
- 教训：首版迁移脚本用 `Get-Content -Raw`（PowerShell 5.1 按 GBK 读）导致 UTF-8 中文注释/字符串损坏、字符串闭合引号被吞。已 `git reset --hard` 回滚，改用 `[System.IO.File]::ReadAllText`（UTF-8）重做，编码无损。
- 验证闸：§3.5 两条 grep（XML 与 java 旧包名）全空；`mvn compile` + `mvn test`（187+25，0 失败）全绿。
- 当前分支：refactor/core-domain-package
- 下一待办：Step 3 — 剩余包归位（statemachine/score/mcp/observability/event/doorbell/util 共 22 文件）+ 删空包。

### Step 3 — 剩余包归位 + 删空包（已完成）
- 已完成项：
  - `statemachine`(1)→`task.statemachine`；`score`(1)→`task.score`；`mcp`(7)+`service/McpToolService`→`agent.mcp`；`observability`(3)→`agent.observability`；`event`(4)→`shared.event`；`doorbell`(6)→`shared.doorbell`；`util/AgentCapability`→`agent`(根包)。共 24 个文件（文档 §4 表格明细 24 项；文档标题"22"为其自身表格计数笔误，以表格为准）。
  - 全项目 48 个 java 文件的 package 声明 / import / FQN 同步改为新包。
  - 删除已空的旧包目录：entity/mapper/service/statemachine/score/mcp/observability/event/doorbell/util（均确认 0 文件后删除）。core 顶层剩余：agent / task / system / shared / outbox（outbox 为既有包，不在本次范围）。
- 说明：`com.helloai.core.service.*` 残留 4 处均为 javadoc `{@link}/@see` 陈旧引用（ResilientDispatcher/AgentSelector/ExecutionCommandConsumer，本就不在 core.service），非 import、不影响编译，属既有陈旧注释，按"不做范围外改动"保留。
- 验证闸：旧包名 import 残留 grep 全空；`mvn compile` + `mvn test`（187+25，0 失败）全绿。
- 当前分支：refactor/core-domain-package
- 下一待办：Step 4 — 全局收口验证（§5.1 四条 import 残留 grep + 构建测试 + 接口冒烟）。
