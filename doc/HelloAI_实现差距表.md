# HelloAI 实现差距�?

## 1. 文档定位

本文档用于回答：

- 文档原先承诺了什�?
- 当前代码实际做到了什�?
- 差距属于“应改文档”还是“应补功能�?

本文档是当前版本对表、验收与排期的主参照文档�?

---

## 2. 当前总体结论

- 已交付闭环：MCP SSE 接入、鉴权、核心工具链路、在线状态三件套、熔断降级、Reconcile 健康检查、AgentHub V3 门铃通知通道（PR-1/2/3 + 运行�?E2E 实测 ALL PASSED + PR-4 保活�?双心跳；**2026-08-07 已搁�?*——外�?Agent 无法处理平台推送，�?N13）、外�?Agent 一键接入全套化（checkIn/checkOut 纳入默认授权 + executor SKILL 升级为含 MCP 全套工具/打卡/门铃的完整说明书）�?*改派/抢占撤销通知（A0-1，�?.60：改�?回收后旧执行者收 sub_task.reassigned / sub_task.unassigned + pullTasks 消息 reassigned 标记，一个轮询周期内可感知任务已转移�?*�?*MCP 接入体验补齐（A0-2，�?.61：Session 失效 404 fixHint 修复提示 + 无状�?REST 别名通道 POST /api/mcp/jsonrpc 免握手同步回�?+ SKILL 四步握手避坑�?*�?*工具面统一 + SKILL 速查表（A0-3，�?.62：三通道 10 工具对齐 + verify-tool-matrix 声明 vs 文档防漂�?+ 405 语义修复�?*�?*外部 Agent 信息获取能力补齐（A0-4，�?.63：getDepsSummary 主动拉前置产出摘要（数据口径与执行链同源�? sub_task.rejected/approved 评分反馈通知（summary 携带最�?review 评分/评语�? pullTasks read 状态位�?includeRead（未读优先，已读倒序补齐），三通道 11 工具对齐，真实环境完�?review 闭环实测通过�?*�?*LLM 供应商模型多选配置（§6.89：V49 `llm_provider_model` 模型�?+ 内置 4 厂商 11 模型种子 + �?default_model 迁移 + 后端模型管理全套（Service/QueryService/6 �?Admin 端点 + Agent 注册角色模型唯一性校验）+ 前端 Settings.vue 模型多�?UI（内置只�?自定�?默认模型�? `verify-llm-provider-models.ps1` 真实环境 38/38 ALL PASSED；配套修复物理唯一约束与逻辑删除冲突（V50/V51 部分唯一索引）、注册预校验、toggleModel 最后启用保护、deleteById 级联清理�?*
- 文档失真：历史路线图、技术方案与部分说明文档曾混入过时事实或实施日志
- 部分落地：执行命令主链、Provider 配置复用、工牌模式与 `credential_vault`
- 未落地：工作流模板、浏览器�?Agent 真实接入链路

---

## 3. 文档失真项（D�?

| 编号 | 主题 | 当前状�?| 结论 |
|---|---|---|---|
| D1 | MCP 工具数量口径 | �?已关�?| README 已明确：工具数量不写死，�?`tools/list` 实际输出为准�?026-07-13�?|
| D2 | 兼容通道定位 | �?已关�?| README 已明确：MCP SSE 是唯一主通道，REST `tools/list` / `tools/call` 属兼容保留（2026-07-13�?|
| D3 | 路线图正文混入实施日�?| �?已关�?| 实施记录已迁移到《迭代执行记录》，路线图已退出事实源 |
| D4 | Spring AI 版本口径 | �?已关�?| 现行版本以代码、基线文档与回归结果为准 |
| D5 | `/api/tools/cli` 鉴权口径 | �?已关�?| 代码验证：`WebMvcConfig` �?`/api/tools/cli` 已通过 `excludePathPatterns` 排除鉴权（公开下载入口，设计如此）。历史技术方案不再作为事实源�?026-07-13�?|
| D6 | 心跳刷新规则口径 | �?已关�?| README 已明确：`last_seen_at`/在线态刷新以 `heartbeat` 为主；仅�?pull/ack 也计入活跃需代码配合再升级（2026-07-13�?|
| D7 | README 文档边界 | �?已关�?| README 已收口为项目介绍与使用说明，不再承载阶段性执行结�?|
| D8 | PS 5.1 脚本 UTF-8 编码规范未沉淀�?skills | �?已关�?| 5 �?`helloai-preflight` SKILL.md�?agents/.qoder/.trae/.cursor/.claude�? `AGENTS.md` 同步落地“脚本必须显式声�?UTF-8 编码”规则，覆盖运行时输出编码（[Console]::OutputEncoding / $OutputEncoding）、源文件 BOM（UTF-8 with BOM + `Parser.ParseFile` 自检）、管道原始字节传输（`cmd /c type` / `[Diagnostics.Process]`）、here-string 串入 U+FEFF 隐限（入�?`TrimStart([char]0xFEFF)`）四个子项，并于 2026-07-16 补第 5 子项�?*双引号内 CJK 导致 PS 5.1 解析器提前闭合字符串（全角括�?`（）` 尤甚，抛 `Unexpected token '}'` / `字符串缺少终止符`）→ 修复范式统一�?单引�?+ `+` 拼接，runtime 字面量纯 ASCII、中文只留注�?**（落地参�?`verify-execution-dispatch-guard.ps1`）。以后任何新�?verify-*.ps1 / start-*.ps1 / test-*.ps1 / hook / CI 脚本都应遵循�?026-07-16�?|

---

## 4. 路线图项差距（N�?

| 编号 | 主题 | 当前状�?| 差距定义 | 处理建议 |
|---|---|---|---|---|
| N1 | Outbox / 命令可靠投递底�?| ②a + ②b 已落�?| `AgentOutboxService`（SubTask 状态变更事件）已具备事务�?Outbox 能力�?*Phase 2H ②a** 新增 `agent_command_outbox` �?+ `AgentCommandOutboxService`�? 个最小方法：createPending / listReadyForRelay / markSent / markFailed / markFinalFailed�? `OutboxRelayTask`（helloai-job，Redis �?+ 应用侧指数退避）+ `AgentCommandOutboxRelayProperties`（`enabled/interval-ms/batch-limit/max-retry/base-backoff-seconds`）。`ExecutionCommandService` �?`dispatch-mode �?{MQ,BOTH}` 时改为同事务�?outbox，OutboxRelay 异步取行�?`ExecutionCommandMqPublisher` 真正投递；`aggregate_type` 固定 `EXECUTION_COMMAND` 防止后续统一 outbox 语义发散�?*Phase 2H ②b�?026-07-15）扩展为�?CONFIRMED + 可对 NACK/超时回退重试�?* Flyway V20 �?`status` �?VARCHAR(32) 收敛�?SMALLINT（兼�?`PENDING/SENT/FAILED/CONFIRMED` �?`0/1/2/3`），新增 `last_sent_at` / `confirmed_at`，CHECK 约束扩展�?`0/1/2/3`，重�?PENDING 部分索引并新�?`idx_agent_command_outbox_sent_scan` SENT 部分索引；`AgentCommandOutboxStatus` 新增 `CONFIRMED(3)`，`AgentCommandOutboxService` 新增 `listExpiredSentForRetry / markSent(id,sentAt) / markConfirmed(id,confirmedAt) / markFailedFromSent / markFinalFailedFromSent`；`ExecutionCommandMqPublisher` 新增 `publishWithCorrelation(command, correlationKey)` 返回 `CorrelationData`，`OutboxRelayTask` �?`publishWithCorrelation` 后异步注�?confirm 回调，区�?ACK/NACK/return/超时四种路径，命�?ACK 且无 return �?`markConfirmed`，其它路径走 SENT→PENDING 指数退避回退�?SENT→FAILED 终态；`application.yml` 开 `publisher-confirm-type: correlated` + `publisher-returns: true`，`RabbitMQConfig` 注册 `ConfirmCallback`/`ReturnsCallback` �?`setMandatory(true)`；`OutboxRelayTaskTest` 用例�?5 扩到 7（新�?Confirm NACK、SENT 超时）。本轮明确不做：Poller 降级、`OutboxCompensationTask` 新增调度（沿�?`OutboxRelayTask`）、DLQ、per-event 业务级熔断、旧 `ExecutionCommandMqPublisher.publish()` 静默丢失 confirm future 的清理（单独立项 R2�?| 继续�?T4 RabbitMQ E2E 失败可恢�?�?T5 Poller 降级；T6 §5.2 后置 |
| N2 | 可配置工作流模板 | 未落�?| 缺模板表、模板管理、模板化调度入口�?Team 编排 | 后续独立迭代 |
| N3 | MCP Server 工具�?| 已交�?| MCP SSE 主通道工具链已可用，已具备外部 Agent 最小执行闭环能力：`pullTasks/ack/claimSubTask/heartbeat/uploadArtifact/submitResult/reportBlocked/getAgentStatus`（其�?`submitResult` 对接统一回写入口，`reportBlocked` 记录阻塞原因证据链） | 保持现状 |
| N4 | 心跳与在线判�?| 已交�?| 在线态三件套、在线计算态与巡检收敛已具�?| 保持现状 |
| N5 | 熔断降级 | 已交�?| per-agent 熔断与同角色替补策略已具�?| 保持现状 |
| N6 | 执行命令消费与结果回�?| 已交付（�?T5 Poller 兜底 + Validator 启动�?fail-fast 闭环�?| MQ 主链已连通：Phase 2D MQ Consumer 骨架 / Phase 2E 生产端接入（`ExecutionCommandMqPublisher` + `dispatch-mode` 四挡 + 双开�?+ DispatchValidator producer-fail-fast�? Phase 2F 两个阻断性修复（事务时机 `afterCommit` + 显式 JSON 序列化）/ Phase 2G 双路消费 E2E（DB CAS + Redis + `event_consumption_log` 三层幂等抵消�? Phase 2H ②a Outbox 最小闭环（`agent_command_outbox` 同事务写�?+ `OutboxRelayTask` 1000ms 扫描 + Redis 锁）/ Phase 2H ②b Confirm/Retry（`PENDING/SENT/CONFIRMED/FAILED` 四�?+ `CorrelationData` + publisher confirms + Confirm-aware Retry + SENT 超时回退�? T4 Outbox ②b E2E（S1 broker NACK / S2 mandatory return / S3 confirm timeout / S4 control happy path 四场景实测，N1 可标已交付证据）�?*T5�?026-07-15）Poller 兜底重塑 + Validator 闭环�?* `AgentExecutionProperties.ConsumerMode` 三种模式语义全部重塑�?"Poller 仅作孤儿/超时/补偿兜底"，主消费路径�?`MqExecutionCommandConsumer` 或本地事务事件承担；`ExecutionCommandPoller` 删除 `listAllPending` 分支，所�?consumer-mode 统一�?`listOrphanPending`，`trigger` 前缀恒为 `poll-recovery:`、timeline 事件恒为 `sub_task_execution_command_poll_recovery`；`ExecutionDispatchValidator` 新增 `consumer-mode �?{POLLER, BOTH}` + `consumer-enabled=false` 启动�?`IllegalStateException` fail-fast（阻�?"主消费路径全关但 Poller 仅兜底，PENDING 永远不被消费" 的事故形态），保�?`dispatch-mode �?{MQ, BOTH}` + producer 缺失 fail-fast �?consumer=false WARN；`application.yml` `helloai.mq.execution-command.consumer-enabled` 默认�?`false �?true` + 灰度节奏注释（MQ 环境就绪 �?主链稳定 �?可选退�?EVENT）；`AgentExecutionRecordService.listAllPending` �?`@Deprecated` 兼容保留；`ExecutionCompensationTask` 保持独立（不并入 Poller，职责边界清晰）；`ExecutionCommandPollerTest` 删除 PollerMain 嵌套类（5 �?listAllPending 主路径用例）+ 新增 DowngradeConsistency 5 用例；`ExecutionDispatchValidatorTest` 新建 14 用例覆盖 DispatchModeFailFastOnProducer / DispatchModeFailFastOnRelay / ConsumerModeFailFast / DispatchWarnOnConsumerDisabled / ValidCombinationsAndPriority 五类场景；`scripts/powershell/verify-poller-e2e.ps1` 已升级到 v3.1，并�?**2026-07-16 最新实�?runTag=`20260716-174605` �?S1-S5 = PASS 15 / FAIL 0**：pre-flight 增强为健康检查重�?+ `listening=` 诊断；mock execution hard gate 前移�?prepare 之前；S2/S4/S5 改为独立 `ASSIGNED` sub_task 隔离以对�?`startIfNeeded` 契约；S4 �?orphan age 调整为“超�?poller threshold 但不�?30s timeout compensation”窗口，验证 `polled=3 / progressed=3 / poll_recovery distinct_sub_tasks=3`；S5 验证 `last_attempt_at`、`sub_task_execution_command_poll_recovery`、`poll-recovery:` trigger �?`rogue_consume_events=0` 四项证据链均�?Poller 独自留下。结果回写入口收口为 `ExecutionResultHandler.handleReport(ExecutionResultReport)`，平台内执行链与外部 MCP `submitResult` 统一走该入口。`AsyncExecutionResultConsumer` 改造后置仍待后续独立轮�?| 保持现状；S5 最小等价验证已具备，且 **`scripts/powershell/verify-execution-dispatch-guard.ps1` 已实�?G1/G2/G3 = PASS 12 / FAIL 0**（G1/G2 fail-fast + exitCode 1 + 日志命中期望片段 + 6565 �?Listen，G3 `/api/health` 200）；S6 已从"手动 MQ-isolation 重启验证"重定义为独立的启动期 fail-fast 守卫脚本，不再塞�?Poller E2E——因�?T5 之后�?S6 组合会被 `ExecutionDispatchValidator` `@PostConstruct` 直接拦截，属"被守卫拦截的非法部署形�?而非"能跑的验�?�?*2026-08-03 依赖感知双轨上下文注入（§6.43）：** 执行链上下文升级�?直接前置的结构化摘要（`findRecord` 精确取单条）+ 完成内容本体（物化附件优先、`context.lastExecution.output` 回退�?双轨注入（`## 依赖产出参考（直接前置）` 章节，多前置按声明顺�?HashMap 全量收集防覆盖）；Phase A JSONB `appendExecutionRecord`/`initialize` �?taskId 粒度分段锁（多前置并行回填不互覆，单实例安全/多实例需�?Phase B �?Redis 锁）；Phase B `task_running_spec` 独立表行级天然无覆盖竞态；`buildExecutorPromptSection` 去掉全量 executionRecords 摘要段（�?Baseline + ContextSummary）；`scripts/shell/verify-deps-context-e2e.sh` 真实环境 PASS——双前置并发完成后下�?prompt 同含两前置产出（`sub_task_spec_context_loaded` payload depCount=2/loadedCount=2/truncatedCount=0/degraded=false）�?|
| N7 | 健康检查改�?| 已交�?| Reconcile、离线重分配、兜底收敛已具备�?*2026-08-10 增量（�?.56�?*：redispatchOfflineSubTask �?isReady 依赖守卫，依赖未就绪保持 PENDING + sub_task_dispatch_skip_dependency | 保持现状 |
| N8 | 网页�?AI 浏览器接�?| 未落�?| 只有枚举与预留，没有真实接入模块 | 后续独立迭代 |
| N9 | Provider 配置�?ChatClient 复用 | 已交�?| Provider 配置入口已统一（`helloai.providers`�? provider/model 解析已收口（`AgentProviderResolver`�? `ProviderChatModelCache` �?(provider, baseUrl, apiKey 指纹) 缓存 ChatModel 实例（避免每�?new DeepSeekChatModel）；`DeepSeekProviderChatClientFactory` 已接入缓存，SHA-256 指纹保证明文 API Key 不入 cache key�?*2026-07-30 增量�?* 修复 `AgentProviderProperties` 前缀绑定失效（`helloai.providers` 前缀 + `providers` 字段导致实际绑定路径�?`helloai.providers.providers.*`，yml 从未绑上；改�?`helloai` 前缀）；`ProviderConfig` 增加 `api-key`（配置即"已生�?）；新增 `LlmProviderCatalogService`（枚举已配置 provider 的可用�?= apiKeyConfigured && factorySupported�? `GET /api/admin/agents/llm-providers` 目录接口 + 注册 API_KEY_LLM Agent 时平台密钥自动补�?credential_vault（尽力而为、已�?ACTIVE 凭证不覆盖）；yml 预置 moonshot / minimax / dashscope 配置段（参�?springai 项目，缺 Factory 实现前目录标记不可用）�?*2026-08-07 增量（平台配置动态化，�?.51）：** 4 �?provider �?yml api-key 全部置空（含 spring.ai.deepseek），平台�?API Key 改由管理员在"系统设置"页动态配�?轮换（AES-GCM 加密写入 credential_vault PLATFORM 级，owner_id 固定 0，Flyway V45 放开 owner_type CHECK）；`PlatformProviderConfigService` 统一解析（API Key = vault ACTIVE > yml > null；baseUrl/默认模型 = sys_config > yml > Factory 内置默认），保存�?`ProviderChatModelCache.clear()` 实时生效；`LlmProviderCatalogService` 目录可用性改读配置服务；新增 `AdminProviderConfigController`（GET /api/admin/platform/providers/list + PUT /{provider}/api-key + PUT /{provider}/settings�? 前端系统设置�?模型配置"区块；外网地址断层修复（`AgentBaseUrlResolver` 统一解析 sys_config["helloai.base-url"] > yml > 请求推导 > localhost，getMySkill/onboarding 共用）；排除 `DeepSeekChatAutoConfiguration` 修复 api-key 置空后的启动�?fail-fast；e2e 脚本 `scripts/powershell/verify-platform-config.ps1`�?ReadOnly 模式不写库；**2026-08-07 已在 local profile 完整写库实测 PASS 22 / FAIL 0**）�?*2026-08-14 增量（模型多选配置重构，§6.89）：** V49 新增 `llm_provider_model` 关联表（Provider 多模型，默认模型 is_default 必一�? 内置 4 厂商 11 模型种子�?026-08-14 官网口径�? �?`default_model` 迁移；后�?`LlmProviderModelService/QueryService` + `AdminLlmProviderController` 6 个模型端点（list/saveAll/addModel/deleteByName/toggleByName/setDefaultByName�? `AgentController.listAvailableModels`（注册下拉）+ 注册/编辑 `validateModelUniqueInRole` 角色模型唯一性强制校验；前端 `settings.ts` 模型 API + `Settings.vue` 模型多�?UI（内置只�?自定义可�?默认模型单选）；单�?48 全绿 + `verify-llm-provider-models.ps1` e2e 38/38 ALL PASSED；同步修复：Provider 创建/模型重名的物理唯一约束 vs 逻辑删除冲突（V50/V51 改部分唯一索引 `WHERE deleted=0`）、registerOrGet 先建后验�?Agent（注册前预校验）、toggleModel 不禁最后一个启用模型、deleteById 级联物理清理模型�?*2026-08-14 §6.91 前端注册弹窗模型分组下拉对接（`AgentList.vue` 内部 LLM 可选模型，留空走默认）+ 同角色同模型唯一性实测确认（重复注册被拒、同角色不同模型允许�?* | 保持现状�? �?ProviderChatClientFactory 已全部存在且均为 §6.51 改造对象（DeepSeek 官方 SDK + Moonshot/DashScope OpenAI 兼容 + Minimax Anthropic 兼容） **2026-08-24 后补注记（V59，§6.153）**：Provider 管理交互与凭证验证增量已交付——「添加供应商」重塑为两步式「添加模型」弹窗（供应商卡片网格 + 表单弹窗，预置供应商添加=覆盖写 Key），`llm_provider.billing_type` 计费类型字段落地（默认 API_KEY，TOKEN_PLAN/CODING_PLAN 置灰预留），LLM Provider 与博查 API Key 连通性验证端点（`verifyApiKeyById/{id}` / `verifyWebSearchApiKey`，raw HTTP 最小探测，结果收敛 success/message 绝不抛异常），Agent 编辑显示所选模型；详见增量历史末尾 2026-08-24 条目 |
| N10 | 工牌模式 + `credential_vault` | 已收�?| 最小模型、绑定与托管语义已具备；Agent API Key 的最小轮换语义已落地（`ACTIVE / EXPIRED` + 审计备注）�?*2026-08-07 增量�?* `owner_type` 放开 PLATFORM 级（Flyway V45），平台级凭证按 provider 唯一、owner_id 固定占位 0，支�?先启动后配置"（�?.51）�?*2026-08-13 收口（�?.81，C2）：** 盘点实证无明文密钥存量需迁移（`agent.api_key` 为工�?consumerToken、`sys_config` �?llm 键、真�?Key �?AES-GCM 加密�?vault）；读取路径已全�?vault 化且�?17 例单测锁定优先级（Agent �?`CredentialVaultBindingServiceTest` 6 例——无 vault �?null 不回落工�?/ secretRef 环境变量优先 / 解密路径；平台级 `PlatformProviderConfigServiceTest` 11 例——vault > yml 过渡期双�?/ secretRef 空回退 yml）；权限颗粒度审计闭环（`/api/**` 全量鉴权 + CredentialController admin-only + MCP 零凭证接�?+ 执行链按 owner 隔离）；存量治理交付孤儿凭证清理 SQL�?1 �?owner 已删记录逻辑删除�?4 ACTIVE + 27 DISABLED，待用户执行；现�?agent 12 条保留） | 保持现状；清�?SQL 执行后可关闭 |
| N11 | 调度策略：外部优�?+ 空闲优先 + LLM 保底 | 已交�?| 候选选择策略收口为可配置项（`preferExternal` / `requireIdle` / `forceAccessType` / `autoAssignOnCreate`），并已落地“外�?Agent 连续失败阈值后自动回退到平台内 API_KEY_LLM”闭环：V17 �?agent.consecutive_failure_count/last_failure_at/last_fallback_at + sub_task.external_fallback_count；`ExternalAgentFailureTracker` �?`ExecutionResultHandler.handleReport` / `ExecutionCompensationTask` / `AgentHealthCheckTask` 三处统一累加与重置；`SubTaskDispatchService.redispatchForFallback` 绕过 `AgentSelector`（不�?preferExternal 影响）直接选同角色 API_KEY_LLM Agent；`ExternalAgentFallbackTask`（helloai-job�?0s 周期 + Redis 锁）扫描超阈�?CLI_CLIENT Agent 触发重新分发；阈值与冷却期可�?`helloai.dispatch.fallback.{failure-threshold,cooldown-minutes}` 调节；�?.52 人工介入闭环�?026-08-10）：返工达上限自动核验写 context.manualIntervention 标记（SubTaskReviewService），执行密集任务不回退给无本机能力�?API_KEY_LLM（redispatchForFallback 能力预检），REVIEW 孤儿扫描排除条件�?�?review_record"改为"有标�?（防返工上限卡死永久无人兜底），前端子任务详情页人工介入面板（全�?Agent 选择器：外部 trae/qoder/claudecode + 内部 LLM，驳回改�?直接通过）；core 426/426 全绿 + vue-tsc 0 error **2026-08-10 增量（�?.56 依赖守卫 + 执行密集能力预检全链路下沉）**：redispatchOfflineSubTask �?isReady 依赖守卫（依赖未全部 DONE 保持 PENDING + timeline sub_task_dispatch_skip_dependency）；ResilientDispatcher.assignNext 主路�?+ assignNextFallback 替代选人增加执行密集能力预检（记 sub_task_dispatch_skip_no_capability + context.manualIntervention[dispatch_skip_execution_dense]）；SubTaskReviewService 对执行密集且提交者无本机能力跳过自动核验（review_skip_execution_dense_no_capability）；SubTaskPendingOrphanTask / ExternalAgentFallbackTask 两条 PENDING 兜底巡检跳过�?manualIntervention 标记任务；core 全量 + job 全量 BUILD SUCCESS（新�?10 回归用例）；**2026-08-11 增量（�?.57 人工驳回重置返工计数�?*：ReviewService 人工驳回（createReview REJECTED）改�?SubTaskService.reworkFresh——reworkCount 归零 + 清除 context.manualIntervention + 换派（可空保持原执行者），改派后的新执行者提交走正常自动核验（防 reworkCount 残留命中 skip_max_rework 跳过审核、无节点流转）；自动驳回仍走 rework() 累加（达上限�?REVIEW 等人工），两条路径语义分工；**2026-08-11 增量（�?.58 离线失败计数语义修正�?*：AgentHealthCheckTask 仅当离线时有在跑任务（ASSIGNED/IN_PROGRESS）才 recordFailure—�?提交后停止心�?的静默待�?Agent 不再每完成一单累�?1 次失败，�?N11 阈值误回退；executor SKILL.md �?提交不等于下�?心跳警示块；**2026-08-11 增量（�?.59 任务�?agentPolicy + 能力声明，V47�?*：task.agent_policy JSONB（plannerAgentId / executorAgentIds[] / reviewerAgentId / fallbackPolicy AUTO·RESTRICTED·NONE / difficulty LOW·MEDIUM·HIGH�? task.required_skills + agent.skills 三列；TaskAgentPolicy 静态工具类收口全部解析（防御式回落默认）；AgentSelector 新增 AgentSelectionConstraints（白名单 + 技�?AND 全匹配）贯穿 pickPreferred/pickAlternative 3 参重载与 ResilientDispatcher assignNext 3 参（首�?fast-fail �?AgentUnavailableException �?受约�?fallback）；Planner/Executor 四入�?Reviewer 选人链读�?policy（指定失效回退自动）；N11 回退受策略约束——fallbackPolicy=NONE �?difficulty=HIGH 跳过回退改人工介入（sub_task_fallback_skip_policy + markManualIntervention，不�?LLM），RESTRICTED 仅回退 executorAgentIds �?API_KEY_LLM（不在集合等�?NONE）；core 指定 5 测试�?90/90 全绿（含技能过�?指定失效回退/N11 HIGH 不落 LLM�?| 保持现状 |
| N13 | AgentHub V3：长连接门铃通知通道（外�?Agent 秒级唤醒�?| **已搁置（2026-08-07�?*：PR-1~PR-4 均已交付�?E2E 实测通过；技术瓶颈——外�?AI Agent（安装版 / CLI 版）均为单向执行器，无法处理平台推送的门铃信号�?Agent 端代码不可修改，通道无人消费故搁置；代码保留运行，SKILL/脚本已下线门铃内�?| �?`doc/archive/HelloAI_门铃通知通道设计.md` 补一条“服务端 �?外部 Agent 单向 SSE 门铃”，把外�?`CLI_CLIENT` 感知时延�?0~30s 轮询级降到秒级；MCP 仍是唯一任务协议层，门铃丢失无损回退轮询�?*PR-1 门铃内核�?026-07-17）：** `DoorbellProperties`（helloai-common，`helloai.doorbell.{enabled=true,emitter-timeout-ms=1_800_000,keepalive-interval-ms=15_000}`�? `DoorbellSignal`（`type/eventType/refType/refId/serverTime` + 静态工�?`connected()/keepalive()/inbox(...)`，`@JsonInclude(NON_NULL)`�? `DoorbellRegistry`（进程内 `ConcurrentHashMap<Long,SseEmitter>`，register 关旧建新 / unregister �?`remove(k,v)` 值条件删防误删新�?/ get / isConnected / size�? `DoorbellService`（`connect(agentId)` 校验 enabled、建 emitter �?onCompletion/onTimeout/onError 清理回调、register 后立即回�?`connected` 握手；`ring(agentId,signal)` 未连返回 false 尽力而为；`disconnect` / `connectionCount`；`doSend` 失败静默注销�? `AgentDoorbellController`（helloai-api，`GET /api/agents/doorbell/sse`，`produces=text/event-stream`，`@RequestAttribute("_authId") Long agentId` 复用 AuthInterceptor `/api/**` Bearer 鉴权链）�?*PR-2 响铃接线�?026-07-17）：** 一处收口——`AgentInboxService.send()`（三条通知路径 TaskController 直发 / SubTaskService 状态流�?/ MQ NotificationConsumer 全经此方法）`save(inbox)` 成功�?`publishEvent(new InboxMessageCreatedEvent(agentId,eventId,eventType,refType,refId))`，`DuplicateKeyException`（`(event_id,agent_id)` 联合唯一）幂等分�?return 不发事件避免重复响铃；新�?`InboxMessageCreatedEvent`（helloai-core/event�? `DoorbellRinger`（`@Async("doorbellExecutor") @TransactionalEventListener(phase=AFTER_COMMIT)` �?`doorbellService.ring(...)`，异常静默靠轮询兜底，与 Outbox / 本地执行事件 AFTER_COMMIT 时序哲学一致，保证“先落库后响铃”）+ `DoorbellExecutorConfig`（helloai-start，`doorbellExecutor` 2/4/500 + `DiscardPolicy`，与 `executionCommandExecutor` 池隔离，响铃永不拖累主链路）。不新增 Flyway / �?/ MQ 队列 / WebSocket 依赖（门铃是纯运行时连接态，`SseEmitter` �?WebMVC 原生零新增依赖）。单�?17 例全绿：`DoorbellRegistryTest`(5) / `DoorbellServiceTest`(6) / `DoorbellRingerTest`(4) / `AgentInboxServiceTest`(2，`spy(new AgentInboxService(publisher))` �?`save` 验证�?不发事件)，全 reactor MAIN `install` BUILD SUCCESS�?*PR-3 值班鉴权收口�?026-07-17）：** `DoorbellService` 构造注�?`AgentDutyLeaseService`，`connect(agentId)` �?enabled 校验后前�?`isOnDuty(agentId)` 闸门（无 ACTIVE 值班租约�?`BizException(500)` 拒连，先打卡再接电话，设�?§6.1）；反向断连用事件解耦规�?`DoorbellService �?AgentDutyLeaseService` 构造循环——新�?`DutyLeaseClosedEvent`（helloai-core/event），`AgentDutyLeaseService` �?`@RequiredArgsConstructor` + `ApplicationEventPublisher`，`closeLease`（覆�?checkOut）与 `expireLeases`（覆盖租约到期）在关闭行�?>0 时发事件，`startLease` 防御性关旧不发（避免�?checkIn 即断连）；`DoorbellDutyListener`（`@Async("doorbellExecutor") @TransactionalEventListener(AFTER_COMMIT)`，与 `DoorbellRinger` 对称）监听后 `disconnect(agentId)`，异常静默。端到端脚本 `scripts/powershell/verify-doorbell-e2e.ps1`（S1 未在岗建连→HTTP 500 code=500 / S2 在岗建连→HTTP 200 + `event:connected` 握手�?/ S3 租约到期→`DutyLeaseExpirationTask` �?EXPIRED→事件驱动主动断门铃、SSE 后台 job 结束），遵循规则 6 UTF-8 with BOM + 单引号拼�?+ runtime �?ASCII，`Parser.ParseFile` 自检 PARSE-OK。单�?22 例全绿（新增 `DoorbellDutyListenerTest`(4) + `DoorbellServiceTest` 补未在岗拒连用例�?7），�?reactor MAIN `install` BUILD SUCCESS�?*PR-4 保活帧调�?+ 双心跳（2026-07-17）：** `DoorbellKeepaliveTask`（helloai-core，`@Scheduled(fixedRateString=helloai.doorbell.keepalive-interval-ms 默认15s)`）本地无锁遍�?`DoorbellRegistry` 对所有活跃连接广�?`keepalive` 帧穿透反代空闲超时—�?*�?`DutyLeaseExpirationTask` �?Redis 选主锁相�?*：SSE 连接是进程内状态，每实例必须保活自�?Registry 里的连接，选主只让一台跑会导致其他实例连接被反代掉断；新�?`DoorbellRegistry.forEach(BiConsumer)`（弱一致遍历）+ `DoorbellService.broadcastKeepalive()`（坏连接�?`doSend` 静默注销，返回送达数）。双心跳：新增开�?`helloai.doorbell.refresh-heartbeat`（默�?false，保守——SSE 单向 TCP 存活≠Agent 进程健康，避免僵尸连接掩盖真实离线），开启后�?`connect()` 建连顺带调一�?`HeartbeatService.seen(agentId)` �?`last_seen_at`（keepalive 轮不刷），失败静默不阻断建连；`DoorbellService` 构造注�?`HeartbeatService`（→AgentMapper/Redis 不回指，无循环依赖）。单�?33 例全绿（新增 `DoorbellKeepaliveTest`(4) + `DoorbellRegistryTest` 5�? + `DoorbellServiceTest` 7�?2），�?reactor MAIN `install` BUILD SUCCESS | **2026-08-07 搁置**：任务感知一律改�?`pullTasks` 轮询，Redis Pub/Sub fanout 演进项不再推进（详见迭代记录 §6.50）。历史证据保留：**PR-3 已交付（2026-07-17�?*：`isOnDuty` 建连闸门 + `checkOut`/租约到期事件驱动主动断连 + `verify-doorbell-e2e.ps1` 三场景验证脚本，单测 22/22 全绿。剩：运行时端到端冒烟已�?2026-07-17 真实环境实测 **ALL PASSED**（agentId 2077974111691915266：S1 未在岗建�?HTTP 500 code=500 / S2 在岗建连 HTTP 200 + `event:connected` 握手�?/ S3 租约到期�?`EXPIRED,lease_expired` + 事件驱动主动断连、SSE 后台 job Completed）；仅余多实�?Redis Pub/Sub fanout（设�?§12 演进项，单实例部署下用不到）�?*PR-4 已交付（2026-07-17�?*：保活帧调度（每实例本地无锁广播 keepalive�? 双心跳（`refresh-heartbeat` 默认关，建连�?`last_seen_at`），单测 33/33 全绿 |
| N12 | AgentHub V1 P0：值班租约闭环 + 值班优先调度 | 已交�?P0 + P1 + R2/R3 验证回执 + A2 �?1 段（STRICT 独占报锁�?| 2026-07-16 落地 AgentHub V1 P0 三件（checkIn / checkOut / DutyLeaseExpirationTask）真实环�?E2E：`agent_duty_lease` 表（`V1__init_all.sql` �?1508 行，AgentHub V1 T3 建表�?*注：早期文档误记�?V18，V18 实为 `event_consumption_log`**）承�?Agent 值班租约，状态机 `ACTIVE / CLOSED / EXPIRED`（`uk_duty_lease_agent_active` partial unique index 防止同一 Agent 多条 ACTIVE 行）；`AgentDutyLeaseService.checkIn` 赋予 Agent 值班权（workMode=NORMAL/STRICT/maxConcurrent/ttlMinutes�? heartbeat 联动；`checkOut` 主动闭锁并记�?closeReason（服务端参数�?`closeReason` 为准，旧字段�?`reason` 兼容）；`DutyLeaseExpirationTask`（helloai-job，`@Scheduled fixedRate=30_000` + Redis Lua 锁）扫描过期 ACTIVE 租约翻为 `EXPIRED`，close_reason=`lease_expired`；`AgentSelector.pickAlternative` 增加 `dutyRank` 排序（值班中的 Agent 优先于未值班），多候选用例排序时调用 `agentDutyLeaseService.isOnDuty(agentId)`，`lenient()` mock 避免单候选用�?UnnecessaryStubbing。`MyBatisPlusMetaObjectHandler.insertFill` �?`consecutiveFailureCount=0` 默认填充（v2.4 N11 字段遗漏修复）。`McpMcpServer` �?`checkIn` / `checkOut` 工具（`@Tool` + `@ToolParam`），认证上下�?`requireAuthId(sessionId,_sessionId)` 覆盖客户端传�?agentId。`agent_mcp_server` �?V21 seed 新建 Agent 自动启用 `checkIn/checkOut`（partial unique index `idx_ams_agent_tool WHERE deleted=0`，ON CONFLICT 子句必须显式�?partial 条件）。E2E 脚本：`verify-agenthub-duty-e2e.ps1`（S1 checkIn / S2 checkOut / S3 Lease 过期扫描 3 场景实测通过 ALL PASSED）。skill 规则 6 + D8 同步落地 PS 5.1 脚本编码防护四原则�?*P1 值班只读报表接口已交�?*（`AgentDutyLeaseController`：`GET /api/admin/duty-leases` 分页列表 + `GET /api/admin/duty-leases/overview` 状态概览，`DutyLeaseResponse`/`DutyOverviewResponse` DTO，`AgentDutyLeaseService.listLeases/countByStatus` 只读查询，列�?agentName �?nameCache 避免 N+1）�?*P1 dashboard 前端接入 + R2/R3 收尾已落地（2026-07-16�?*：`helloai-ui/src/api/duty.ts` + `types/duty.ts` + `views/duty/DutyLeaseList.vue`（分页表 + 状�?Agent ID 过滤）；`Dashboard.vue` �?“Agent 值班概览”区块（4 �?stat �?+ “查看全部租�?→�?链接，失败仅 console.warn 不阻断主图）；`router/index.ts` 注册 `/duty-leases`、`MainLayout.vue` 增加 `Clock` 图标菜单项；`ExecutionCommandMqPublisher.publish(ExecutionCommand)` 旧入口删除（无调用方、消除第二套时序假设），单测同步删除 AFTER_COMMIT 用例、改�?`publishWithCorrelation`；V22 `agent_command_outbox_backfill_timestamps` 回填 status=1/3 历史�?`last_sent_at`/`confirmed_at`（用 `update_time` 保守近似，IS NULL 守卫保证幂等）。验证脚�?`scripts/powershell/verify-dashboard-duty-leases.ps1`（S1 overview 字段�?/ S2 list 分页结构 / S3 status=ACTIVE 过滤生效 / S4 V22 backfill 抽查）�?*A2 �?1 �?STRICT 独占报锁已交付（2026-07-17�?*：`WorkMode` 枚举（`AUTO/STRICT` + `lenientParse`/`strictParse` 双策略，DB 读取宽容/MCP 入参严格校验非法值→BizException 拒绝）；`AgentSelector.pickFromCandidates` �?`.filter(a -> !isOnStrictDuty(a.getId()))`（STRICT Agent 退出别人失败后�?pickAlternative 替补池抢派）+ `isOnStrictDuty` 私有方法�?`getActiveLease` + `lenientParse` 判定；`McpToolService.checkIn` �?`strictParse` 严验入参、落库用 `mode.name()` 字符串；`AgentSelectorTest` 新增 5 �?STRICT 用例�?9/19 全绿）覆盖跳�?STRICT 在岗 / 全部 STRICT 时返�?/ 无租约当 AUTO / 脏值宽�?/ 异常回退 AUTO；`verify-agenthub-duty-e2e.ps1` 追加 S6.1/S6.2/S6.3 三子场景�?*真实环境实测 ALL PASSED**，S6.1 workMode=STRICT→DB work_mode=STRICT、S6.2 workMode=strict 小写→DB work_mode=STRICT 大小写不敏感、S6.3 workMode=BOGUS_VALUE→BizException 拒绝、lease count 不增）。本轮明确不做：A2 �?2 段（动�?TTL 自适应）、A2 �?3 段（concurrency 预扣）、调度重新计入值班权重以外的二次排�?| **已交�?P0 + P1 + R2/R3 + A2 �?1 段（STRICT 独占报锁�?*；剩�?**A2 �?2 段（动�?TTL 自适应，�?.83�?* �?**A2 �?3 段（concurrency 预扣，�?.86�?* 均已交付；E2E 脚本 `verify-agenthub-duty-e2e.ps1` 已可重复回归 |
| N14 | 真实 AI Agent 子任务自主闭�?| 场景 1 已实�?/ 场景 2~5 待测 | 2026-07-17 脚本化五步闭环（verify-onboarding-submit.ps1 PASS=12）证�?API 链路通；**2026-08-14（�?.94�?*：场�?1 happy path 已由真实 AI（本会话 Qoder 作为 EXECUTOR）按 SKILL.md 全协议链实测通过——注册→getAgentStatus 自检→checkIn 打卡→pullTasks 值守→listAvailable+claimSubTask 自主认领→getDepsSummary→heartbeat（认领后自动续约�?maxTTL 240min，E1/A0-8 实证）→uploadArtifact→submitResult（manifest 多文件物�?2 附件内容逐字匹配 + 对话�?displayText 概览）→REVIEW 流转→checkOut 签退→teardown 零残留；协议事实：自主认领（claimAtomic）不产生 sub_task.assigned 收件箱消息，REVIEW 通知�?PLANNER�?*2026-08-15/16（�?.95�?*：Trae（trae-excutor）自主轮询完成真实购物车任务�? 子任务全 DONE�? 次返工），平台自动核验（inner-deepseek-pro-reviewer）内容级核验实战实证——Reviewer 真实读取附件字节，Round1 拦截 4 �?uploadArtifact 纯登记（MinIO 对象未传）「内容不可读」附件并驳回，Round2 读到大到 vue 源码 + diff 后通过；剩余场�?2 blocked / 3 超时替补 / 4 附件 / 5 双值班未测 | 按《项目进度》M5 场景矩阵继续执行，场�?1~5 全部通过后改标“已交付�?|
| N15 | Controller 分层红线收口（�?.3 + 3.x 包归位） | 已交付（2026-07-20�?| �?`doc/HelloAI_CODE_STYLE.md §6.3` �?1 条「禁止注�?Mapper」与�?2 条「禁止书�?SQL/QueryWrapper 条件」强制收�?6 个历史违�?Controller（ActivityController / AdminDashboardController / AgentDutyLeaseController / AttachmentController / DashboardController / FeedController）：删除 Mapper 注入、改为构造器注入 Service、查询与 QueryWrapper 全部下移至对�?Service 新增方法（ActivityLogService / AdminDashboardService / AgentDutyLeaseService / AttachmentService / DashboardService / FeedService）；`AdminDashboardService` �?`DashboardService` 不继�?ServiceImpl（跨 Mapper 聚合，返�?Map 避开 core→api DTO 依赖，�?.7 聚合看板语义）。同步完成两项包归位（git mv 保留历史）：`com.helloai.config` 2 个类（MyBatisPlusMetaObjectHandler / AdminInitializer）并�?`com.helloai.start.config`；`helloai-start/.../chat/DeepSeekProviderChatClientFactory` 移至 `helloai-core/.../core/agent/chat/provider`，`helloai-core/pom.xml` �?`spring-ai-starter-model-deepseek` 依赖。CODE_STYLE.md §6.3 待收口清单与 3.x start 配置类待收口段落同步收口。`mvn -DskipTests clean compile` 7 模块�?SUCCESS；迭代记录详�?`doc/log/HelloAI_迭代执行记录.md` §2026-07 Controller 分层红线收口 | 保持现状 |
| N16 | Planner 平台内自动拆解（需求→草案→确认→既有分发链） | 已交付（2026-07-28�?| V26 落地“需�?�?LLM 自动拆解 �?用户确认/拒绝 �?进入既有分发链”闭环：`SubTaskStatus` 新增 `PENDING_PLAN_REVIEW` 草案态（不参�?claim/分发/超时回收/统计，状态机仅允�?�?PENDING/CANCELLED 且任何状态不可转入）+ `TaskStatus` 新增 `PLANNING`（防重复触发�? Flyway `V26__planner_plan_review_status.sql` 重建两张�?CHECK 约束；`PlannerAnalysisService`（core/planner 包，编排收口 core 对齐 §6.3）：decompose（校�?PENDING + 已有�?CANCELLED 子任务拒�?+ CAS lambdaUpdate �?PLANNING 防并�?+ `AgentSelector.pickPreferred(PLANNER)` �?API_KEY_LLM Agent + `prompts/planner-decompose.md` 模板渲染 + `PlatformAgentExecutionService.executeSync` �?LLM + strip markdown fence 容错解析 JSON 数组（title 必填/数量�?0/priority 归一化）+ saveBatch 草案落库 + timeline `task_plan_generated`；失败路�?CAS 回退 PENDING + `task_plan_failed`�? listDrafts / confirmPlan（草案逐条 changeStatus �?PENDING，Task �?IN_PROGRESS，按 `autoAssignOnCreate` 逐条 `dispatchPendingSubTaskAuto`，与手工创建子任务分发路径同构）/ rejectPlan（草案翻 CANCELLED 保留审计，Task 回退 PENDING 可重新拆解）；`TaskController` 四个薄入�?`POST/GET /{id}/plan` + `/plan/confirm` + `/plan/reject`；旁路排查确认全链路安全（job 定时任务/redispatch/XML mapper/dashboard 统计均精确匹配既有状态，草案态天然隔离）；外�?Planner `skills/planner/SKILL.md` 升级�?executor 版同等水准（MCP 全套 10 工具 + 门铃 + 常驻打卡 + 拆分四要�?+ 六步排障闭环）；单测 `PlannerAnalysisServiceTest`(13) + `SubTaskStateMachineTest` �?V26 用例(3)，helloai-core 244 全绿；端到端脚本 `scripts/powershell/verify-planner-decompose.ps1`（拆�?确认/拒绝/重复拆解拒绝四路径，待真实环境实测）。本轮明确不做：前端规划确认页、planner 专用 MCP 工具（decomposePlan）、子任务依赖 DAG；不�?`SubTaskAutoExecutionDispatcher` accessType 过滤 | 保持现状；~~前端规划确认页后续独立迭代~~ �?2026-07-29 已交付（拆解链前端补全，�?§6.18）；~~端到端脚本与浏览器闭环实测待真实环境回归~~ �?2026-07-29 真实环境已收口（zsh �?`verify-planner-decompose.sh` e2e 12 步全�?+ 浏览�?UI 闭环实测通过，见 §6.18）�?*2026-07-30 增量**：`PlannerAnalysisService.orderByDependency`（稳�?Kahn 入度拓扑排序）使草案审阅/分发按依赖正序展示（根节点在前，dependsOn 恒指向更靠前行），见 §6.27�?*2026-08-11 增量（�?.59，V47�?*：拆解链�?Planner 增加任务级指定语义——`PlannerAgentPicker.pickForTask` 优先�?`task.agent_policy.plannerAgentId`（任务创建时显式指定），其次会话钉住，最后自动选择；指定失效（删除/禁用）回退自动选择不阻断拆解�?*2026-08-19 增量（拆解异步化改造，§6.103�?*：decompose �?HTTP 线程同步�?LLM（最坏约 245s，前�?axios 120s 超时倒挂）改为「提交即返回 + 异步执行 + 前端轮询 + 兜底回收」——同步段收窄为校�?+ CAS �?PLANNING + timeline `task_plan_async_submitted` + 提交 `plannerDecomposeExecutor` 线程池（2/4/20，`helloai.planner.decompose` 外置，池�?AbortPolicy �?回退 PENDING + BizException「拆解排队已满」）后立即返回空列表（`planById` 契约 `R<List<SubTask>>` 不变，对 §6.7 有意例外，TaskController 零改动）；LLM 段迁�?`PlannerDecomposeAsyncService/Impl`（`@Async` + 入口幂等守卫�?PLANNING 才执�?+ 失败闭环回退 PENDING + `task_plan_failed` 不重抛），新�?`task_plan_llm_call_end` timeline（costMs/finishReason/tokenUsage/success 观测缺口补齐）；`PlanningTimeoutTask`（helloai-job�?0s + Redis token 锁）CAS 回收卡死�?10 分钟�?PLANNING 任务（PLANNING→PENDING + `task_plan_timeout_recovered`，与异步成功路径天然互斥）；前端 TaskList 提交即返�?/ PlanReviewDialog 3s 轮询�? 分钟上限，草案到达或回退 PENDING 即停�? RequirementChat 文案改异步语义。测试拆分：`PlannerAnalysisServiceTest` 重写 13 �?+ 新增 `PlannerDecomposeAsyncServiceImplTest` 12 �?+ `PlanningTimeoutTaskTest` 6 例全�?| 保持现状 |
| N17 | Planner 对话式需求澄清（模糊想法→多轮追问→终稿→建任务→顺路拆解） | 已交付（2026-07-29�?| V29 落地"第二步立�?入口：新�?`requirement_conversation`（title/status ACTIVE/FINALIZED/ABANDONED + CHECK、task_id **软引用无 FK**（不加入 deleteTaskCascade 引用面，删任务允许悬挂）、final_title/final_description 终稿暂存、round_count�? `requirement_message`（conversation_id/role user·assistant/content/seq），�?V19 范例全审计列 + 触发�?+ 逐列 COMMENT；`RequirementClarifyService`（core/planner�?*完整复用 PlannerAnalysisService 五段�?*：pickPlannerAgent 12 行刻意复制不抽象 + classpath 模板 `prompts/requirement-clarify.md`（占位符 `{{CONVERSATION_HISTORY}}` transcript 文本，协议严�?JSON 单对�?`{"type":"question"}` / `{"type":"final","title","description"}`�? executeSync（context �?conversationId + scene=requirement_clarify�? `stripToJsonObject`（照 stripToJsonArray 改花括号版）+ BizException；类不加事务，LLM 耗时不占 DB 事务）：create/sendMessage（轮数硬上限 20，超限引导手动建任务；LLM 失败 user 消息保留可重发）/finalize（校验终稿非�?�?�?Task PENDING + best-effort 通知 PLANNER 写收件箱（照 TaskController.create 通知段搬 core）→ 会话回填 taskId+FINALIZED �?timeline `task_created_from_clarify`�?abandon/listConversations（LIMIT 50�?detail；`RequirementConversationController` 六薄端点 `/api/requirement-conversations`；@MapperScan �?`com.helloai.core.planner.mapper`；前端独立页 `/requirement-chat`（会话列�?+ 气泡�?+ 终稿卡片「创建任务并自动拆解」→ finalize �?前端调既�?`taskApi.plan` �?�?`/tasks?review={taskId}` 自动开草案审阅弹窗；FINALIZED 只读 + 查看任务链接�? 菜单/路由/TaskList review query 接线；单�?`RequirementClarifyServiceTest`(13) 全绿（question/final 双路径、fence 容错、轮数上限、finalize 语义）；zsh 脚本 `scripts/shell/verify-requirement-clarify.sh` 真实环境 10 步全绿（创建→推进终稿→finalize 建任务→FINALIZED 拒发→plan 草案生成→abandon 回归）；浏览器闭环实�?8 步全�?console 0 error。本轮明确不做：SSE 流式（Doorbell SSE �?Agent 侧信号通道不可蹭）、终稿手动编辑（不满意继续对话让 LLM 修正）、会话删除（�?abandon）、列表分�?| 保持现状；演进项：SSE 流式输出、终稿人工微调、会话分页�?*2026-07-30 增量**：`RequirementClarifyService.regenerate` �?会话�?FINALIZED 且原任务已被删除"的悬挂恢复路径（复用终稿重建 PENDING Task，不重跑 LLM，抽�?`buildTaskFromDraft` 统一建任务逻辑），�?§6.27�?*2026-07-31 增量（V33 结构化选项式澄清）**：追问从纯文本升级为双模协议（`mode: structured / freeform`）——LLM 可输出结构化问题卡（每轮 �? 问、每�?2~4 选项、单�?多�?自定义补充、recommended 推荐标记�? `progress` 0~100 完成度自评（仅前端进度条展示，无业务分支）；Flyway V33 `requirement_message` �?`payload` TEXT 列一列两用（assistant 行存问题 JSON / user 行存 `selections` 选择快照 / 纯文�?NULL，与 V32 同款 TEXT 约定）；解析降级为一等公民（�?JSON 且不�?`"type"` �?原文�?freeform 追问落库不报错；structured 校验失败 �?降级 freeform 丢弃 questions）；content/payload 职责分离（content 由引导语+问题+选项合成可读文本�?LLM transcript 完整）；prompt 升级双模协议 + 五维度自检清单（业务场�?功能范围/性能并发/安全合规/交付预算）；前端 `StructuredQuestionCard.vue` 选项卡片 + 顶部进度�?+ 选项提交�?`selectedOptions`。单�?21 例全绿（+4：structured 落库/校验降级/�?JSON 降级/选项快照�? vue-tsc 0 �?+ `verify-requirement-clarify-structured.ps1` 真实环境实测 PASSED（structured 追问 progress=25 两问结构完整 �?选项提交 �?selections 快照落库 �?abandon）。见 §6.32�?*2026-08-03 增量（V39 Planner 对话双模式）**：单会话 `mode` 字段（CHAT 自由对话 / CLARIFY 方案澄清，NULL 老数�?CLARIFY 兼容），用户主导双向切换（to-clarify 置位落库 + 澄清模板基于全量历史重跑一轮；to-chat 仅置位不�?LLM），CHAT 走通用助手模板 `prompts/requirement-chat.md` 纯文本问答（�?JSON 协议、无 progress、无首轮联网搜索、独�?50 轮上限），意图词自动切换（「整理成方案/生成方案/落地实施」等命中即先落库�?CLARIFY、该条消息即澄清首轮、意图词永远放行保证转方案出口）；新会话缺省 CHAT，创建接�?`initialMode` 可快捷直�?CLARIFY；Flyway V39 `mode VARCHAR(16)` + CHECK 约束 + DO 验证块；`RequirementClarifyService` create 四参重载/sendMessage 上限按模式分�?doRound 意图置位/runLlmRound 按模式选模板与 scene/switchToClarify/switchToChat；`ClarifyMessageRequest.initialMode` + Controller 三端点；前端模式徽标 + 进度条与终稿卡条件化 + 「转为方案」按�?+ 新会话模式单选。单�?35/35 全绿�?14 ChatModeAndSwitch�? vue-tsc 0 �?+ `verify-planner-chat-dual-mode.sh` 真实环境 8 �?PASS（CHAT 纯文�?payload=NULL �?二轮�?CHAT �?to-clarify 产终�?�?finalize 建任�?�?意图词自动切 CLARIFY �?to-chat 反向仅置位）。见 §6.44�?*2026-08-03 增量（V40 意图词二次确认）**：去掉「转为方案」按钮，转方案收敛为「意图词触发 + 对话内二次确认」——CHAT 命中意图词（整理成方�?生成方案/落地实施等）不再自动�?CLARIFY，置 `pending_clarify_confirm` 标记并回复固定确认询问（不调 LLM、不加轮数）；用户回复确认词（确�?好的/开始吧等）或再次表达意�?�?转入 CLARIFY（该条消息即澄清首轮）并清标记；回复其他内容 �?清标记继续自由对话。Flyway V40 `pending_clarify_confirm SMALLINT NOT NULL DEFAULT 0`（按规范 §9.3 不用 BOOLEAN�? DO 验证块；自定�?`SmallIntBooleanTypeHandler`（SMALLINT↔Boolean，@TableField 显式指定不注册全局映射）；`RequirementClarifyService` doRound 三段状态机 + sendMessage 上限分派放行确认�?意图�?+ switchToClarify/switchToChat 防御性清标记；切换端点按规范 §8.2 整改 `POST /{id}/to-clarify`→`/toClarifyById/{id}`、`/{id}/to-chat`→`/toChatById/{id}`；前端移除「转为方案」按钮与 handleToClarify、clarify.ts 路径同步。单�?ChatModeAndSwitch 14�?9 例全绿（2 例改待确认语�?+ 1 例增�?+ 5 例新增确认流�? 修复既有 `PlannerAnalysisServiceTest.shouldDecomposeAndPersistDrafts` pre-existing Error（补 list(Wrapper) stub�? helloai-core 全量 389/389 全绿 + vue-tsc 0 �?+ `verify-planner-chat-dual-mode.sh` 改造后 8 �?PASS（STEP7 意图词→待确认→回复确认→CLARIFY 全流程断言）。见 §6.45�?*V40.1 同日追加**：意图词正则扩展口语化话术（整理方案/出个方案/写方�?做个方案/做方�?方案整理，覆�?帮我整理方案�?�? 澄清模板首轮强制 structured（进�?CLARIFY 后第一轮追问必有推荐选项卡片；用户实�?没看到推荐选项"根因即意图词未命中导致仍�?CHAT 纯文本模式）。单�?+1（colloquialIntentPhraseEntersPendingConfirm）全�?390/390 + E2E STEP7 意图词改口语化重�?8 �?PASS。见 §6.45�?*口嗨切换治理**：`requirement-chat.md` �?4 条改�?系统自动处理切换、LLM 不得提及/预告/扮演方案整理模式"，消�?CHAT 模式 LLM 凭空宣称"切换到方案整理模�?的误导（实测数据库该会话全程 mode=CHAT，推荐卡片缺失根因即会话从未真正进入 CLARIFY）。见 §6.46�?*2026-08-04 增量（V40.2 /planner 命令 + CHAT 追问卡片�?*：意图词正则对口语话术仍敏感（「帮我整理成技术方案文档吧」不命中），新增斜杠命令 `/planner`（大小写不敏感，可带多行附加文本）为显式转方案入口——命令前缀不落消息、附加文本先落库 user 消息�?LLM 上下文再�?CLARIFY（`switchToClarify(Long,String)` 重载 + Controller `toClarifyById` body �?`@RequestBody(required=false)` 兼容�?body）；CHAT 轮容错双模（`tryParseChatStructured` 宽松解析：仅 structured question �?payload 出推荐卡片，其余一律纯文本降级零破坏），`requirement-chat.md` 新增「输出形态」节引导 LLM 追问时出 structured JSON（每�?�? 题、每�?2~4 选项、recommended 每题至多 1 个）；前�?`PLANNER_COMMAND_RE` 命令识别 + `handlePlannerCommand`（新会话 initialMode=CLARIFY 直达 / 已有会话 toClarify 带附加文本）+ `activeStructured` 放开 CHAT 模式交互卡。单�?ChatModeAndSwitch 19�?3 例全�?+ helloai-core 全量 394/394 + vue-tsc 0 �?+ E2E 改造后全流�?PASS（STEP4.1 CHAT 轮宽松断言 / STEP5 toClarifyById 带附加文本断言 +2 消息）。见 §6.46�?*2026-08-19 增量（V41 对话完善四项，�?.105�?*：① 意图确认从纯文本询问改为弹窗式结构化选项卡（仅「确�?取消」两选项、不给推荐项；关键实施发现：卡片提交文本「问题文本：确认」不命中 `CONFIRM_PHRASE_PATTERN` 开头锚定正�?�?新增 selections 快照判定通道 `isConfirmCardAccept`/`confirmCardValueOf`，三通道确认 = 手打确认�?OR 再次意图�?OR 卡上点选「确认」；sendMessage 轮数上限放行条件同步纳入卡选；`buildConfirmAskPayload` �?ObjectMapper NON_NULL 副本序列化避�?`recommended:null` �?payload）；�?意图词正则扩�?8 组组合正则（新建/创建/建立+计划/任务/方案、帮�?给我+方案、生�?�?�?�?计划任务方案、帮我总结、总结成方案等，覆盖「新建个计划吧」「帮我生成计划」「给一个方案」口语）；③ 联网搜索从仅首轮（V34 `rounds==0`）放宽为 CLARIFY 模式多轮都搜 + 新增 `WebSearchOutcome`（planner/search，搜索过程与结局快照，`toContextText()` 承接原注入文本；异常降级 failed outcome、查询词空白返回 null 不落键）+ assistant 消息 payload �?`webSearch` 键（provider/query/costMs/total/failed/reason/results 明细�? 前端新增 `WebSearchBar.vue` 折叠查验条（对齐 DeepSeek「已搜索 xx 个网页�? Kimi「已联网检�?· N 个信源」形态，展开可见实际搜索词与来源链接/站点/摘要，failed / total=0 态同样可视化，回应用户「是否真搜了/搜了什�?来源是否正确」三问）；④ 澄清选项无论单选多选每选项独占一行（`.sq-options` 纵向单列 + `.sq-option` width:100%）。`RequirementClarifyServiceTest` 53/53 全绿（新�?8 例）+ vue-tsc 0 错；�?Flyway，重启后端生效，真实环境回归（确认弹窗交�?/ 查验条展开回显 / 多轮搜索 / 选项单列）待实测�?*2026-08-19 增量（V45，�?.111�?*：联网搜索从�?CLARIFY 放宽�?CHAT/CLARIFY 任意模式每轮生效——doRound 触发条件改为 `isWebSearchEnabled(conversation)`（NULL/true 视为开启，成本由各自轮数上限封�?CHAT 50 / CLARIFY 20）；CHAT 轮消费检索结果：`{{WEB_SEARCH_CONTEXT}}` 注入 Prompt + assistant payload 携带 `webSearch` 查验键（结构化追问卡 `buildQuestionPayload` 合并 / 纯文本回�?`buildWebSearchOnlyPayload`，无搜索保持�?null 形态零兼容成本）；`requirement-chat.md` 注释头阶�?2 标注移除�? 文件注释/文案口径同步（含前端 tooltip）；`RequirementClarifyServiceTest` 63/63 全绿（原 CHAT 不搜索用例反�?`chatRoundTriggersWebSearch` + 新增开关关�?`chatRoundWebSearchDisabled`、用户原�?URL 场景回归 `chatRoundWithUrl_fetchesPageAndInjects`）；�?Flyway、无新配置，重启后端生效。见 §6.105 |
| N18 | 外部技能规范库与纪律制审查（DeepSeek Harness Skills 借鉴�?| P0+P1+P2 已落地（2026-08-20�?| 方案文档 `doc/design/HelloAI_DeepSeek_Harness_Skills借鉴方案.md`�?026-08-20 拍板�? 项决策：Reviewer 全面纪律�?/ 最终报�?trim 哲学 / 平台自命�?eng- 前缀 / P0 同步实施）�?*P0 已交付（§6.113�?*：`prompts/subtask-review.md` 升级双轨纪律制——轨�?A 对照验收标准 + 轨道 B 工程纪律清单（代�?C1 接口契约 / C2 生命周期与并�?/ C3 验证强度 / C4 范围与必要性，文档 D1 契约与命题完�?/ D2 无思维链泄�?/ D3 结构清晰），blocker/nit 分层驳回（纪律缺陷实际造成误用、泄漏、竞态、文档代码矛盾才 blocker），issues 四元组格�?`[defect] 缺陷 [location] 位置 [impact] 影响 [evidence] 依据`；JSON schema 五字段（pass/score/issues/comment/analysis）零变更、后端零改动；单测回�?SubTaskReviewServiceTest + MqReviewCommandConsumerTest 35/35 全绿；顺手清�?prompt 模板 cot-leakage（`requirement-chat.md` �?V39/V45/V40.2 版本戳、`requirement-clarify.md`「首轮」→「每轮」）�?*P1 已交付（§6.114�?*：① 平台自命�?`eng-` 前缀外部技能规范库 3 份落 `helloai-core/src/main/resources/skills/plugins/`——`eng-code-review.md`（来�?dsh-code-review，速览 C1-C4 + 四元组格式）/ `eng-doc-standard.md`（来�?dsh-prose-standard / dsh-doc-standards，速览 D1-D3�? `eng-verification.md`（来�?dsh-pre-push-checks，对�?VERIFICATION 围栏），文件约定「执行速览在前、首�?`---` 分隔详细规范」，注入只取速览�?token 成本；② Spec 注入：新�?`PluginSkillSpecService/Impl`（`renderSection(taskId)` �?`task.required_skills` �?`SkillNormalizer` 归一 �?�?KNOWN_SPECS 注册表求�?�?classpath 读速览 �?拼�?# 平台技能规范」段；任务不存在/未命�?文件缺失均返回空�?best-effort 绝不阻断执行链），`SubTaskExecutionServiceImpl.executeOnce` 装配�?`mergeSpecSections` 拼接（TaskRunningSpecService 接口与两 Impl 零改动——注入点实现在执行装配点而非 Spec 服务内部，与「调用方按需组装」既有模式一致），timeline payload �?`pluginSpec` 布尔观测；③ `planner-decompose.md` 验收标准升级为「每条必须可检查——含可观察验证点（判定动�?+ 预期结果）」，�?subtask-review 轨道 A 形成拆解�?审查侧闭环；�?`requirement-clarify.md` 五维自检加第六维「边界与排除项」（不覆盖场�?不支持平�?不含交付物必须显式写清）；⑤ executor SKILL.md 补「技能标签（skills）与平台技能规范（eng-*）」节（注册声�?`"skills": ["shell", "eng-code-review"]` 示例 + 命中即注入语�?+ 不达标驳回）；⑥ 单测 `PluginSkillSpecServiceImplTest` 新建 7 例（空�?未命�?命中渲染/多命中声明序/未知技能忽略）+ `SubTaskExecutionServiceTest` 适配新依�?24/24 回归 = 31/31 全绿；顺带修�?CRLF 行尾下速览分隔符匹配失败（详细规范被整体注入）缺陷�?*P2 已交付（§6.115�?*：① 值班 checkIn 技能上报——`McpToolService.checkIn` 5 参重载（reportedSkills�? `CheckInResult.mergedSkills` 回显；`McpToolServiceImpl.mergeReportedSkills` 与既�?agent.skills 取并集（SkillNormalizer 同义词归一 + 去重保序 + 只增不减 + 无新增不写库 + 失败 best-effort 不阻断打卡）；`McpMcpServer` checkIn 工具�?`skills` CSV 参数（parseCsvSkills 兼容逗号/顿号/空格�? 工具 description �?P2 上报说明；executor SKILL.md 三处同步（�?.1/§1.2 工具�?+ 🧭 租约机制块）。② task-final-report.md trim 哲学重写——废除铁�? 50% 字数红线改「信息密度优先」（契约性事�?100% 保留 + 叙事压缩 + 删除标准），铁律3 改「契约性事实完整保留（一字不改）」，执行摘要收紧 �?00 字，绝对禁止清单加「叙事膨胀」，自检清单字数项换为叙事压�?事实保留两项，删 `{{TOTAL_OUTPUT_LENGTH}}` 占位符；`TaskFinalReportServiceImpl` 同步�?totalOutputLength 计算�?replace、systemPrompt �?trim 哲学提示。单�?`McpToolServiceTest` 新增 4 �?26/26 全绿 + `TaskFinalReportServiceTest` 13/13 回归全绿。选人链路已存在（V47/A2/A3：agent.skills + task.required_skills AND 匹配 + SkillNormalizer 同义词归一 + AgentSelectionConstraints 全链注入），本轮只补能力上报与报告哲学，不重建选人链路 | 保持现状（P0/P1/P2 全部收口�?|
| N19 | Agent 模态能力声明与核验媒体可见�?| ①② 已落地（2026-08-20�?/ �?未落�?| **背景**：executor 提交截图类证据时自动核验 reviewer 看不到图片——核验链路纯文本，且 `readAttachmentContent` �?`new String(bytes, UTF_8)` 读附件，图片二进制乱码会被注入核�?Prompt；capabilities 无模态维度�?*�?模态能力建模（§6.118�?*：`AgentAccessType.defaultCapabilities()` 三类统一新增 `supportsImageUnderstanding/supportsAudioUnderstanding/supportsVideoUnderstanding`（默�?false，模态取决于底层模型而非接入通道�? `AgentCapability` 常量，注册时经既�?capabilities 覆盖通道声明�?*�?核验注入硬化（�?.118�?*：`SubTaskReviewServiceImpl` 仅文本类附件注入正文（mimeType 优先 + 扩展名兜�?+ fail-close，杜绝二进制乱码�?Prompt）；含图�?音视频附件时注入「本提交�?N 个媒体附件…无法查看原内容，从严核验、评分保守」显式标注（独立�?attachment-content-enabled 开关）；A0-5 证据硬检查不变（图片仍计有效证据）。单�?34/34 全绿�?*�?多模态核验调用链未落�?*：Spring AI Media 图片注入 + 含图附件优先多模�?reviewer 挑选，涉及 ChatClient 调用链改造与 provider 兼容性（DeepSeek 通道不支持图片，�?OpenAI 兼容/Anthropic 兼容可用�?| �?另立项；当前 reviewer 未声明多模态能力前行为退化为「文本注�?+ 媒体从严标注�?|
| N20 | 反馈回路�?1 层：质量画像 + 调度回灌 + 动�?TTL 复合�?+ executorDoneIssues LLM 回填；契约先行拆解（Phase 2�?| 已交付（2026-08-21 / 08-22 / 08-23�?| 评审建议「质量画像回�?+ 反馈回路」第 1 层全链落地（Phase 1，�?.124）：**�?画像落库（V54�?*：`agent_quality_profile` 表（agent_id 部分唯一索引 WHERE deleted=0 + 审计列全套；reviewed_count/approved_count/first_reviewed_count/first_pass_count/total_score/rework_round_sum/issue_defect_stats JSONB + reviewer_reviewed_count/reviewer_disagreement_count �?Phase 4 预留 + last_review_record_id 防重锚点）；V55 `task_running_spec.contract`、V56 `sub_task.is_contract` 为契约先行（Phase 2）预留�?*�?画像服务（agent/quality 包）**：`AgentQualityProfile` 实体 + `AgentQualityProfileMapper`（incrementCore 单条 UPDATE 原子增量 + mergeDefectStats JSONB LATERAL 原子合并，规避读改写竞态）+ `AgentQualityProfileService/Impl`（getProfile / computeQualityScore（首轮通过�?0.5 + 平均分归一 0.5，null 安全�?null�? renderHistorySection（Phase 3 预留�? rebuild 重算兜底�? `QualityProfileUpdater`（挂�?ReviewServiceImpl.recordAutoReview/createReview 两处同事务增量；执行者维度取 sub_task.assigned_agent_id 落库时刻归属；防�?last_review_record_id 递增判定；更新失�?best-effort 不阻�?review 主链路）+ `DefectLabelParser`（issues 四元�?[defect] 标签提取，归一化截�?30 字符，增�?重算同口径）�?*�?调度回灌**：`AgentSelector.resolveComparator` �?dutyRank 之后插入 qualityRank（qualityScore/20 档位 × quality-weight，默�?0.1 可配�?0=关闭，null 安全不参与排序）；`AgentDutyLeaseServiceImpl.resolveTtlMinutes` performanceScore 升级为复合分（失败折算分 + qualityScore×qualityWeight，clamp [0,100]，质量分缺失回退原逻辑开关可回退）�?*�?executorDoneIssues LLM 语义对比回填**：`ExecutorIssueResolutionAssessor`（agent/quality�? classpath 模板 `prompts/executor-done-issues.md`（严�?JSON `{"doneIssues":[],"reason":""}` + strip fence 容错解析，失败降级跳过）；LLM 通道走平台级凭证（`PlatformProviderConfigService`�? 无凭证跳过，超时独立配置默认 30s；挂�?`ExecutionResultHandler` 成功回写路径——检�?context.reviewHistory 最后一�?executorDoneIssues 为空且有 issues �?异步语义对比 �?回填该轮（写入前重读 context 防覆盖）+ timeline `sub_task_executor_done_issues` 成功/跳过/失败三�?payload�?*�?验证**：单�?helloai-core 868/868 全绿 + helloai-job 66/66 无回归（AgentQualityProfileServiceTest 14 / QualityProfileUpdaterTest 13 / ExecutorIssueResolutionAssessorTest 21 + AgentSelectorTest/AgentDutyLeaseAdaptiveTtlTest 增补）；`AdminQualityController` 两个薄透传端点（POST /api/admin/quality/rebuild/{agentId} + /dispatch/{subTaskId}，Controller 零编排，�?ps1 实测）；`scripts/powershell/verify-quality-profile.ps1` S1~S5（S1 画像增量初始�?/ S2 首轮通过�?返工轮数+缺陷标签口径 / S3 qualityRank 回灌选人 / S4 动�?TTL 复合�?/ S5 rebuild 对账；UTF-8 with BOM + Parser.ParseFile 0 error）。真实环境实测待用户执行�?*Phase 2 契约先行拆解（�?.125�?026-08-22�?*：① 拆解协议与落库——`planner-decompose.md` 加契约规则（多模块接�?多组件协作任务强制生�?1 个「契约定义」子任务，deliverable=接口签名/数据模型/错误码表、验收标准可检查，�?DAG 最上游，JSON �?`"contract": true`�? `PlanDraftItem.contract` 解析 + `SubTask.isContract`（V56 SMALLINT，SmallIntBooleanTypeHandler 同款映射）落库；�?spec 契约字段双实现——`TaskRunningSpec` 不可变模型加 `contract` Map（builder/toMap/fromMap/toBuilder 往返，isEmpty 计入 contract�? `TaskRunningSpecService.updateContract(taskId, Map)` 接口�?Jsonb/Table 双实现（Jsonb �?taskId 粒度分段�?+ 重建 builder 保留 contract；Table �?`specMapper.updateContract` PgJsonbTypeHandler�? 渲染节�?# 任务契约」（Baseline 之后，contract 非空时全局注入所有下游执�?Prompt，双实现同步）；�?契约产出回流——`SubTaskCompletionListener.onSubTaskCompleted` 首步 `backfillContract`（isContract=1 检�?+ 产出提取物化附件优先/`context.lastExecution.output` 回退同源口径 + `updateContract` �?`{subTaskId,title,content,backfilledAt}` + success/skipped/failed 三�?timeline `sub_task_contract_backfilled`，失�?best-effort 不阻断解锁下�?Task 收尾）；�?验证——单�?helloai-core 883/883 全绿 + helloai-job 66/66 无回归（PlannerDecomposeAsyncServiceImplTest 14 / TaskRunningSpecJsonbServiceTest 8 / TaskRunningSpecTableServiceTest 4 / SubTaskCompletionListenerContractTest 5）；`AdminQualityController` �?GET /api/admin/quality/spec-section/{taskId} 薄透传端点（ps1 S3 断言契约节渲染）；`scripts/powershell/verify-contract-first.ps1` S1~S4（S1 拆解含契约子任务�?is_contract=1 / S2 契约子任�?DONE �?task_running_spec.contract 非空 + timeline success / S3 下游执行 Prompt 含�?# 任务契约」节 / S4 非契约任务零噪音；UTF-8 with BOM + Parser.ParseFile 0 error）。真实环境实测待用户执行 | 保持现状；Phase 3 历史表现摘要注入（�?.130�?026-08-21）已交付：`AgentQualityProfileServiceImpl.renderHistorySection` 接通执行装配点——`SubTaskExecutionServiceImpl.executeOnce` �?pluginSpec 段后�?`mergeSpecSections` 注入 historySection，私有方�?`renderHistorySectionSafely` try-catch best-effort 降级空串绝不阻断执行主链路（N18 P1 + §6.128 同款哲学）；timeline `sub_task_spec_context_loaded` payload �?`historySummary` 布尔观测；单�?`SubTaskExecutionServiceTest` 新增 @Nested ExecuteOnceHistorySection 3 用例（注�?空省�?异常降级�?83�?86 全绿 + helloai-job 66/66 无回归；`verify-quality-profile.ps1` 新增 S6（预�?REWORK 子任�?�?POST /api/sub-tasks/executeById 触发异步执行 �?conversation_message 全量 prompt 断言「你的历史表�?累计评审 N �?本轮提醒�? task_timeline historySummary=true；无执行记录 SKIP / 有记录无 prompt FAIL 二态区分；UTF-8 with BOM + Parser.ParseFile 0 error）。真实环境装配自检待用户重启后�?S1~S6�?*真实环境修复（�?.132�?*：自检发现 rebuild 500——MyBatisPlusConfig 全局注册 Map→JacksonTypeHandler 劫持 `selectRebuildSource` �?List&lt;Map&gt; 返回（首�?record_id 裸数字被�?JSON 反序列化�?MismatchedInputException）；已改 `RebuildSourceRow` 具体 DTO 返回 + 注册块防复发注释 + 单测 14/14 全绿，重启后重试 rebuild 即可；**Phase 4 Reviewer 双审 + 抽检（§6.142，2026-08-22）**：① V57 `review_recheck_log` 表 + `ReviewRecheckLog` 实体/Mapper（task 域）；② `ReviewProperties`（helloai.review.*：双审开关/共识策略/抽检开关/间隔/比例/上限/窗口，yml 未显式声明走默认值）；③ `ReviewerPicker` 接口 + Impl（review/picker/，候选选取三段逻辑搬入，CODE_STYLE §3.x 补 picker 子包）；④ `SubTaskReviewServiceImpl` 抽取 doReviewWith + 新增 doDualReview（双审只落一条 review_record 归 reviewer1 防执行者画像重复计数；分歧 markManualIntervention + sub_task_reviewer_disagreement + recordReviewerStats(1,1)；共识 applyVerdict + sub_task_dual_review_consented；ANY 任一通过即过）+ recheckReviewRecord（前置校验 → recordRecheck → 画像计数 → sub_task_recheck_consistent/discrepancy）；⑤ `AgentQualityProfileService.incrementReviewerStats` + Mapper 增量 SQL（UPDATE 无行 INSERT 兜底 + 唯一索引冲突回退，best-effort）；⑥ `ReviewService` countRecheckCandidates / listRecheckCandidateIds / recordRecheck（APPROVED + 窗口 + NOT EXISTS 排除已抽检）；⑦ `ReviewerRecheckTask`（helloai-job，@Scheduled + Redis 锁 Lua 安全解锁；抽样批量 ceil(候选×比例) 下限 1 上限 maxBatch；单条失败不中断）；⑧ 验证——helloai-job 73/73 全绿（ReviewerRecheckTaskTest 7）+ core 聚焦 40/19/8 全绿；期间排查修复 job 模块 @Nested 测试类 0 run 历史问题（target/test-classes 过期编译产物，重编译恢复，与 Phase 4 代码无关）；`scripts/powershell/verify-reviewer-dual.ps1` S1~S3（双审一致 / 分歧转人工 / 抽检计数；UTF-8 with BOM + Parser.ParseFile 0 error）。真实环境实测待用户执行；§6.143（2026-08-22）修复实测暴露的产品缺陷——ProviderChatModelCache.buildKey 补 model 维度（5 参主签名 + 3 个 factory 传 model + 测试同步），Agent 改 model_type 即时生效无需重启，core 聚焦 42/42 全绿；**verify-quality-profile.ps1 真实环境全绿（§6.144，2026-08-22）**：S1~S6 = PASS 42 / FAIL 0 / SKIP 0——Phase 1（画像增量/指标语义/qualityRank 回灌/动态 TTL 复合分/rebuild 对账）与 Phase 3（历史表现摘要注入 executor prompt）真实环境验收通过；产品代码零改动，三轮修复均在脚本侧（① Invoke-Tool 参数名 `$Args` 与 PS 自动变量 `$args` 冲突导致 ConvertToFinalInvalidCastException → 改名 `$ToolArgs`，顺带函数内 12 处 Write-Output 改 Write-Host 防返回流污染；② S4 lease 查询列名 `started_at/expires_at` → `start_time/expire_time`（agent_duty_lease 表 `_time` 后缀惯例）；③ S6 prompt 多行被 Get-PsqlFields 首行截断 → SQL regexp_replace(content,'[[:space:]]+',' ','g') 单行化）；实测 TTL exec-a=134min（含画像复合分）vs exec-b=122min（无画像）、差 12min 与期望一致；前置修复：preset agent modelType 参数化（gpt-4o → 目录内模型 dashscope:qwen3.6-Flash/qwen3.7-plus、moonshot:kimi-k3）+ Ensure-TestAgent SQL fallback**Phase 5 质量度量看板（§6.147，2026-08-23）**：review 域统计——ReviewRecordMapper 4 投影 SQL（selectTrendSource / selectIssuesForStats / selectReworkDistribution / selectReviewerLeniency，窗口 create_time >= now()-days*interval '1 day' + deleted=0，投影返回具体 DTO record 防 §6.132 Map 劫持复发）+ review/dto 4 record（QualityTrendPoint / DefectDistribution / ReworkRoundPoint / ReviewerLeniency）+ ReviewService 4 方法（缺陷标签聚合复用 DefectLabelParser 同口径，补名走 AgentService.listByIds 缺失回退 ID）；agent 域统计——AgentQualityProfileMapper 2 投影 SQL（selectOverviewRow COALESCE 兜底单行必返 / selectRankingRows 动态 LIMIT）+ agent/quality/dto 2 record（QualityOverview / AgentQualityRank）+ AgentQualityProfileService 2 方法（statsOverview / statsAgentRankings，qualityScore 逐行调 computeQualityScore 口径唯一防 SQL 漂移）；聚合——QualityDashboardService（review 域新建无环）+ QualityDashboardResponse（review/dto）+ AdminQualityController 3 薄透传端点（GET /overview /agents?limit= /dashboard?days=，admin.quality.enabled 门控复用）；前端——paths.ts + api/quality.ts + types/quality.ts + QualityDashboard.vue（5 图，echarts init 模式对齐 Dashboard.vue）+ 路由/菜单；验证——helloai-core 922/922 全绿 + helloai-job 66/66 无回归（ReviewServiceTest +4 / AgentQualityProfileServiceTest +2）；verify-quality-dashboard.ps1 S1~S6 真实环境 6/6 全绿（门控 403 / 排行补名 / days<=0 兜底 30；修复 getByKey 响应 Map{key:value} 的 S1 解析）；P2 修复：ReviewProperties.dualReviewTimeoutSeconds 默认 120->90s（deadline 加锁后才起算，双审窗口收进核验锁 TTL 120s 内防锁过期竞态重复双审，ReviewDualExecutorConfig / SubTaskReviewServiceImpl 注释同步）；； | 保持现状；Phase 4 Reviewer 双审 + 抽检（§6.142，2026-08-22）已交付；Phase 5 质量度量看板（§6.147，2026-08-23）已交付 |
| N21 | admin 授权拦截（`/api/admin/**` 角色校验�?| 已交付（2026-08-21�?| 来源：后端代码评审报�?P0「admin 授权缺口」——`AuthInterceptor` 只做认证（写 `_authType`），全仓库对 `_authType == "admin"` 的检查仅 3 处（`AuthController`/`CredentialController`/`SubTaskController`），�?`/api/admin/**` 前缀�?8 �?Controller（AdminAgent / AdminLlmProvider / AdminProviderConfig / AdminConfig / AdminPrompt / AdminDashboard / AdminQuality / AgentDutyLease，后两者为评审报告未列出的暴露面）均无角色校验，持�?Agent API Key 的外�?AI 可横向越权调用（含改写平台级 API Key）。本轮交付：�?新增 `AdminOnlyInterceptor`（helloai-api/interceptor，校�?`_authType == "admin"` 否则 `BizException(403)`）并�?`WebMvcConfig` 注册�?`/api/admin/**`（位�?AuthInterceptor 之后�? �?admin Controller 一次性全覆盖，后续新�?admin 端点自动被覆盖）；② `/api/agents/register` 公开注册口补 `allowRegistration` 开关门控（�?registerWithToken 同文案）；③ 单角色现状标注：`sys_user.role` 仅展示不生效（存量均 ADMIN），授权边界�?`_authType == "admin"`，多角色演进时再引入 role 校验；④ 既有 3 处手�?`requireAdmin()`（路径不�?`/api/admin/**` 下）作为纵深防御保留；⑤ CODE_STYLE V1.8 新增 §6.8 授权拦截红线；⑥ 防回归脚�?`scripts/powershell/verify-admin-authz.ps1`（agent Bearer �?8 前缀端点 + 1 写端点断言 403 / admin 同批端点断言 200 / 无凭证断言 401�? `AdminOnlyInterceptorTest` 3 例单�?| 保持现状；涉及授权面的改动后必跑 verify-admin-authz.ps1；评审报告其余条目（依赖方向/巨型�?事务缺失/规范收口）另立项 |
| N27 | 分布式健壮性改造：锁选型收口（Redisson / ShedLock）+ MQ 容量治理 + 死信台账 | 已交付（2026-08-26） | 来源：`doc/design/分布式健壮性改造实施清单.md` v1.2（阶段 0-5）——12 处手写 `setIfAbsent` Redis 锁与 11 个无锁 @Scheduled 任务的多实例并发隐患；RabbitMQ 队列无容量阈值 / 无快速失败 / 死信无告警出口。本轮交付：**① 锁选型（CODE_STYLE V1.16 新增 §3.y 铁律 + §12 整章重写）**——review 域 12 处手写锁清零迁 Redisson RLock（`review:lock:{subTaskId}` 动态 key，`tryLock(0,120,SECONDS)` 显式 leaseTime；`redisson-spring-boot-starter` 4.0.0 + `redisson-spring-data-34` 适配 Boot 3.4，helloai-core 直接声明依赖）；11 个 job 定时任务迁 ShedLock `@SchedulerLock`（name=任务名 / lockAtMostFor=原 TTL 口径，`shedlock-spring` + `shedlock-provider-redis-spring` 6.6.0，helloai-job 直接声明依赖；`ShedLockConfig` 新建于 start.config，`helloai:schedlock:` key 前缀；`SchedulingConfig` 既有 ThreadPoolTaskScheduler 被 `@EnableSchedulerLock` 代理包装，调度配置零改动）；豁免清单 5 个多实例并行无害类（scanReviewOrphans / ExecutionCommandPoller / DoorbellKeepaliveTask / SessionAuthCleaner / McpSessionAuthCleanupTask）写入 §3.y；版本由父 pom properties 集中管理（沿用 `mybatis-plus.version` 模式）；**② MQ 容量治理（§3.z）**——核心 4 队列 `x-max-length=50000` + notification 队列 `1000`，`x-overflow=reject-publish`（前置 RabbitMQ ≥ 3.10，docker-compose 3.12.14 已满足，实机探测列为部署门禁；不满足则降级仅声明 x-max-length），监听端 prefetch 默认 250 收紧至 10（3 个 MANUAL ACK 消费者全局生效），ConfirmCallback NACK 日志补「可能是队列满」提示；**③ 死信台账（阶段 4）**——Flyway V60 `mq_dead_letter_archive` 表 + `DlxAlertConsumer`（先落台账再 ACK；dlxQueue 未挂 DLX，任何情况 basicNack(requeue=false) 禁止 requeue）。验证：helloai-job 63/63 + helloai-core 932/932 + `DlxAlertConsumerTest` 3/3 + 全模块 compile 零错误。迭代详情 `doc/log/HelloAI_迭代执行记录.md` §6.163 | 保持现状；实机部署门禁：RabbitMQ ≥ 3.10 探测、队列参数 immutable 需 Management UI 删旧重建、notification 队列积压核查、启动后观察 `helloai:schedlock:` key；后续新增 @Scheduled 先对照 §3.y 豁免清单 |
| N28 | 内部 LLM Agent 执行凭证平台级化（API Key 轮换实时生效） | 已交付（2026-08-28） | 执行/探活/选人三链统一走 `AgentLlmCredentialResolver`：平台级（模型配置 / PLATFORM 级 vault）优先、Agent 级兜底；注册链不再向 credential_vault 复制平台密钥快照 | 缺陷修复：系统管理轮换 API Key 后旧注册 Agent 仍用旧 Key 执行报错——根因是注册时 `bindPlatformApiKeyIfAbsent` 落 AGENT 级密钥快照而轮换只动平台级；修复后执行密钥按 modelType 关联平台级凭证实时解析，存量快照自动失效不清理，兼容管理端手工绑定自定义密钥链路。迭代详情 §6.164 |
---

## 5. 当前建议优先�?

1. ~~先继续关闭文档失真项：D1、D2、D5、D6~~ �?全部已关闭（2026-07-13�?
2. ~~D7、D8 已关闭（2026-07-13 / 2026-07-16）~~
3. 再继续夯实执行链：N1 ②a + ②b 已落地（最小闭�?+ Confirm/Retry）→ T4 RabbitMQ 失败可恢�?E2E �?�?T5 Poller 降级 + Validator 启动�?fail-fast 闭环 ✅；N6 标已交付（含 **`scripts/powershell/verify-poller-e2e.ps1` 最�?PASS 15 / FAIL 0** + **`scripts/powershell/verify-execution-dispatch-guard.ps1` PASS 12 / FAIL 0**�?
4. AgentHub V1 P0 已交付（2026-07-16）：N12 值班租约闭环 + 值班优先调度；E2E 脚本 `verify-agenthub-duty-e2e.ps1` 可重复回�?
5. **N12 P1 剩余三项（STRICT 独占报锁 / concurrency 预扣 / 动�?TTL�?*：已拍板 A2（从轻到重三项顺序分 3 �?atomic round），**A2 �?1 �?STRICT 独占报锁 已交付（2026-07-17�?*——`WorkMode` 枚举（`AUTO/STRICT` + `lenientParse`/`strictParse` 双策略）+ `AgentSelector.pickFromCandidates` �?`.filter(a -> !isOnStrictDuty(a.getId()))` 过滤（STRICT Agent 退出别人失败后�?pickAlternative 替补池）+ `McpToolService.checkIn` �?`strictParse` 严验入参（非法�?BizException 拒绝，不静默降级�? `AgentSelectorTest` 新增 5 �?STRICT 用例�?9/19 全绿�? `verify-agenthub-duty-e2e.ps1` 追加 S6.1/S6.2/S6.3 子场景（**真实环境实测 ALL PASSED**，S6.1 STRICT 落库 / S6.2 strict 小写→DB 仍是 STRICT / S6.3 BOGUS_VALUE 被拒绝且 lease count 未增）�?*A2 �?2 段（动�?TTL 自适应）已交付�?026-08-13，迭代记�?§6.83�?*——`AgentDutyLeaseProperties`（`helloai.agent.duty-lease` 配置段：adaptive-ttl-enabled 开�?+ min/max/default/full-score�? `AgentDutyLeaseService.resolveTtlMinutes`（显�?TTL 优先，否�?score 线性映�?[0,100]→[5,240]min，无 score 用连续失败数折算，开关关回退 30min�? `adaptiveRenew`（在跑子任务 �?240min 长窗口，空闲 �?动态窗口）+ `McpToolService.checkIn`/`refreshDutyLease` 联动；单�?12/12 + 全量 341/341 全绿，`verify-agenthub-duty-e2e.ps1` 追加 S7 真实环境 ALL PASSED（score=0 �?~5min / score=100 �?~240min，S1/S2/S3/S6 同步回归）�?*A2 �?3 段（concurrency 预扣）已交付�?026-08-13，迭代记�?§6.86�?*——`ConcurrencyQuotaService` 接口 + `InFlightDbQuotaService`（DB 实时统计：租�?maxConcurrent > capabilities.maxConcurrentTasks > 不限制）+ `AgentSelector` 选人链满额跳�?+ `SubTaskService.assignNext` FOR UPDATE 行锁原子防线 + `enforceMaxConcurrent` 开关（默认开，关闭即 E2 前行为）
6. **N13 AgentHub V3 门铃通知通道**：PR-1 内核 + PR-2 响铃 + PR-3 值班鉴权收口 + PR-4 保活�?双心跳均已交付（2026-07-17，单�?33/33 全绿 + �?reactor install BUILD SUCCESS）；**运行时端到端 `verify-doorbell-e2e.ps1` 真实环境实测 ALL PASSED（S1 拒连 500 / S2 200+connected / S3 到期�?EXPIRED + 事件驱动断连�?*；~~仅余多实�?Redis Pub/Sub fanout（�?2 演进项）~~ �?**2026-08-07 整体搁置**（外�?Agent 无法处理平台推送，任务感知改走 pullTasks 轮询，见 N13 状态列与迭代记�?§6.50�?
7. **外部 Agent 一键接入全套化�?026-07-17，路 A 修产品）**：`checkIn`/`checkOut` 纳入 `DEFAULT_EXECUTOR_TOOLS`�?�?0，靠 `isToolEnabled` 懒启用覆盖存�?Agent、无需 Flyway）；executor SKILL.md 由纯 REST 升级为含 MCP 全套 10 工具/打卡/门铃的完整说明书；新增第 2 步真实路径验证脚�?`verify-onboarding-doorbell.ps1`（注册→MCP checkIn→门�?connected�?*真实环境实测 ALL PASSED PASS=7 FAIL=0**）；新增�?3 步验证脚�?`verify-onboarding-pull.ps1`（门�?inbox 信号→MCP pullTasks 取任务，**真实环境实测 ALL PASSED PASS=12 FAIL=0**，门�?refId=pullTasks subTaskId=创建 ID 三处一致）；新增第 4 步验证脚�?`verify-onboarding-heartbeat.ps1`（双心跳：server→client 保活�?`event:keepalive` + 连接不中�?/ client→server `heartbeat` �?`last_seen_at`+在线态，**真实环境实测 ALL PASSED PASS=12 FAIL=0**）；新增�?5 步验证脚�?`verify-onboarding-submit.ps1`（注册→checkIn→ASSIGNED 子任务→`submitResult(success)`→子任务流转 `REVIEW` 完整闭环），并在验证中定�?修复 `submitResult` 成功路径**事务自死�?*（`ExecutionResultHandler.handleReport` �?`heartbeatService.active()` �?agent �?+ `failureTracker.recordSuccess()` `REQUIRES_NEW` 改同一�?�?自死锁约 40s 超时；修复：failureTracker 计数挪到主事�?`afterCommit` 执行，仅 `CLI_CLIENT`+成功路径受影响，`ExecutionResultHandlerTest` 4/4），**真实环境实测 ALL PASSED PASS=12 FAIL=0**（submitResult 毫秒�?`status:applied`、子任务 `ASSIGNED`→`REVIEW`）；至此外部第三�?AI 接入完整闭环五步全通；`mvn -pl helloai-core -am compile` BUILD SUCCESS
7b. **当前下一里程碑：真实 AI 子任务闭环联调（M5�?*——qoder/trae 打卡链路已通（2026-07-18），
    按《项目进度》M5 场景矩阵执行。~~场景 1~3 全绿前，planner 自动拆解（编排层）不启动~~
    �?**2026-07-28 按用户决策提前启�?planner 编排层（V26 已交付，�?N16�?*：拆解链与执行链通过
    草案�?`PENDING_PLAN_REVIEW` 硬隔离（确认前不进分发链），planner 缺陷与执行环缺陷可独立定位，
    原串行门禁的依赖前提已解除；M5 场景矩阵继续推进�?
8. N9 / N10 / N11 维持现状
9. 最后推进产品化编排能力：N2、N8
10. §5.2 后置（WorkUnit / STOP/PAUSE/REPLAN / 用户输入可重入）—�?等可靠投递与兜底职责收紧之后再开

---

## 6. 本轮文档治理结论

- `v3.0` 已不再作为路线图继续维护，而是降级为《架构设计参考�?
- `v2.4` 已归档为历史资产，不再作为事实源
- 后续若出现“设计理念、目标态、实现状态、执行记录”混写，优先拆回文档矩阵，而不是继续往单个文件里堆内容
- **2026-07-20 架构治理增量**：CODE_STYLE §6.3 待收口清单（6 �?Controller）与 3.x start 配置类待收口段落同步收口，本轮新�?N15（Controller 分层红线收口 + 两项包归位）记录在案
- **2026-07-20 调度链缺陷修复（v2.6 §4.1�?*：`helloai-core/pom.xml` 新增 `spring-boot-starter-aop` + 新建 `helloai-common/.../config/AgentHealthProperties`（默�?`offlineMinutes=5`）统一心跳阈值；`AgentSelector`/`ExternalAgentFailureTracker`/`AgentHealthCheckTask` 全部接入共享阈值（消除 Java/SQL 漂移）；`AgentHealthCheckTask.reassignStaleTasks` 首�?`redispatchOfflineSubTask` 失败后二次走 `dispatchPendingSubTaskAuto`（原 Agent role 缺失回退 EXECUTOR）；`ExternalAgentFallbackTask.scan()` 拆分为阶�?A（N11 阈值回退�? 阶段 B（`recoverPendingUnassigned` 全局 PENDING 兜底）；新增 `SubTaskMapper.selectPendingUnassignedWithoutActiveExecutionRecord`（PENDING + assigned_agent_id IS NULL + 有历�?record + 无活�?record）；新增 `ResilientDispatcherAopIntegrationTest`（Spring AOP 织入真实验证 fallback�? `AgentHealthCheckTaskTest`�?1 个反射调 `reassignStaleTasks`）覆盖二次选人；顺手修�?5 �?Doorbell 测试 package/目录不一致；`mvn test` reactor 7 模块�?SUCCESS，helloai-core 216 + helloai-job 38 + 聚焦测试 96 个全绿；jar `helloai-start-1.0.0-SNAPSHOT.jar` 62MB 可构建（沙箱�?PG/Redis，真实断心跳链路验收需外部环境）。迭代详�?`doc/log/HelloAI_迭代执行记录.md` §2026-07-20 调度链缺陷修复（v2.6 §4.1�?
- **2026-07-20 重分配熔断（V24�?*：`V24__sub_task_reassign_attempt_count.sql` 新增 `sub_task.reassign_attempt_count` 列；`AgentDispatchProperties.maxReassignAttempts`（默�?5）控制熔断阈值；`SubTaskDispatchService.checkReassignCircuitBreaker()` 在所�?4 个重分配入口（`redispatchOfflineSubTask`/`redispatchAssignedTimeout`/`redispatchForFallback`/`dispatchBlockedSubTask`）前统一检查；达到阈值后标记子任�?CANCELLED + 记录 `sub_task_cancelled` timeline 事件，打�?Agent 全掉线→无限重分�?死循环。迭代详�?`doc/log/HelloAI_迭代执行记录.md` §2026-07-20 重分配熔断（V24）�?*2026-07-28 V25 升级（旁路已封堵 + 死信兜底已交付）**：`dispatchPendingSubTaskAuto` 入口�?`checkReassignCircuitBreaker`（封�?AgentHealthCheckTask 二次选人 / ExternalAgentFallbackTask.recoverPendingUnassigned / SubTaskPendingOrphanTask 三个定时任务的无计数旁路）；达阈值改�?`DEAD_LETTER`（V25 Flyway 重建 CHECK 约束，非终态，替代�?CANCELLED�? timeline `sub_task_dead_letter`；新增人工兜底接�?`POST /api/sub-tasks/dead-letter/redispatch/{id}`（`redispatchDeadLetter` 清零计数 + 直接指派 ASSIGNED）；`ResilientDispatcher.assignNext` fast-fail 补心跳新鲜度检查（复用 `AgentSelector.isHeartbeatFresh`，消�?DB �?ONLINE �?Agent 已死"�?5-6 分钟误派窗口）；ASSIGNED 超时阈值配置化 `helloai.dispatch.assigned-timeout-minutes`（默�?10）；验证脚本 `scripts/powershell/verify-subtask-deadletter.ps1`。迭代详�?§6.10
- **2026-07-20 M4.5 派发控制�?*：落�?`doc/M4.5_派发控制台实施清�?md` 全量范围。`SubTaskController` �?`POST /api/sub-tasks/batch`（复�?`SubTaskService.createBatch` 逐项隔离）与 `GET /api/sub-tasks/{id}/timeline`（复�?`TaskTimelineService.listBySubTaskId`）；新建 `helloai-api/.../dto/subtask/TaskTimelineItem.java` DTO；前端新�?`src/components/QuickDispatchDialog.vue` + `src/api/module.ts` + 注入 SubTaskList "快速派�?按钮 + SubTaskDetail "执行时间�?卡片 + 详情�?5s 轮询（终态自动停 + onBeforeUnmount 清理）；`helloai-ui` `npm run build` 通过；Controller 红线合规（无 Mapper 注入）。迭代详�?`doc/log/HelloAI_迭代执行记录.md` §6.9 M4.5 派发控制�?
- **2026-07-28 任务-子任务关联打�?+ 子任务列表真分页**：断点在前端（`SubTaskList.vue` 不读 `route.query.taskId`）而非后端；前端打�?taskId 过滤 + 主任务信息条 + 真分页；后端 `SubTaskService.list(taskId,status,assignedAgentId,page,pageSize)` �?§6.3 �?Controller 收口条件构造，`GET /api/sub-tasks` 新增可�?`page`/`pageSize`（不�?page 保持纯数组，SKILL.md 外部 Agent 契约零破坏）；`SubTaskResponse` 新增 `taskTitle`（Controller `attachTaskTitles` 批量回填�?N+1）。`/available`、`/mine` 仍在 §6.3 待收口清单。helloai-core 228 测试全绿 + vue-tsc 0 错。迭代详�?§6.11
- **2026-07-28 任务管理 CRUD 收口 + 级联删除（竞态免疫）**：`TaskController` 新增 `POST /{id}/republish`（DONE 禁发，重�?PENDING + �?eventId 重通知全部 PLANNER，不动子任务）、`GET /{id}/related-counts`、`DELETE /{id}`（body �?confirmTitle 精确匹配，照 Agent confirmName 范式）；`TaskService.deleteTaskCascade` 事务内按序物理删 inbox→execution→review→timeline→sub_task→module→task（前三�?SQL 依赖 sub_task 子查询先删，规避 `@TableLogic` 软删�? �?Mapper �?`@Delete` 注解 physicalDeleteXxx）；竞态免疫依�?DB 唯一事实�?：删后所有消费端回查 not_found 丢弃，另�?`NotificationConsumer` �?`refTargetExists` 防御分支兜底在�?MQ 通知；前�?`TaskFormDialog`/`TaskDeleteDialog`（影响面统计+在途警�?标题确认�? TaskList 新建/编辑/重新发布/删除按钮。改派竞�?4 漏洞（旧 inbox 不作�?handleReport 不校�?agentId/�?PENDING 执行记录不取�?Poller 重放）本轮未修待下轮。mvn 56 测试全绿 + vue-tsc 0 错。迭代详�?§6.12
- **2026-07-28 值班租约列表�?Agent 维度展示 + 历史记录分页对话�?*：值班租约页从租约平铺改为 Agent 维度分组——每 Agent 一行展示最新租�?+ 租约总数，点"更多"弹窗分页查看�?Agent 全部历史，主列表也分页。后�?`AgentDutyLeaseMapper` 新增 `selectLatestPerAgent`（PG `DISTINCT ON (agent_id)` 取组内最�?+ JOIN COUNT 子查询带�?lease_count）与 `countDistinctAgents`；`AgentDutyLeaseService.listLatestPerAgent` 手工�?Page（自定义 SQL 不走分页插件）；新增 `GET /api/admin/duty-leases/by-agent` 端点 + `DutyAgentLatestResponse`/`AgentDutyLeaseLatestRow`（extends 冗余 leaseCount）。前端新�?`DutyLeaseHistoryDialog.vue`（复用既�?list 端点 agentId 过滤，零新增历史查询端点�? `DutyLeaseList.vue` 重写；顺手修�?`duty.ts` `list.agentId` 参数 `number �?LongId` 雪花 ID 精度隐患。既�?list/overview 端点零改动，Dashboard 不受影响；DB 无变更。helloai-core 228 测试全绿 + vue-tsc 0 �?+ DISTINCT ON SQL �?postgres_helloai MCP 真库验证。迭代详�?§6.13
- **2026-07-28 Planner 平台内自动拆解（V26�?*：落�?需�?�?LLM 自动拆解 �?用户确认/拒绝 �?进入既有分发�?闭环（详�?N16）。`SubTaskStatus.PENDING_PLAN_REVIEW` 草案�?+ `TaskStatus.PLANNING` + Flyway V26 重建 CHECK 约束；`PlannerAnalysisService`（core/planner，编排收�?core 对齐 §6.3）decompose/listDrafts/confirmPlan/rejectPlan 四方法，CAS 防并�?+ strip fence 容错解析 + 失败 CAS 回退；`TaskController` 四个薄入口；`skills/planner/SKILL.md` 升级�?executor 版同等水准；单测 `PlannerAnalysisServiceTest`(13，项目内首例 lambdaQuery/lambdaUpdate 链式 mock) + 状态机 V26 用例(3)，helloai-core 244 全绿 + 全模�?compile OK；`verify-planner-decompose.ps1` 语法自检通过、待真实环境实测。原 §5 7b "场景 1~3 全绿前不启动 planner 编排�?门禁按用户决策解除（草案态硬隔离�?planner 缺陷与执行环缺陷可独立定位）。迭代详�?§6.16
- **2026-07-29 任务级联删除 FK 违反修复 + 拆解链前端补全（N16 前端遗留收口�?*：阶�?0 修复 §6.12 级联删除遗漏——`V1__init_all.sql` �?FK 引用 `sub_task(id)` 的表�?6 张，`deleteTaskCascade` 只删�?2 张，子任务带附件/巡检/会话数据时删任务必炸 `attachment_sub_task_id_fkey`；`AttachmentMapper`/`PatrolRecordMapper`/`ConversationArchiveMapper`/`ConversationMessageMapper` 各补 `physicalDeleteByTaskId`（sub_task 子查询物理删，照 ReviewRecordMapper 范式）并接入 `TaskService.deleteTaskCascade` 删除序。拆解链前端（纯 helloai-ui，后端零改动）：`types/index.ts` �?`PENDING_PLAN_REVIEW`/`PLANNING` 枚举 + SubTask �?deliverable/acceptance/priority + 新增 `TASK_STATUS_MAP`；`api/task.ts` �?plan（单请求 timeout 120s�?planDrafts/confirmPlan/rejectPlan；新组件 `PlanReviewDialog.vue`（草案表格含依赖序号映射 + 确认分发/拒绝重拆双动�?+ 空草案兜底）；`TaskList.vue` 状态列�?TASK_STATUS_MAP + PENDING �?AI 拆解"按钮（loading + 成功直接开审阅�? PLANNING �?审阅草案"按钮。helloai-core mvn test 全绿 + vue-tsc 0 �?+ build 通过；浏览器闭环实测�?`verify-planner-decompose.ps1` 回归待真实环境（本轮后端未启动）。迭代详�?§6.18
- **2026-07-29 拆解链真实环境回归收口（同日第二轮）**：`verify-planner-decompose.ps1` 等价迁移�?macOS zsh �?`scripts/shell/verify-planner-decompose.sh`（curl+jq，照 verify-dashboard-duty-leases.sh 模板；补�?ps1 缺失的托管凭证绑定步�?STEP2.1——新注册 planner Agent �?deepseek 凭证时拆解必 500「Agent 未配置启用态托管凭证」，对齐 verify-inner-loop-e2e.ps1 做法绑定 `/api/credentials/agents/{id}/api-key`），真实环境 e2e 12 步全绿（confirm 路径 5 草案 PENDING_PLAN_REVIEW 转正 + Task IN_PROGRESS / reject 路径草案作废回退 PENDING / 重复拆解拒绝）；浏览�?UI 闭环实测通过（confirm 路径 6 草案确认分发→「进行中」、reject 路径 4 草案作废→回「待规划」可重拆、console 0 error）。N16 全部遗留收口；带附件子任务删除回归仍待造数验证。迭代详�?§6.18
- **2026-07-29 对话式需求澄清窗口（V29，同日第三轮�?*：新�?N17—�?第二步立�?入口，模糊想法在 `/requirement-chat` 独立页与 LLM 多轮对话澄清（追问每轮最�?3 问，轮数硬上�?20），LLM 产出终稿后一键「创建任务并自动拆解」（finalize �?Task PENDING + 通知 PLANNER + timeline，前端顺路调既有 plan 接口，跳任务列表自动开草案审阅弹窗）。Flyway V29 两张新表（task_id 软引用无 FK，deleteTaskCascade 零改动）+ `RequirementClarifyService`（复�?PlannerAnalysisService 五段式范式）+ 六端�?Controller + 前端会话列表/气泡�?终稿卡片。单�?13 例全�?+ zsh 脚本 `verify-requirement-clarify.sh` 真实环境 10 步全�?+ 浏览器闭环实�?8 步全过。迭代详�?§6.19
- **2026-07-30 子任务详情展示优化（方案1�? 拆解/澄清链配�?+ 执行产出物化方案设计**：前端（helloai-ui，纯前端）新�?`MarkdownView.vue`（markdown-it + dompurify�? `ReviewVerdictView.vue`（核验分析结构化卡片），`SubTaskDetail.vue` 执行对话�?Markdown 富文本渲�?+ 时间线事件人话化（`EVENT_META`�? Agent 注册名映�?+ 返回列表归属主任�?+ 产出复制/导出 .md（方�?�? 核验请求剥离 HTML 注释。后端配套：`PlannerAnalysisService.orderByDependency`（Kahn 拓扑正序�? `RequirementClarifyService.regenerate`（悬挂任务重建）�?*新增设计文档 `doc/design/HelloAI_执行产出物化与结构化多文件产出方�?md`（方�?/3，仅设计草案、后端代码未实现�?*：本地文件系统存�?+ `ArtifactStorage` 抽象（config 门控 `helloai.storage`）、方�? 是方�? 降级形态、物�?`afterCommit` best-effort、下�?`local://` 流式改造；待后续落地时回填"`attachment` 表从 0 写入 �?内置执行链产出物�?状态变化。工程：新增 `docker-compose.server.yml` / `nginx.server.conf` + `.gitignore` 忽略 `.tmp/`。迭代详�?§6.27
- **2026-07-30 澄清对话重试按钮 + Planner 手动选择下拉�?*：新�?`PlannerAgentPicker`（core/planner）收编澄�?拆解两处重复选型逻辑（等权重优先空闲；pinned 失效宽松回退自动）；`requirement_conversation` 新增 `planner_agent_id` 钉住字段（Flyway V31），手动选中�?Planner 从澄清跟到拆解（`pickForTask` �?task_id 软引用反查）；新端点 `GET /requirement-conversations/planner-options`（内�?PLANNER 可�?+ 在班外部 Agent 置灰）与 `POST /{id}/retry`（仅最后一条为 user 消息时，不增消息不加轮次）；前端 RequirementChat 新会�?Planner 下拉�?+ 失败重试条（canRetry 数据驱动，刷新后仍可重试�? create 失败�?title 找回已落库会话。helloai-core 312 测试全绿 + vue-tsc 本轮文件 0 错；真实环境验证待后端重启（V31 自动迁移）。迭代详�?§6.29
- **2026-07-31 方案2 执行产出物化 + 主任务交付物实时聚合 zip 下载**：�?.27 设计文档的方�? 与主任务打包下载两层同轮落地，「仅设计草案、后端代码未实现」状态翻转（设计文档头部状态行已同步更新，方案3 LLM manifest 仍为遗留）。后端物化链：`ArtifactStorageProperties`（`helloai.storage.{enabled,provider,local.base-path,max-artifact-bytes}`�? `ArtifactStorage`/`LocalArtifactStorage`（`local://{bucket}/{objectKey}`，normalize+startsWith 穿越防护�?`StoredArtifact` 存储三件�?+ `ExecutionOutputParser`/`ParsedOutput`/`ArtifactFile` 解析三件套（纯文本→�?.md，方�? 扩展位已预留�? `ExecutionArtifactService.materialize`（best-effort，register �?assignedAgentId 过归属校验，时间�?`sub_task_artifact_materialized`）挂�?`ExecutionResultHandler` 成功分支 afterCommit；`AttachmentService.loadContent` + `AttachmentController.download` 流式改造（`local://` 走后端流式输出，�?local 保持 302）。主任务 zip：`SubTaskDependencyOrder`（Kahn 拓扑�? `TaskDeliverableService.buildZip`（内存组�?UTF-8，`00-任务概览.md` 子任务完成情况表 + 拓扑�?DONE 产出，优先物化附件、无可读附件回退 `context.lastExecution.output`，排除草�?取消，重名后缀�? `GET /api/tasks/{id}/deliverables/download`（RFC 5987 中文文件名）。前端：TaskList「交付物」按�?+ SubTaskDetail「产出附件」卡片（轮询刷新/无附件隐藏）+ `utils/download.ts` blob 落盘。mvn 333 测试全绿�?21：parser 5/storage 6/artifact 5/deliverable 5�? vue-tsc TSC-OK（顺带修�?`tsconfig.node.json` �?skipLibCheck 阻断 `-b` 构建）。历史任务（attachment 0 条）�?context 回退可立即下载；重启后端生效（无 Flyway）；真实环境 E2E 回归待做。迭代详�?§6.30
- **2026-07-31 任务最终整合报告（V32，Planner 整合全部子任务产出）**：交付物 zip 各子任务产出分立的后续补齐——任务收口后�?Planner（pickForTask 钉住，澄清→拆解→整合同一人）把全�?DONE 子任务产出整合为一份连贯报告（执行摘要+重组正文+结论）。Flyway V32 task 专列三列 TEXT（final_report/final_report_agent_id/final_report_time，避开 JSONB 写入需 XML 覆盖的改造）；`TaskFinalReportService.generate`（仅 DONE，取数与 zip 同源拓扑序，单段 8000 字符截断，timeline �?llm_call_start/generated/failed）；自动触发：`SubTaskCompletionListener.tryCloseTask` CAS 赢家分支�?`TaskAutoCompletedEvent`（赢家唯一防重），服务 `@Async+@EventListener` 承接（发布点无事务），开�?`helloai.dispatch.auto-final-report-enabled`（默�?true），失败吞掉不阻断收口；手动：`GET/POST /api/tasks/{id}/final-report`（历史任务补生成/覆盖重生成）；zip 有报告时置顶 `01-最终整合报�?md` 子任务顺�?02- 起；前端 TaskList「报告」按钮（�?DONE�? `FinalReportDialog`（MarkdownView + 生成/重生�?+ 复制/导出 .md）。mvn 343 全绿�?10�? TSC-OK；重启后 V32 生效，真实环�?E2E 待回归。迭代详�?§6.31
- **2026-07-31 结构化选项式需求澄清（V33，同日第二轮�?*：N17 澄清链路交互升级——追问由纯文本升级为「选项点选为主、自由输入兜底」的双模协议，用户面对模糊需求不再需要打字长文回答。Flyway V33 `requirement_message.payload` TEXT 一列两用（assistant=结构化问�?JSON / user=选择快照 / 纯文�?NULL）；prompt 三形态输出（structured question / freeform question / final+progress:100�? 五维度自检清单；`RequirementClarifyService` 扩展 `sendMessage(id,message,selections)` 重载 + `parseReply` 降级 freeform 一等公民（判据 `rawOutput.contains("\"type\"")`�? `normalizeQuestionReply`/`isStructuredValid`/`fillStructuredDefaults`/`composeAssistantContent`（structured content 合成可读文本，payload 丢失不影�?LLM 上下文）+ `ClarifyQuestion`/`ClarifyOption`（weight 预留字段无业务消费）/`ClarifySelection` 三嵌套类；`RequirementMessageService.addMessage` 4 参重载（3 参委托保兼容）；前端 `StructuredQuestionCard.vue`（选项 chip + recommended 标记 + 可多�?+ 自定义补�?+ 卡片 `:key` 绑最后消�?ID 换轮重置�? RequirementChat 顶部进度条（FINALIZED 直接 100）。progress 仅展示无业务分支；旧消息 payload NULL 自然走纯文本气泡向后兼容。`RequirementClarifyServiceTest` 21/21 全绿（原「非 JSON 报错」用例语义更新为降级断言 + 新增 4 例）+ vue-tsc 0 �?+ `verify-requirement-clarify-structured.ps1` 真实环境实测 PASSED（脚本修复一处：中文 JSON 体须 UTF-8 字节发送，否则触发后端 Invalid UTF-8 middle byte）。本轮明确不做：weight 权重业务消费、多轮对话策略（P1）、progress 驱动业务分支。迭代详�?§6.32
- **2026-08-01 对话式需求澄清联网搜索开关（V34�?*：N17 增量——首�?LLM 调用前按用户问题预检索行业资�?竞品/技术方案注�?Prompt 增强拆解质量，仿腾讯 ima copilot �?DeepSeek/Kimi 网页版的"联网搜索"开关。会话级开关（NULL/true=默认开�?/ false=关闭），前端仅在新建会话前可设（已存 ACTIVE 会话锁定）。Flyway V34 `requirement_conversation.web_search_enabled BOOLEAN`（COMMENT 明示 NULL=默认开启）；`WebSearchProperties`（helloai-common，`helloai.web-search.{enabled=true, provider=bocha 默认, timeout-ms=3000, max-results=5, max-snippet-chars=200, query-keyword-limit=40, bocha-*, tavily-*}`�? `WebSearchService` 接口（`provider()` + `search(query,maxResults)`�? `WebSearchServiceRouter`（@Primary，`ObjectProvider<WebSearchService>` 解析候选）+ 供应商实�?`BochaWebSearchService`（`@ConditionalOnProperty(provider=bocha, matchIfMissing=true)` 默认激活，国内 `https://api.bochaai.com/v1/web-search`�? `TavilyWebSearchService`（`@ConditionalOnProperty(provider=tavily)`，海�?`https://api.tavily.com/search`）；用户服务器在境内 �?博查默认。`RequirementClarifyService.doRound` 首轮（`rounds==0`�? 开关开启时按首条消息截�?40 字调一�?`webSearchService.search`（关键词截断�?`queryKeywordLimit` 控制），失败一�?warn log + 返回空串（不阻断主流程）；`renderPrompt` 替双占位�?`{{CONVERSATION_HISTORY}}` + `{{WEB_SEARCH_CONTEXT}}`（无资料时占位为"（无可用联网资料�?�?Prompt 语义节稳定）；`prompts/requirement-clarify.md` 新增「联网检索资料」节明确"核心需求仍以用户描述为�?/"不要�?JSON 字段引入'参考资�?�?原则。`ClarifyMessageRequest` �?`Boolean webSearchEnabled`；Controller 透传三参�?Service；旧 `create(message,plannerAgentId)` 重载保留保兼容。前端：`RequirementChat.vue` `webSearchEnabled` ref 默认 true；输入栏左侧「联网搜索�? Connection 图标 + tooltip + `el-switch` inline-prompt（开/关）；`activeId==null` 时可改，已有 ACTIVE 会话置灰（开关仅建会话生效）；`watch(detail.conversation.webSearchEnabled)` 已存会话同步原值（不可改）；`handleSend` 新会话分支透传 `webSearchEnabled.value`；`types/RequirementConversation` �?`webSearchEnabled?:boolean|null`；`clarifyApi.create` �?`webSearchEnabled` 第三参。设计取舍：�?会话级而非回合级（避免每次重新检索副作用）；�?首轮注入而非多轮（避�?token 爆炸与上下文漂移）；�?失败降级而非事务回滚（联网是增强非核心）；④ 接口 + Router（业务侧零供应商感知，切�?新增零业务改动）；⑤ 默认 bocha 而非 Tavily（境内可用性）。`scripts/shell/verify-websearch-e2e.sh`（UTF-8 �?+ set -euo pipefail + curl + jq）覆盖三条路径——关（`webSearchEnabled:false` 落库 false + roundCount=1�? 开（`true` 落库 true + roundCount=1�? 不传（落�?null 保默认开启兼容）；`bash -n` + `zsh -n` 语法自检通过；`mvn compile` 沙箱�?mvn 需 IDE 验证；单测增量（联网降级 + 占位符替�?+ Provider 路由）待补。本轮明确不做：每轮重新检索、多供应�?failover、按角色区分检索策略、检索词 LLM 改写。迭代详�?§6.38
- **2026-08-03 Planner 对话双模式（V39，N17 升级�?*：需求澄清会话升级为 Kimi/DeepSeek 式双模式——CHAT 自由对话（通用助手模板纯文本问答，�?JSON 协议、无 progress、无首轮联网搜索、独�?50 轮上限）+ CLARIFY 方案澄清（沿用既有全部行为：首轮联网搜索 + progress 自评 + JSON 三选一协议）。单会话 `mode` 字段（NULL 老数�?CLARIFY 兼容），用户主导双向切换（to-clarify/to-chat 两端点），CHAT 命中意图词（整理成方�?生成方案/落地实施等）自动单向�?CLARIFY 且该条消息即澄清首轮、意图词永远放行。Flyway V39 `requirement_conversation.mode VARCHAR(16)` + CHECK 约束 + DO 验证块；`RequirementClarifyService` 双模式分派（create 四参重载�?initialMode / sendMessage 上限按模式分�?/ doRound 意图词置�?/ runLlmRound 按模式选模板与 scene / switchToClarify / switchToChat）；`prompts/requirement-chat.md` 通用助手模板；`ClarifyMessageRequest.initialMode` + Controller 三端点；前端模式徽标 + 进度条与终稿卡条件化 + 「转为方案」按�?+ 新会话模式单选。单�?35/35�?14 ChatModeAndSwitch）；`verify-planner-chat-dual-mode.sh` 真实环境 8 �?PASS（CHAT 纯文�?payload=NULL �?二轮�?CHAT �?to-clarify 产终�?finalTitle �?finalize 建任�?PENDING+回填 �?意图词新会话自动�?CLARIFY �?to-chat 仅置位不加消息）。迭代详�?§6.44
- **2026-08-03 意图词二次确认（V40，N17 升级�?*：去掉「转为方案」按钮，转方案改为「意图词触发 + 对话内二次确认」——CHAT 命中意图词不再自动切 CLARIFY，置 `pending_clarify_confirm` + 回复固定确认询问（不�?LLM、不加轮数）；回复确认词或再次意图词 �?转入 CLARIFY（该条消息即澄清首轮）；回复其他内容 �?清标记继续自由对话。Flyway V40 `pending_clarify_confirm SMALLINT NOT NULL DEFAULT 0`（�?.3 不用 BOOLEAN�? DO 验证块；自定�?`SmallIntBooleanTypeHandler`（SMALLINT↔Boolean）；doRound 三段状态机 + sendMessage 上限放行确认�?意图词；切换端点�?§8.2 整改 `toClarifyById`/`toChatById`；前端移除「转为方案」按钮。单�?ChatModeAndSwitch 19 例全�?+ 修复 `PlannerAnalysisServiceTest.shouldDecomposeAndPersistDrafts` pre-existing Error（补 list(Wrapper) stub�? helloai-core 全量 389/389 全绿 + vue-tsc 0 �?+ `verify-planner-chat-dual-mode.sh` 改造后 8 �?PASS（STEP7 意图词→待确认→回复确认→CLARIFY 全流程断言）。迭代详�?§6.45
- **2026-08-01 执行链依赖上下文注入（V35，执�?Agent 参考上游产出，同日第二轮）**：N6 执行链补缺——此�?`depends_on` 只做调度排序（解锁下�?拓扑/ready 守卫/跳过分发），执行 Agent �?Prompt 不含任何上游产出，依赖关系“只排序不传上下文”（用户审查时序图发现：只见领取/执行，不见参考依赖）。本轮在纯执行入�?`SubTaskExecutionService.executeOnce` 中新�?`loadDependencyContext`：按 `dependsOnIdList` 声明顺序�?`SubTaskService.listByIds` 批量查前置，DONE �?`context.lastExecution.output` 非空 �?�?`## 上游产出参考` 独立章节注入 `buildUserPrompt`（前置标�?状�?+ 产出正文，单条超 4000 字符截断并显式标注）；DONE 无产�?/ �?DONE（死信指派等旁路）→ 注入状态说明占位；查询或渲染异常一�?warn + 返回空上下文（产出参考是增强信息不是交付门槛，沿�?V34 联网搜索降级哲学）。可观测：声明依赖时记录�?timeline 事件 `sub_task_deps_context_loaded`（payload：depCount/loadedCount/truncatedCount/degraded，无依赖零噪音）。抽公共工具 `SubTaskOutputExtractor`（shared/util，静态方法读 `context.lastExecution.output`）并替换 `TaskFinalReportService` / `TaskDeliverableService` 两处同款私有实现，消除多消费方先例漂移。前端：`sequenceFlow.ts` LABEL �?`sub_task_deps_context_loaded: '装配依赖产出'` 且泳道归 EXT（执�?Agent）；`SubTaskDetail.vue` EVENT_META 加同 key（`参考上游产出` + 描述），时序�?时间线能看到“读取上游产出”环节。测试：`SubTaskExecutionServiceTest` 新增 5 例（无依赖不查库 / 有依赖注入产�?+ timeline 事件 / 前置无产出占�?/ 超长截断 / 降级不阻断且 degraded=true），16/16 全绿；全模块 `mvn compile` SUCCESS + `vue-tsc --noEmit` 0 错。无 Flyway 无配置项；真实环�?E2E 待回归。本轮明确不做：产出摘要�?向量化、跨任务依赖上下文、按依赖层级多级透传（仅直接前置）。迭代详�?§6.39
- **2026-08-02 子任�?LLM 对话消息可视�?+ reviewHistory 多轮累积（V38，�?.41�?*：N6 可观测性子增强——针对用户发现的两个互补缺环加近�?对话流黑�?（实际送给 LLM �?user prompt 未落库）�?单轮驳回信息丢失"（`context.lastAutoReview` �?Map 覆盖式写入导致历�?reviewer issue 被静默替换）。本轮同时落地两件：**（a）LLM 对话消息可视�?*——`SubTaskExecutionService` 构造器注入 `ConversationService`（同 `ExecutionResultHandler` 同款）；`executeOnce` �?`recordEvent(sub_task_llm_call_start)` 后、`executeSync` 前插�?`try { conversationService.addMessage(subTaskId, agent.getId(), "user", "agent", task.getUserPrompt(), "sub_task_execute_user_prompt"); } catch { warn }`；与 V28 既有 `sub_task_execute` 输出对偶形成 user→assistant 完整对话；异常路径（LLM 拋错）prompt 仍留库，�?`ExecutionResultHandler.handleFailure` 写入 `sub_task_execute_failed` 互补（前者保输入后者保错误）；不入�?`ApiKeyAgentExecutor` / `AgentChatClientService`，拦截点保持唯一�?*（b）reviewHistory 多轮累积**——`sub_task.context.lastAutoReview` (Map) 重构�?`context.reviewHistory` (List<Map>)，每�?`{round, ts, reviewerAgentId, issues, comment, score, executorDoneIssues}`；`SubTaskReviewService.rejectAndRework` 从覆盖式改为 append（读已有 List 或把�?lastAutoReview 包成首轮），同时�?`reviewHistory` �?`lastAutoReview`（最新值）保完全向后兼容；`appendReworkContext` �?`### �?N 轮` 铺开（优先读 reviewHistory，缺失时回退 lastAutoReview 包成单轮，issues 字段同时支持 List 新格式与 String 旧格式）；`executorDoneIssues` 字段预留读取但本轮不主动写（语义相似度对比留作后�?hook）�?*数据�?*：Flyway V38 `V38__review_history_backfill.sql` 幂等回填——`reviewHistory` 为空�?`lastAutoReview` 非空的子任务统一包成 `reviewHistory[1]`（`round=1` + `ts=update_time::text` 兜底 + `executorDoneIssues=[]`），保证新增读路径不因历史数据残缺而静默丢失�?*前端**：`SubTaskDetail.vue` `CONV_TAG_MAP` 新增 `sub_task_execute_user_prompt: { label: '执行请求', type: 'info' }`；现有「执行对话流」组件按 toolName 自动渲染气泡 + 折叠 + MarkdownView，零新增卡片/tab�?*验证**：`SubTaskExecutionServiceTest` 新增 `@Nested ExecuteOnceUserPromptAndReworkHistory` 5 例（user prompt 落库 / 异常路径 / reviewHistory 双轮铺开 / 全空跳过 / legacy lastAutoReview 兼容�? `SubTaskReviewServiceTest` 新增 4 例（首次驳回 round=1 / 二次驳回 round=2 / 兼容 / executorDoneIssues=[]），**9/9 全绿**；`mvn -pl helloai-core -am compile` SUCCESS + `vue-tsc --noEmit` 0 �?+ `npm run build` 产出 `SubTaskDetail-COMACOSA.js` 含新映射键；PS1 验证脚本 `scripts/powershell/verify-llm-conversation-stream.ps1` 新建覆盖 S1~S5 场景（对话流双气�?/ 首次驳回 reviewHistory=1 / 二次驳回 reviewHistory=2+userPrompt>=3 / dist 含标签键 / V38 回填由调用方�?MCP postgres_helloai 验证），`Parser.ParseFile` 静态自检 PARSE_OK�?*遗留**：① `executorDoneIssues` 自动回填 hook（语义相似度对比留作专门迭代）；�?PLANNER 对话流（PLANNER �?`requirement_message` V29-V33，不混用 conversation_message）；�?ApiKeyAgentExecutor / `AgentChatClientService` 下沉（拦截点 executeOnce 已足够）；④ user prompt 流式预览（一�?4-8KB TEXT 字段够用）；�?review_record 表改动（既有 round 足够，审计链不破）。N6 仍标"已交�?，本轮为子增强不�?N 列表新开条目。迭代详�?§6.40
- **2026-08-04 子任务分发失败快速兜�?+ 整合报告生成状态防重（V41�?*：用户实测两问题的收口——①�?0人小团队企业OA系统」任务子任务 #7 无人领取根因闭合（瞬时全员忙�?+ requireIdle 过滤�?pickPreferred 抛异�?+ unlockDownstream �?warn 吞掉 + 孤儿巡检 30 分钟窗口过长），兜底：孤�?PENDING 巡检阈�?30�? 分钟（`AgentExecutionProperties.pendingOrphanThresholdMinutes`，isReady 守卫保证不误伤）+ `SubTaskCompletionListener` 解锁失败�?`sub_task_dispatch_deferred` timeline 事件（异常不再静默）；② 报告按钮可重复点击导致重复生成，落地 `FinalReportStatus` 四态（NONE/GENERATING/DONE/FAILED，与 TaskStatus 解耦）+ Flyway V41 `task.final_report_status`（存量回�?DONE�? `TaskFinalReportService.generate` CAS 防重入（生成中拒绝重复调用，成功 DONE、失�?FAILED 可重试）+ 前端按钮禁用/「报告生成中」状态展�?5s 轮询。helloai-core 单测 397/397 全绿 + vue-tsc 0 �?+ 真实环境并发防重全链路冒�?PASS（GENERATING→第二次被拒→DONE|19091）。迭代详�?§6.47
- **2026-08-02 Snowflake ID 全链路字符串化修�?*：用户在使用"对话新建 �?终稿确认 �?查看任务/自动拆解"链路时，遇到 `GET/POST /api/requirement-conversations/{id}` �?`/api/tasks/{id}/plan` 返回 `400 Bad Request`。根因是 Snowflake 长整�?ID（如 `2083818000152453122`）超�?JavaScript `Number` 安全整数范围（`2^53-1`），前端 JSON 解析后精度截断，再传回后�?URL 路径参数�?Spring 解析失败。本轮全链路加固�?*后端**——`BaseEntity.id` �?`@JsonSerialize(using = ToStringSerializer.class)`，所有继承实体（Task/SubTask/RequirementConversation/RequirementMessage/ConversationMessage 等）�?`id` 统一序列化为字符串；`helloai-common/pom.xml` 新增 `jackson-databind` 依赖以支撑注解；`RequirementConversation.taskId`/`plannerAgentId`、`RequirementMessage.conversationId` 加同样注解；关键 DTO（`TaskResponse`、`SubTaskResponse`、`AgentResponse`、`ReviewResponse`、`ModuleResponse`、`ConversationMessageItem`、`TaskTimelineItem`）的所�?`Long` �?ID 字段�?`@JsonSerialize`/`@JsonSerialize(contentUsing)`�?*前端**——`clarifyApi` 所�?URL 拼接�?`String(id)`；`RequirementChat.vue` �?`activeId` 与所有会�?任务 ID 交互强制 string；`TaskList.vue`、`PlanReviewDialog.vue`、`FinalReportDialog.vue`、`TaskDeleteDialog.vue`、`TaskFormDialog.vue` 中所�?`row.id` / `props.task.id` �?API 或拼 URL 处加 `String()` 防御�?*可观测性增�?*——`SubTaskReviewService` 新增 `subtask_review_result` 对话消息（通过/驳回、评分、问题、评语），前�?`SubTaskDetail.vue` 把执行对话流按「执行轮�?/ 核验轮次」分组展示，user prompt �?assistant 返回成对可见，返工轮次可展开查看完整 Prompt 含历史审核意见�?*验证**：`mvn clean compile -pl helloai-common,helloai-core,helloai-api,helloai-start -am -DskipTests` SUCCESS；`npm run build` SUCCESS�?*遗留**：后端必须重启后 `BaseEntity` 注解�?DTO 注解才生效；已运行的旧会�?子任务历史数据中�?ID 仍是 number，新创建的对象走字符串协议。迭代详�?§6.41
- **2026-08-07 LLM Provider 动态化方案B（V46，N9 §6.51 后续�?*：上一轮把「平台级 API Key + 外网地址」从 yml 抽到 DB/runtime，但 Provider 本身（baseUrl / defaultModel / protocol / 启用状态）仍依�?yml 硬编�?4 家。本轮交�?Codex++ 风格的「动态添加供应商」能力——任何内�?/ 自定�?OpenAI / Anthropic 兼容厂商不再需发版。Flyway `V46__llm_provider_table.sql` 新增 `llm_provider` 表（provider_code/provider_name/protocol_type OPENAI_COMPATIBLE|ANTHROPIC_COMPATIBLE/base_url/default_model/enabled/builtin/sort_order/extra_config JSONB�?+ `update_update_time` 触发�?+ `idx_llm_provider_enabled` 部分索引 + 幂等写入 4 �?builtin（deepseek/moonshot/minimax/dashscope）；后端 `system.entity.LlmProvider` + `LlmProviderMapper` + `LlmProviderService`（ServiceImpl，code 校验：正�?`[a-z0-9][a-z0-9-]{1,63}` + 唯一�?+ builtin 不可�?code + builtin 不可�?+ toLowerCase 归一化提前到 validateCode 之前 双重防御�?+ `LlmProviderQueryService.findByCode/listEnabled/listAll/getBaseUrlWithFallback`；通用厂牌族：`OpenAiCompatibleProtocolFactory`（原 Moonshot/DashScopeFactory + 新增自定义场景）+ `AnthropicCompatibleProtocolFactory`（原 Minimax 抽出来）+ `LlmProviderChatClientFactoryRegistry`（按 `provider.protocolType` 分发到对�?ProtocolFactory，deepseek 走官�?SDK 优先匹配）。`ProviderChatModelCache.buildKey` 扩展�?`(provider, apiKey, baseUrl, protocolType)` 四参数防 OpenAI/Anthropic 实例串错�? 个专�?Factory（Moonshot / DashScope / Minimax�? 抽象基类 `AbstractOpenAiCompatibleProviderChatClientFactory` 全部删除（残留检查在 §3.3）；`LlmProviderCatalogService` �?DB listAll 枚举 + `PlatformProviderConfigService` 三层 fallback（DB > sys_config > yml）收�?API Key �?baseUrl 读取。`AgentChatClientService` 构造参数由 `ObjectProvider<List<ProviderChatClientFactory>>` 改为 `LlmProviderChatClientFactoryRegistry` 注入。New `AdminLlmProviderController`（`/api/admin/llm-providers`，`list/getById/toggleById/{id}/deleteById/updateById/{id}` + api-key �?`PlatformProviderConfigService`�? `CreateLlmProviderRequest/UpdateLlmProviderRequest/LlmProviderResponse`。前�?`settings.ts` �?`llmProviderApi` + `LlmProviderResponse/CreateLlmProviderRequest` 接口；`Settings.vue` 重写�?Codex++ 风格「左侧供应商列表 + 右侧详情面板」布局 + �? 添加供应商」对话框（名�?/ 协议下拉 / Base URL / 默认模型 / API Key�? builtin 标签 + 启用开�?+ 删除按钮（builtin 不显示）+ ServerURL 用途调整为“生�?SKILL 接入内容”。外部访问地址 `sys_config["helloai.base-url"]` 不动。单测：`LlmProviderServiceTest` 9 例全绿（重点修补：`ServiceImpl.baseMapper` 擦除类型�?BaseMapper，不�?type 参数�?`ReflectionTestUtils.setField` 注入 + 补齐 MIXED-Case 归一�?/ Bad@Code 非法字符 / 单字长度的三段校验）+ `PlatformProviderConfigServiceTest` �?DB 优先/兜底用例 + `AgentChatClientServiceTest`/`PlatformAgentExecutionServiceTest` 改造从 ObjectProvider 改为 Registry mock；`mvn -pl helloai-core test` 416/416 全绿 + `mvn clean package -DskipTests` 7 模块 BUILD SUCCESS。文档：CODE_STYLE V1.6 增补 3.x 业务域分包「system = ...,LLM Provider」职责补丁与「新�?LLM 厂商仅动 llm_provider �?+ chat.provider」动限范围。本轮明确不做：拖拽排序前端实现、Provider 粒度限流 / 配额、`from external import` 第三方批量导入、事件总线配置变更广播、Provider 配置变更审批流。迭代详�?§6.52
- **2026-08-11 MCP 接入体验改进（A0-2，trae 实战反馈�?1/2/4，�?.61�?*：外�?Agent 接入三痛点收口——① **Session 生命周期核验**（反编译 spring-ai 1.1.8 所�?MCP Java SDK 0.18.3 确认）：session 严格绑定 SSE 连接，断开即被回收，`POST /mcp/messages?sessionId=<旧sid>` 确定性返�?404 "Session not found"；SDK 无保留窗口可配置 �?**复用决策：不�?transport 改造，REST 别名通道承担免握手复�?*；② **同步响应**：`POST /api/mcp/jsonrpc`（McpController，@Deprecated 承载 A0-2）无状态同步返回完�?result（免 SSE、免握手�?0 工具矩阵�?MCP 对齐），MCP 通道保持协议标准；③ **错误修复提示**：`McpAuthFilter` �?`BufferedResponseWrapper`（setContentLength/flushBuffer no-op + flushToUnderlying，真�?Tomcat 无损�? 404 �?evict SESSION_AUTH（防陈旧鉴权缓存复活失效 session�? body �?`fixHint`（重握手 / �?REST 别名双路径提示）；④ **Schema 确认**：tools/list 参数 Schema �?spring-ai `@ToolParam` 自动生成（description + required），无需补；关键发现—�?*�?`notifications/initialized` �?3 步握手时 tools/call 永久挂死**（服务端 exchangeSink 等待 initialized 信号，HTTP 永不返回，实�?20s+ 超时，非报错而是无响应），已强化写入 executor SKILL.md §1.4（四步握�?+ arguments 显式透传 sessionId/agentId + 失效双修复路径）；McpMcpServer 路径 1 需客户端在 arguments 显式�?sessionId（spring-ai 1.1.x 不隐式注�?ToolContext）。验证：单测 McpAuthFilterTest 6 + McpControllerJsonrpcTest 8 全绿；真实环�?404 + fixHint + SESSION_AUTH evict 日志确认；`scripts/powershell/verify-mcp-session-e2e.ps1` PASS=17 FAIL=0（含 D1-D3 四步握手 tools/call 全链 + F1 未知 sessionId 确定�?404/fixHint 断言 + F2 断连复用观察）。遗留：SDK 断连回收延迟窗口�?2s 内旧 session �?200）与断连后偶发挂死属 SDK 内部行为，外�?agent �?404 �?REST 别名即可；跨连接 session 复用需自定�?transport，留作专门迭代�?
- **2026-08-12 工具面统一：三通道 10 工具对齐 + SKILL 逐动作速查�?+ 405 语义修复（A0-3，�?.62�?*：外�?agent「零试错接入」目标收口——① **盘点结论**：三通道工具面（MCP SSE `McpMcpServer` @Tool / REST 别名 `POST /api/mcp/jsonrpc` / REST 直�?`POST /api/mcp/tools/*`）中，前两�?10 工具对齐，但 REST 直通仅实现 7 个，�?`getAgentStatus`/`checkIn`/`checkOut` 3 个（此前 checkIn/checkOut 只挂 `/api/agents/check-in-out` 单端点，�?getAgentStatus 直通）；② **补直通端�?*：`McpController` 新增 3 个直通端点（无状态同步，复用 `McpToolService` 业务下沉，与 MCP/别名通道同一实现），REST 直通声明与实现 10/10 对齐；③ **SKILL.md 改版**：新增〇章外部接入总览 + 0.1 三通道分工速查表（长连�?/ 免握�?/ �?JSON-RPC 三形态选型�? 0.2 REST 端点速查总表�?3 条路由逐动作标注：方法 + 路径 + curl 示例 + 返回结构 + 错误码），外�?agent 只读 SKILL 即可正确调用；④ **防漂�?*：入库脚�?`scripts/powershell/verify-tool-matrix.ps1` 7 步校验（S1 三通道 10 工具 diff / S4 SKILL 0.1 表与工具清单 diff / S5 13 条路由存在性探�?/ S6 旧路径残�?0 / S7 checkIn/checkOut 回执断言），真实环境 PASS=23 FAIL=0；⑤ **405 语义修复**：`GlobalExceptionHandler` �?`HttpRequestMethodNotSupportedException` �?HTTP 405（此前被 Exception 兜底�?500，POST-only 路由 GET 探活失真），SKILL 错误码表与运行时一致；�?**V47 遗留测试缺陷修复**：`AgentSelector.pickAlternative` V47 �?3 参（constraints 贯穿 fallback）后主代码已�?3 参调用，�?6 处测�?stub/verify �?mock 2 �?�?stub 失效返回 null �?5 �?fallback 用例失败；补�?3 �?`any()` 后全�?**486 测试全绿（core 474 + api 12）BUILD SUCCESS**。迭代详�?§6.62
- **2026-08-12 MinIO 附件存储集成 + 附件目录路径规范（A0-5 遗留②收口，§6.75�?*：把 MinIO 从「docker-compose 里躺着」变成附件存储后端——引�?`io.minio:minio:8.5.12`（根 pom 版本集中管理）；`MinioArtifactStorage` 落地（storageUrl=`minio://{bucket}/{objectKey}`，懒创建客户�?+ 首次写入自动建桶）；`CompositeArtifactStorage`（@Primary 路由：store �?`helloai.storage.type` 选主存储，load/supports 按协议前缀分派，存�?local:// 与新 minio:// 附件同时可读/可下�?可作执行证据）；objectKey 统一规范 `{ownerName}/{yyyy}/{MM}/{taskId}/{subTaskId}/{uuid8}-{safeName}`（Local �?Minio 双实现一致，归属�?执行 Agent 注册名，接口 static 清洗防路径穿越）；`ExecutionArtifactService` 物化链传 ownerName+taskId；`application.yml` storage.type 默认�?minio（端�?凭证 `${MINIO_*}` 环境变量兜底）；uploadArtifact 工具描述�?executor/planner SKILL.md 同步「平台可直读 minio:// + 路径规范」。验收脚�?`scripts/powershell/verify-minio-artifact.ps1`（G1 MinIO health / G2 minio:// 附件存在�?objectKey 合规 / G3 下载 200+非空+�?302）。单测全�?551 全绿（新�?Minio/Composite 存储测试 + Local/ExecutionArtifact 适配）。真实环�?G2/G3 待后端重启后实测。遗留：agent.name 无唯一索引（重名目录合并）；外�?Agent 上传路径�?SKILL 约定；存�?local 附件不迁移�?
- **2026-08-12 登录链路脚本化验�?+ MinioArtifactStorage 启动缺陷修复（�?.76�?*：登录页�?API 登录（注�?+ 账号密码登录为主）改造后，新�?`scripts/shell/verify-login-e2e.sh` 脚本模拟登录全链路—�?1 项用例：空用户名/空密�?未知用户/错误密码/非法登录类型（旧 api 入口服务端拒绝）全部拒绝，账号密码登录成功拿�?token，`/api/auth/me` �?token 返回身份、无 token 返回 401，logout 后旧 token �?401 拒绝；真实环境（jar 启动 6565�?*11/11 �?PASS**（`ADMIN_USER/ADMIN_PASSWORD` 可覆盖，默认 admin/admin123）。顺带修�?v2.7（�?.75）引入的启动缺陷：`MinioArtifactStorage` 手写包级测试构造器屏蔽�?Lombok `@RequiredArgsConstructor` 生成的构造器（Lombok 规则：类中已存在任何构造器即不再生成），Spring 启动�?`No default constructor found`——删除测试构造器恢复 Lombok 构造器，单测改同包字段注入 mock client，应用可正常启动，存储层单测全绿。遗留：`type=agent` 登录通道保留�?MCP/CLI 使用（前端已不暴露）。`@Valid` 参数校验 500 问题已由 §6.78 修复�?
- **2026-08-12 MinIO 附件 E2E 真实环境验证 + 前端登录页构建验证（§6.77�?*：A0-5 遗留②完整闭环——新�?`scripts/shell/verify-minio-artifact.sh`（macOS zsh 版，替代 PowerShell 版）自动触发「最小执行物化链」（无需 LLM 凭证：建 Agent �?建任�?�?建子任务指派 �?claim �?submitResult 触发 ExecutionResultHandler 物化 �?ExecutionArtifactService.store 写入 MinIO），真实环境 **9/9 �?PASS**：G1 MinIO health / G2 附件列表存在 minio:// �?objectKey 符合 `归属�?�?�?taskId/subTaskId/uuid8-文件名` 规范 / G3 下载 200+非空+Content-Disposition �?302；`docker exec helloai-minio mc ls` 确认桶内对象真实存在�?9B）——物化落桶全链路闭环，�?.75 遗留④关闭。前端侧：`vue-tsc --noEmit` 0 �?+ `npm run build` 成功�?.82s），登录页改造（�?api 登录，登�?注册双入口）构建通过。遗留：内部 LLM 执行物化待配置平�?LLM 凭证后复核；脚本每次运行新增测试 Agent/任务/子任务（幂等设计无清理）�?
- **2026-08-12 参数校验语义修复：@Valid 失败 500 �?400（�?.78�?*：`GlobalExceptionHandler` 新增 `MethodArgumentNotValidException` handler——`@Valid @RequestBody` 校验失败（如登录空密码）返回 **HTTP 400 + `R.fail(400, 首条字段错误消息)`**（此前被 `Exception` 兜底�?HTTP 500）；前端拦截器基�?body.code 判断不受影响，校验消息直接展示。`verify-login-e2e.sh` [1]/[2] 用例由宽松断言改为明确断言（[2] 空密�?HTTP 400 + code=400 + 消息含「凭证」；[1] 空用户名 username 无校验注解走业务�?500 用户不存在）。真实环境登录链�?11/11 �?PASS + MinIO 物化�?9/9 无回归，§6.76/6.77 遗留③关闭�?
- **2026-08-13 Provider 域单测补�?+ 路由大小写归一修复（C1，�?.80�?*：a0-plan 批次 C1 预检确认「补三个 Factory」为过时项（§6.52 方案 B 已通用化交付），真实缺口为 Provider 域零单测——新�?4 测试文件 41 用例全绿（`OpenAiCompatibleProtocolFactoryTest` 9 / `AnthropicCompatibleProtocolFactoryTest` 9 / `LlmProviderChatClientFactoryRegistryTest` 6 / `LlmProviderCatalogServiceTest` 17），覆盖 apiKey 校验、ChatModel 类型�?defaultModel 三级兜底、baseUrl 回退、缓存四元组复用�?apiKey 隔离、Registry 路由优先级（deepseek 专用 > 协议工厂）、目�?available 组合、平台密钥绑定幂等与供给解析。顺带修复判定不一致缺陷：Registry 路由 protocol_type 大小写归一（`normalizeProtocolType`，与 `isFactorySupported` 口径统一），消除 DB 小写写入时「目录可用、路由失败」的隐性不一致；并修�?§6.75 MinIO 改动引入�?`CompositeArtifactStorageTest` supports stub 回归。验证：`mvn test -pl helloai-core -am -DskipTests=false` **592/592 全绿** + 真实环境 `GET /api/admin/agents/listLlmProviders` 4 provider `factorySupported=true / available=true` 全绿�?
- **2026-08-13 Service 接口/impl 分层拆分（批�?b0-b4，�?.84�?*：core �?Service 层从「类即服务」重构为「接�?+ impl」成对拆分——接�?`{Name}Service` �?`{domain}.service`（继�?`IService<Entity>`），实现 `{Name}ServiceImpl` �?`{domain}.service.impl`（继�?`ServiceImpl<Mapper, Entity>`），Controller 与跨域引用一律只依赖接口。已落地：system �?13 �?/ task �?11 + spec 3 拆（`task/spec` �?Service 合一�?`task/service`�? planner + review �?4 �?+ search 两实现迁�?`planner/service/impl` + RequirementConversation/Message �?impl / agent �?10 移入拆（chat/command/execution/observability/output/mcp 散包 Service 统一归位 `agent/service` + impl）；`TaskAgentPolicy` 归位 `task/policy`、`PlannerAgentPicker` 归位 `planner/picker`。验证：7 模块 compile EXITCODE=0 + 4b import 闭环 201 tests 全过。CODE_STYLE v2.8 同步（�?.x/§4.x/§7/§20/§21.2）。遗留：b4 剩余�?0 现有拆」与 b5（shared DoorbellService + mq MessageDeduplicationService）待续，b6 全量回归�?b4/b5 完成后执行�?
- **2026-08-13 附件管理双分类逐级下钻（�?.85�?*：附件管理页从单层表格升级为「MinIO 附件 / 本地附件」两类顶级文件夹 + 面包屑逐级下钻（Windows 资源管理器式点击跳转）：agent �?年月 �?任务标题（ID 副文本）�?子任务标题（ID 副文本）�?文件下载；后�?`Attachment` 实体 +3 transient 回填字段（taskId/taskTitle/subTaskTitle�? `AttachmentServiceImpl.list` 批量回填标题（listByIds 双查�?N+1，子任务已删容错）；前端兼容 minio 6 段新格式�?local 3 段老格�?objectKey；顺带修复下�?404 bug（`/downloadById/{id}` 正确端点）。验证：`AttachmentServiceImplTest` 3/3 全绿 + `vue-tsc -b --force` 0 错误。真实环境页面效果待用户重启后端后验证�?
- **2026-08-14 执行产出物化方案3 + Reviewer 附件内容级核验（F1-F3 收口，�?.93�?*：方�? 遗留的方�?（LLM manifest 结构化多文件协议）与核验侧内容级缺口一并闭合——① **F1 交付�?*：`Manifest`/`ManifestFile` DTO + `ExecutionOutputParser.parse` 扩展（```json 围栏提取，命中且 files 非空 �?多文件物化；未命�?非法 �?无损降级纯文本单 .md�? `ParsedOutput(files, displayText)`（结构化�?`displayText = summary + "## 产出文件概览" + "- {name}" + EXECUTION_RECORD 尾`，纯文本�?= raw�? `buildUserPrompt` 追加可�?manifest 协议指令（JSON 块位�?EXECUTION_RECORD 块之前）+ `ExecutionResultHandler` 注入 parser 挂接 displayText（物化开启时 output/对话流写 displayText 不刷文件正文）；�?**F2 核验�?*：`SubTaskReviewServiceImpl.buildAttachmentContent`（attachment 表可直读附件逐附件注�?`### {fileName}` 节，每附�?8000 / 总计 24000 字符限额截断，不可直�?为空显式标注�? `prompts/subtask-review.md` 新增�?# 物化附件内容（平台直读，已按限额截断）」节�?{{ATTACHMENT_CONTENT}} 占位 + �?10 �?声称交付�?�?文件正文 �?验收标准"三者一致性判定规则；核验 Prompt 落库 `conversation_message`（tool_name=`subtask_review_prompt`）供审计断言；③ **F3 验证**：`scripts/powershell/verify-artifact-content-review.ps1`（规�?6 UTF-8 头模板）真实环境 **PASS=23 FAIL=0 SKIP=1 ALL PASSED**——S6 manifest 多文件物化（2 附件 mime/size/minio:// 各自可下载且正文匹配 + displayText 概览�?JSON 泄漏）、S7 核验 Prompt 断言（当前环境无绑定 vault �?REVIEWER/PLANNER agent �?脚本自检 SKIP 兜底，绑定后自动执行断言）、S8 纯文本降级回归（�?.md + output 原样）、S9 级联删除 0 残留；设计文档已标注方案3 已实现并补核验侧 §5.4。遗留：S7 内容断言待绑�?REVIEWER vault 凭证后实测；本轮代码与文档未 git 提交，待用户确认后提交�?
- **2026-08-19 拆解异步化改造（§6.103，N16 增量�?*：闭合「前�?120s 超时倒挂后端 LLM 最�?245s」问题——`PlannerAnalysisServiceImpl.decompose` 收窄为同步守卫（校验 + CAS �?PLANNING + `task_plan_async_submitted` + 提交异步即返回空列表，`planById` 契约不变）；LLM 段迁至新�?`PlannerDecomposeAsyncService/Impl`（`@Async("plannerDecomposeExecutor")` 2/4/20 池外�?`helloai.planner.decompose` + 幂等守卫 + 失败闭环回退 PENDING），新增 `task_plan_llm_call_end` 观测事件；`PlanningTimeoutTask`（helloai-job�?0s + Redis token 锁）CAS 回收卡死�?10 分钟 PLANNING 任务（`task_plan_timeout_recovered`）；前端 TaskList 提交即返�?/ PlanReviewDialog 3s 轮询�? 分钟上限�? RequirementChat 异步语义。测试拆分：`PlannerAnalysisServiceTest` 重写 13 �?+ 新增 `PlannerDecomposeAsyncServiceImplTest` 12 �?+ `PlanningTimeoutTaskTest` 6 例全绿，vue-tsc 0 错。无 Flyway，重启后端生效；真实环境端到端回归待实测�?
- **2026-08-19 附件同名去活版本�?+ 核验死信显式化（§6.104，N6 增量�?*：附件管理（§6.85）后续项——收口「Reviewer 版本冲突死循环」：附件版本化（`AttachmentStatus` ACTIVE/INACTIVE/DELETED + `register` 提交前对�?subTaskId �?fileName �?ACTIVE 存量批量�?INACTIVE，同名最新版唯一 ACTIVE；`list` 全量供历史回�?/ 新增 `listActive` 平台可信视角�? 核验证据、执行上游装载、交付物打包、MCP 附件查询 4 处消费方�?`listActive`；`prompts/subtask-review.md` 补版本豁免说明（不得以版本冲突单一理由驳回）；核验 rework 达上限（默认 3）显式记 `sub_task_review_dead_letter` 死信事件（DLQ，与调度死信对称�? `sub_task_manual_intervention_required` 人工介入 timeline（OPS�? �?reason 全集�? 前端时序�?OPS/DLQ 泳道补全与死信折�?+ 附件管理�?activeOnly 开�?详情页历史回查（旧版灰显+「旧版本」标记）。测�?5 �?96 用例全绿（Attachment 15 / Review 28 含死�?payload 断言�? vue-tsc 0 �?+ `scripts/powershell/verify-attachment-version.ps1`（S0-S5 同名去活链路，ParseFile 自检通过，待真实环境）；CODE_STYLE 追加「代码修改符合规�?+ 测试优先 ps1 脚本」两准则�?*§6.104 第二轮（用户实测发现「打回后旧附件仍�?ACTIVE 参与下次核验」是同名去活未覆盖的另一面）**：当 Agent 被打回（自动核验驳回 / 人工驳回）后只修改本�?+ �?output + submitResult（不重新 uploadArtifact）或上传不同名新文件时，�?ACTIVE 附件仍被核验�?listActive 命中、复现死循环。落地：�?`AttachmentService` 接口新增 `invalidateBySubTask(Long subTaskId)`（lambdaUpdate eq subTaskId + eq ACTIVE �?set INACTIVE，与 register 同名去活互补：register 解决「再次上传同名旧版失效」，invalidateBySubTask 解决「被打回但未重新上传时旧证据�?ACTIVE」）；② `SubTaskServiceImpl` 收口在两个打回入口唯一汇聚点（自动核验 `rejectAndRework` �?`rework`；人工驳�?`createReview` REJECTED 分支 �?`reworkFresh`），通过 `ObjectProvider<AttachmentService>` 懒解析打破与 `AttachmentServiceImpl` 的构造器循环（后者依赖本服务�?register 归属校验；项目惯例见 `CompositeArtifactStorage` / `WebSearchServiceRouter` / `ExecutionDispatchValidator` / `McpAuthFilterConfig`），私有方法 `invalidateAttachmentsOnRework` 采用 best-effort try-catch warn（参�?`sendReworkInboxNotification` 哲学——严格抛异常会导�?@Transactional rework 回滚、Agent 收不到返工通知卡死 REVIEW）；�?`McpMcpServer.uploadArtifact` @Tool description 与三角色 `skills/{executor,planner,reviewer}/SKILL.md` 补版本语义（上传时告知名版自动失�?/ 被打回必须重传最新版 / 核验�?ACTIVE 版判定、不得以版本冲突驳回）；�?测试 4 �?40 用例全绿（AttachmentServiceImplTest +2 `invalidateBySubTask` 主路�?nullId 跳过 / HandoverTest reworkFresh 用例 `verify(attachmentService).invalidateBySubTask(SUB_TASK_ID)` / QuotaTest + IsReadyTest 构造参数末尾追�?ObjectProvider mock）。迭代详�?§6.104
- **2026-08-19 Planner 对话完善：意图确认弹�?+ 联网搜索可视�?多轮 + 选项单列（V41，�?.105，N17 增量�?*：用户实测四痛点收口——① 意图二次确认从固定文本询问升级为弹窗式结构化选项卡（`buildConfirmAskPayload` 单题双选项 confirm-switch，仅「确�?取消」不给推荐项�?*关键实施发现**：StructuredQuestionCard 提交文本形如「问题文本：确认」，不命�?`CONFIRM_PHRASE_PATTERN` �?`^` 开头锚定，点「确认」不会切模式 �?�?selections 快照判定通道，三通道确认 = 手打确认�?OR 再次意图�?OR 卡上点选；Jackson `objectMapper.copy().setSerializationInclusion(NON_NULL)` 避免 `recommended:null` �?payload）；�?意图词正则扩�?8 组组合正则（覆盖「新建个计划吧」「帮我生成计划」「帮我总结一下」「给一个方案」等口语，误触发守卫：裸任务词�? 号任务进展如何」不触发）；�?联网搜索从仅首轮放宽�?CLARIFY 多轮都可用（成本�?MAX_ROUNDS=20 封顶�? 新增 `WebSearchOutcome`（过程与结局完整快照，失败降�?failed outcome 不阻断主流程、查询词空白返回 null 未搜不落键）+ payload �?`webSearch` �?+ 前端新增 `WebSearchBar.vue` 折叠查验条（对齐 DeepSeek/Kimi 形态，让用户可查验「是否真正搜索了、搜了什么词、来源内容是否正确」，failed/total=0 异常结局同样可视化）+ `types/clarify.ts` `WebSearchTrace`/`WebSearchSource` 类型；④ 澄清选项纵向单列（`.sq-options` flex-direction:column + `.sq-option` width:100%）。兼容性设计：终稿分支未搜索时保持�?3 �?addMessage 形态保既有测试；老会�?payload �?`webSearch` 键自然不渲染查验条。验证：`RequirementClarifyServiceTest` 53/53 全绿（新�?8 例：确认卡结�?新意图词�?裸词守卫/卡选确认切 CLARIFY/卡选取消续 CHAT/二轮触发搜索/搜索异常降级�? vue-tsc 0 错；踩坑固化两条（`-pl helloai-core` 单跑测试需 `-am` 防本地仓库旧 jar；确认卡序列化必�?NON_NULL）。无 Flyway 无配置项，重启后端生效；真实环境手工回归（确认弹窗交�?/ 查验条展开回显 / 多轮搜索 / 选项单列）待实测�?
- **2026-08-19 DeepSeek 原生联网搜索 adapter（V42，�?.106，N17 增量�?*：用户实测博查效果不理想后的第一步方案——把 DeepSeek �?搜索引擎"用：新增 `DeepSeekNativeSearchServiceImpl`（实现既�?`WebSearchService` 接口，provider=`deepseek-native`，`@ConditionalOnProperty` 条件装配 Router 零改动生效），走 DeepSeek Anthropic 兼容端点 `/anthropic/v1/messages` + `web_search_20250305` 服务端工具单次调用，解析结构�?`web_search_tool_result` 块（title/url/引用原文，siteName �?URL host 推导）映�?`WebSearchResult`；`WebSearchProperties` �?5 �?deepseek-* 字段（超时独�?15s：该路径是一次完�?LLM 调用）。澄清主 LLM 调用链、prompt 模板、查验条、前端零改动（归一化契约不变）。代价：每次搜索成本/耗时高于普通搜�?API，随 CLARIFY 轮数线性。验证：`DeepSeekNativeSearchServiceImplTest` 11/11 全绿（JDK HttpServer 桩端点，报文/解析/降级全覆盖）+ `RequirementClarifyServiceTest` 55/55 回归全绿。切换：`helloai.web-search.provider=deepseek-native` + `deepseek-api-key` 重启生效，回退博查改回 provider 即可；真�?Key 端到端回归待用户配置后实测；Kimi/MiniMax 原生适配�?per-planner 动态路由为二期项�?
- **2026-08-19 联网搜索 URL 分离 + 网页直取（V43，�?.107，N17 增量�?*：用户实测发现新缺陷——消息中的网址（如「给我一份如何快速上�?https://open.maic.chat/ 的操作手册」）未分离，整段输入截前 40 字直接当搜索词，搜索引擎对裸 URL 文本检索效果极差。修复：�?新增 `WebPageFetchService/Impl` + `WebPageContent`（直接访问用户给出的网页并抓取正文：HTML 去标签提纯、字符限额截断、超�?异常降级不阻断），`WebSearchProperties` �?4 �?`urlFetch*` 字段（默认开�?/ 8s 超时 / �?2 �?/ 4000 字符）；�?`doWebSearch` 改为先提取消息中 URL（直取页面内容注入上下文）再�?*剥离 URL 后的语义文本**作搜索词（纯 URL 消息回退域名，空文本不再发起无效搜索）；�?直取页面作为来源置顶合并�?`WebSearchOutcome`（新�?`fetchedPages` 字段），查验�?前端零改动（归一化契约不变，直取来源与普通搜索结果同样渲染）。验证：`WebPageFetchServiceImplTest` 10/10 全绿（JDK HttpServer 桩端点，提纯/截断/降级全覆盖）+ `RequirementClarifyServiceTest` 59/59 回归全绿（新�?4 �?V43 URL 分离回归�? `DeepSeekNativeSearchServiceImplTest` 11/11 回归全绿。无 Flyway，重启后端生效；真实环境�?URL 对话端到端回归待实测�?
- **2026-08-19 联网搜索 V44：SPA 空壳元数据兜�?+ 直取失败域名前置（�?.109，N17 增量�?*：用户实测日志暴�?V43 两个叠加缺陷——open.maic.chat �?JS 渲染 SPA，直�?HTML 只有空壳（textChars=0）且空壳页携带的 title/meta 描述被失败路径整体丢弃；直取全败时搜索词不含域名检索不到站点公开资料；叠加博�?Key 未配�?�?results=0 零资料。修复：�?`WebPageFetchServiceImpl` 正文为空时新�?`salvageMetaText` 兜底（提�?title/meta description/og 标签拼作「站点名�?描述」最低限度资料，ok=true + metaOnly=true�? UA 换浏览器风格降反爬拦截；�?`WebPageContent` �?`metaOnly` 字段并落 payload 可查验；�?`doWebSearch` 搜索词三分支（纯 URL 回退域名保持 V43 / 直取全败时域名前置增强搜索词 / 直取成功含兜底不加前缀）。验证：网页直取 12/12 + 澄清 61/61 + DeepSeek 11/11 全绿。无 Flyway 无新配置项，重启后端生效�?*用户侧待�?*：配置博�?Key（BOCHA_API_KEY）或�?provider=deepseek-native（�?.106）二选一，否则搜索段仍无结果�?
- **2026-08-21 联网搜索 V45：搜索查询规划器（�?.131，N17 增量�?*：用户实测长问句（含敬语/连接符，如「能否给我提供一份快速学习Python + 快速搭建项目的完整方案，按"�?�?做？」）博查零结果——根�?`doWebSearch` 把用户原消息截前 40 字直接当搜索词，敬语/疑问前缀/标点/连接符全是检索噪音且单查询零结果即放弃。改造：�?新增 `SearchQueryPlannerService/Impl`（CODE_STYLE §4.2 接口+impl 成对拆分）——规则清洗总是执行（去敬语/疑问前缀 �?按连接符拆分多主�?�?去标点语气词 �?截断去重�? 条件 LLM 改写（仅规则产出单候选词且长�?疑问句式才进 LLM，JDK HttpClient 直连 DeepSeek 轻量快模型独�?5s 超时，失败降级规则结果）；② `doWebSearch` 改多候选词顺序降级（首个命中即停），规划器空回退截断旧逻辑；③ `WebSearchOutcome`/payload 新增 `queries` 字段，查验条 `WebSearchBar.vue` 多词显示「已依次搜索 N 个关键词」（旧消息回退 query）。`WebSearchProperties` �?`queryRewrite*`/`maxQueries` 5 配置（复�?deepseekApiKey，空 Key 自动禁用）；新增 prompt 模板 `websearch-query-rewrite.md`。验证：SearchQueryPlannerServiceImplTest 7 �?+ RequirementClarifyServiceTest +3 例，helloai-core 全量 896/896 全绿 + vue-tsc 0 错。无 Flyway，重启生效；真实环境回归待实测。迭代详�?§6.131
- **2026-08-21 admin 授权拦截收口（N21，评审报�?P0�?*：后端代码评审报�?P0「admin 授权缺口」闭合——新�?`AdminOnlyInterceptor` 并在 `WebMvcConfig` 注册�?`/api/admin/**`�? �?admin Controller 一次性全覆盖，含评审报告漏列�?`AgentDutyLeaseController`）；`/api/agents/register` �?`allowRegistration` 门控；CODE_STYLE V1.8 §6.8 授权拦截红线；防回归脚本 `verify-admin-authz.ps1` + `AdminOnlyInterceptorTest` 单测�?*纠偏**：本文档 N10 与迭代记�?§6.81 中「平台级凭证管理（AdminLlmProviderController / AdminProviderConfigController）走 admin 拦截器」的表述�?2026-08-21 之前不属实（当时不存在任�?admin 拦截器，凭证管理端点实际裸奔），本轮拦截器落地后该表述才真正成立；历史行不改写，以此条澄清为准�?
- **2026-08-22 依赖方向红线冻结 + system 域搬迁（N22，评审报告 P0，阶段一）**：后端代码评审报告 P0「核心领域依赖方向失控」闭合——CODE_STYLE V1.9 新增 §3.x「依赖方向红线」（planner/review → task → agent → system → shared 单向依赖 + 跨域禁直捅 Mapper + 端口反转合法化 + ObjectProvider 登记制）+ 防回归脚本 `verify-dependency-direction.ps1`（9 条断言，红色命中 exit 1）；system 域 6 文件全部迁出：Attachment*/Module*/ArtifactUpload* → task 域、Dashboard*/AdminDashboard* → task 域 observability 子包（顺带消除 agent.mapper 直捅）；AuthServiceImpl 摘除 agent 依赖（新建 `agent.port.AgentAuthPort` 由 AgentServiceImpl 实现，McpAuthFilter/AuthInterceptor/AuthController 改注入端口）；task→planner 孤点（TaskFinalReportServiceImpl→PlannerAgentPicker）用端口反转消除（task 域 `TaskPlannerPickerPort` 由 planner 域实现）。验证：core 全量 534/534 全绿 + 脚本 8/9 PASS（唯一 FAIL=agent→task.mapper 4 处，属阶段五「task?agent 事件解耦」目标，登记待办）+ `mvn -pl helloai-start -am -DskipTests package` 出包 + 重启后 `verify-admin-authz.ps1` PASS=17 FAIL=0 回归通过。迭代详情 §6.134。
- **2026-08-22 P1 事务缺失三方法收口（N23，评审报告 P1，阶段二）**：`getByIdForUpdate` 审计——主代码唯一调用方 `ExecutionCommandServiceImpl.createAssignedCommand` 已在 `@Transactional` 内，Javadoc 注明「必须在事务内调用」（自开事务会在返回时释放 FOR UPDATE 行锁，故方法本身不补事务注解）；`updateDependsOn` / `markManualIntervention` 接口方法补 `@Transactional(rollbackFor = Exception.class)`（与 changeStatus/block 同惯例，调用方跨类注入代理生效；markManualIntervention 内部 best-effort 吞异常语义保留）。验证：9 个相关测试类 **108/108 全绿**。迭代详情 §6.135。
- **2026-08-22 P1 巨型类拆分（N24，评审报告 P1，阶段三）**：三巨头越过 CODE_STYLE §7.8 类规模红线（>500 行）全数拆解，零行为变更——① `RequirementClarifyServiceImpl`（1342→675 行）四拆：`IntentDetectionService`（意图识别，95 行）/`ClarifyWebSearchOrchestrator`（联网搜索编排，235 行）/`ClarifyReplyParser`（回复解析，352 行）/`ConfirmCardProtocol`（确认卡协议，144 行）入 `planner.clarify` 子包；② `SubTaskReviewServiceImpl`（888→546 行）两拆：`ReviewEvidenceAssembler`（核验证据组装，352 行）/`VerdictParser`（裁决解析，127 行）入 `review.support` 子包；③ `AgentServiceImpl`（856→553 行）按职责聚类四组件：`AgentCredentialService`（API Key 键管）/`AgentSkillPolicyService`（技能推导）/`AgentLifecycleService`（状态机）/`AgentStatsService`（统计），主类删 4 族方法 + 12 参构造器改委托；测试 setUp 同步（@Mock AgentMapper + 4 组件）。每拆独立小闭环（编译 + 相关单测 + 依赖方向脚本）。验证：helloai-core 全量 **896/896 全绿**（AgentServiceTest 14 用例含新增组件用例）。迭代详情 §6.136。
- **2026-08-22 P2 规范收口 5 项（N25，评审报告 P2，阶段四）**：① `SysUserStatus` 枚举（`common.constant`）收口 `"ACTIVE"` 硬编码，3 处存量（AdminInitializer/AuthServiceImpl/SysUserServiceImpl）替换；② `TaskController.deleteById` 改 `POST /api/tasks/deleteById/{id}`（DELETE 带 body 兼容性差）+ 前端 `task.ts` 同步 `request.post`，6 个验证脚本同改；③ 前端路径常量收口：新建 `src/api/paths.ts`（16 组路径字典，带参路径箭头函数统一 `enc()=encodeURIComponent`），16 个 api 文件（task/agent/subTask/settings/clarify/auth/activity/attachment/dashboard/duty/inbox/module/prompt/review/reward/rule）全部替换为 `paths` 引用，内联路径字符串 grep 零残留 + vue-tsc 0 错；④ `AdminQualityController` 加配置门控（sys_config `admin.quality.enabled=true` 才开放 rebuild/dispatch/spec-section 三端点，默认关闭防越权误用），`verify-quality-profile.ps1`/`verify-contract-first.ps1` 插 A1.5 运行时开启步骤，`verify-admin-authz.ps1` 拦截器层不受影响；⑤ catch 吞噬二分审计：全量 107 个 catch 块——空 catch 13 处（core 12 + job 1）全数补理由注释（10 处 dbg best-effort 调试上报 + AgentCapability parseInt 容错 + AgentMcpServerServiceImpl DuplicateKey 并发幂等 + OutboxRelayTask 重试计数保守降级），非空 catch (Exception) 25 处抽查 16 处全合格（带 e 日志/rethrow/wrap 或明确 best-effort 注释），结论无无意吞噬。迭代详情 §6.137。
- **2026-08-22 阶段五 task↔agent 事件解耦（N26，评审报告 P1，阶段五）**：端口反转——task 域新建 `task.port.TaskDispatchPort`（含纯数据 `record DispatchConstraints`），agent 域 `ResilientDispatcher` 实现，`SubTaskDispatchServiceImpl` 注入端口替换具体类直依赖；agent 域对 `task.mapper` import 清零（6 处直捅全部收口：AgentStatsService 3 mapper / AgentServiceImpl 4 mapper / McpToolServiceImpl claimAtomic / AgentDutyLeaseServiceImpl / InFlightDbQuotaService → `SubTaskService` 接口净增 7 方法承接）；ObjectProvider 主代码 8 处减半——4 处 Optional 化（AgentChatClientServiceImpl / McpAuthFilterConfig / ExecutionDispatchValidator / OutboxRelayTask，存在性探测与 getIfAvailable 语义等价）+ 4 处登记保留理由（CompositeArtifactStorage / WebSearchServiceRouter 多候选 orderedStream 路由；SubTaskServiceImpl / ExecutorIssueResolutionAssessor 懒解析打破循环）；AgentStatsService 计数强制 (int) 防 Controller (Integer) 强转 500。验证：core 896/896 + job 66/66 全绿，verify-dependency-direction.ps1 9 项全 PASS（阶段一唯一 FAIL=agent→task.mapper 转绿）。迭代详情 §6.138。
- **2026-08-24 添加模型两步式弹窗 + 计费类型字段 + API Key 连通性验证（V59，§6.153，N9 增量）**：系统设置「添加供应商」按用户参考图重塑为两步交互——第一步 `ProviderPickerDialog.vue`「添加模型」双列供应商卡片网格（`providerCatalog.ts` 4 家内置：deepseek/moonshot/minimax/dashscope，品牌色字母徽标 + 标语 + 右箭头），第二步 `AddModelFormDialog.vue` 表单弹窗（供应商下拉含「已添加」标记与「自定义供应商」选项、类型下拉、API 密钥输入框带眼睛图标 + 绿色「获取 API 密钥」外链、底部添加/取消）；预置供应商「添加」语义 = 覆盖写 Key + `updateLlmProvider(billingType)`（已是 `llm_provider` builtin 行），选自定义才 `createLlmProvider`。**① 计费类型字段（V59 迁移）**：Flyway `V59__llm_provider_billing_type.sql` `llm_provider.billing_type VARCHAR(32) NOT NULL DEFAULT 'API_KEY'`（§9.4 幂等 + DO RAISE NOTICE 验证块）；`LlmProviderServiceImpl` create/update 校验——缺省回填 `API_KEY`，TOKEN_PLAN / CODING_PLAN 一律 BizException 拒绝（「计费类型暂仅支持按量付费」），前端 `BILLING_TYPE_OPTIONS` 两项「敬请期待」置灰；**② API Key 连通性验证（补充需求）**：`LlmProviderKeyVerifyService/Impl`（agent 域，接口+impl 成对 + 构造注入；依赖 `PlatformProviderConfigService` 故不落 system 域——依赖方向红线）——raw HTTP 最小探测（`max_tokens=1` + "ping"，不走 ChatClient 工厂、不污染 `ProviderChatModelCache`；连接 5s / 请求 20s），OPENAI_COMPATIBLE → `{baseUrl}/v1/chat/completions`（已含 /v1 不重拼）/ ANTHROPIC_COMPATIBLE → `/v1/messages` + `x-api-key` + `anthropic-version`；结果收敛 `{success, message, model, elapsedMs}` 绝不抛异常（401/403 → 「API Key 无效或无权限」+ error body message 提取）；`POST /api/admin/llm-providers/verifyApiKeyById/{id}`（§8.2 描述性驼峰）。博查同款：`WebSearchService` default `verifyApiKey()`（不支持的供应商收敛「不支持在线验证」）+ `BochaWebSearchServiceImpl` 覆写（query=ping / summary=false / count=1，HTTP 2xx + body `code==200` 双校验）+ `WebSearchServiceRouter` 委托 + `POST /api/admin/config/verifyWebSearchApiKey`；**③ Agent 编辑显示模型**：`AgentEditDialog.vue` 内部 LLM（`accessType=API_KEY_LLM` 或有 modelType）显示只读「模型」字段（空值显示「系统默认模型」）。前端 `Settings.vue` 接线：按钮改「添加模型」、详情区「验证 Key」按钮与「计费类型」描述项、保存 Provider Key / 博查 Key 后自动触发验证（持久 ElMessage loading + 结果 8s 展示）；两个验证端点均经 `paths.ts` 单一事实源（§19.0）。验证：`LlmProviderKeyVerifyServiceTest` 4 例守卫路径（不发请求即失败）+ `LlmProviderServiceTest` +3 例 billingType 校验，`mvn -pl helloai-core -am test` 全绿（-am 防本地仓库旧快照）；前端 `npm run build` 通过（lint 仅 1 个存量 error：QualityDashboard.vue no-extra-semi，非本轮）；冒烟脚本 `scripts/powershell/verify-api-key-verify.ps1` S0~S7（UTF-8 with BOM + 单引号拼接，待后端运行态实跑）；CODE_STYLE 复核：`verify-dependency-direction.ps1` 9/9 PASS + `verify-code-style-p1-ui-sync.ps1` 双通道 0 违规。迭代详情 §6.153。
- **2026-08-26 分布式健壮性改造（N27，清单 v1.2 阶段 0-5，迭代详情 §6.163）**：12 处手写 Redis 锁清零 + 11 个定时任务分布式锁 + MQ 容量治理 + 死信台账四件收口——**① 锁选型（CODE_STYLE V1.16 §3.y 新增 + §12 章节重写）**：review 域 12 处 `setIfAbsent` 手写锁全部迁移 Redisson RLock（`review:lock:{subTaskId}` 动态 key，`tryLock(0,120,SECONDS)` 显式 leaseTime 保持崩溃残留自动过期语义；`redisson-spring-boot-starter` 4.0.0 + `redisson-spring-data-34` 适配 Boot 3.4.10，helloai-core 直接声明）；11 个 job `@Scheduled` 任务迁 ShedLock `@SchedulerLock`（6.6.0，`shedlock-spring` + `shedlock-provider-redis-spring`，helloai-job 直接声明；`ShedLockConfig` 新建 start.config；name/lockAtMostFor 对照迁移表逐任务核对；`DutyLeaseExpirationTask` 原 Redis Lua 锁一并迁除）；豁免清单 5 个多实例并行无害类（scanReviewOrphans / ExecutionCommandPoller / DoorbellKeepaliveTask / SessionAuthCleaner / McpSessionAuthCleanupTask）写入 CODE_STYLE §3.y 供新增 @Scheduled 对照；版本由父 pom `<properties>` 集中管理（同 mybatis-plus 模式）；**② MQ 容量治理（§3.z）**：RabbitMQConfig 核心 4 队列 `x-max-length=50000` + notification 队列 `1000`、`x-overflow=reject-publish`（前置 RabbitMQ ≥ 3.10，docker-compose 3.12.14 满足，实机探测为部署门禁；低版本降级仅 x-max-length 并记录例外）、listener prefetch 250 → 10（3 个 MANUAL ACK 消费者全局生效）、ConfirmCallback NACK 日志补「可能是队列满」提示；**③ 死信台账（阶段 4）**：Flyway `V60__mq_dead_letter_archive.sql` 新表 + `DlxAlertConsumer`（先落台账再 ACK，dlxQueue 未挂 DLX 禁止 requeue，含 `DlxAlertConsumerTest` 3 例）。验证：helloai-job 63/63 + helloai-core 932/932 + compile 零错误；启动自检（ShedLock key 观察 + 队列重建）与 notification 积压核查列为实机部署门禁。清单与选型条款：`doc/design/分布式健壮性改造实施清单.md` v1.2。
- **2026-08-28 内部 LLM Agent 执行凭证平台级化（N28，迭代详情 §6.164）**：缺陷修复——系统管理（模型配置）更换 API Key 后，已注册的内部 LLM Agent 仍用旧 Key 执行报错，只能删除重建（连带丢失历史信息）。根因：注册时 `provisionPlatformCredential → bindPlatformApiKeyIfAbsent` 把平台级密钥复制成 AGENT 级快照存入 credential_vault，而执行链 `ApiKeyAgentExecutor` 读取的是 AGENT 级快照；系统管理轮换只动 PLATFORM 级凭证，旧 Key 残留。修复：① 新增 `AgentLlmCredentialResolver`（agent 域，平台级优先、Agent 级兜底），执行链 / 探活链 / 选人链统一委托（resolveApiKey / hasUsableCredential，hasUsableCredential 异常防御式降级）——执行密钥按 Agent.modelType 前缀关联 `llm_provider` 平台级凭证实时解析，轮换后实时生效；② 注册链不再落密钥快照（`bindPlatformApiKeyIfAbsent` 删除，`provisionPlatformCredential` 收敛为平台凭证核对语义），存量 AGENT 级快照不清理（平台级优先后自动失效），并兼容 `CredentialController.bindApiKeyByAgentId` 手工绑定自定义密钥链路。验证：AgentSelectorTest / PlannerAgentPickerTest / LlmProviderCatalogServiceTest / AgentLlmCredentialResolverTest / ApiKeyAgentExecutorTest / PlatformAgentExecutionServiceTest 共 89 例全绿 + 全模块 compile 零错误。
