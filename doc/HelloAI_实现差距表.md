# HelloAI 实现差距表

## 1. 文档定位

本文档用于回答：

- 文档原先承诺了什么
- 当前代码实际做到了什么
- 差距属于“应改文档”还是“应补功能”

本文档是当前版本对表、验收与排期的主参照文档。

---

## 2. 当前总体结论

- 已交付闭环：MCP SSE 接入、鉴权、核心工具链路、在线状态三件套、熔断降级、Reconcile 健康检查、AgentHub V3 门铃通知通道（PR-1/2/3 + 运行时 E2E 实测 ALL PASSED + PR-4 保活帧/双心跳；**2026-08-07 已搁置**——外部 Agent 无法处理平台推送，见 N13）、外部 Agent 一键接入全套化（checkIn/checkOut 纳入默认授权 + executor SKILL 升级为含 MCP 全套工具/打卡/门铃的完整说明书）
- 文档失真：历史路线图、技术方案与部分说明文档曾混入过时事实或实施日志
- 部分落地：执行命令主链、Provider 配置复用、工牌模式与 `credential_vault`
- 未落地：工作流模板、浏览器型 Agent 真实接入链路

---

## 3. 文档失真项（D）

| 编号 | 主题 | 当前状态 | 结论 |
|---|---|---|---|
| D1 | MCP 工具数量口径 | ✅ 已关闭 | README 已明确：工具数量不写死，以 `tools/list` 实际输出为准（2026-07-13） |
| D2 | 兼容通道定位 | ✅ 已关闭 | README 已明确：MCP SSE 是唯一主通道，REST `tools/list` / `tools/call` 属兼容保留（2026-07-13） |
| D3 | 路线图正文混入实施日志 | ✅ 已关闭 | 实施记录已迁移到《迭代执行记录》，路线图已退出事实源 |
| D4 | Spring AI 版本口径 | ✅ 已关闭 | 现行版本以代码、基线文档与回归结果为准 |
| D5 | `/api/tools/cli` 鉴权口径 | ✅ 已关闭 | 代码验证：`WebMvcConfig` 中 `/api/tools/cli` 已通过 `excludePathPatterns` 排除鉴权（公开下载入口，设计如此）。历史技术方案不再作为事实源（2026-07-13） |
| D6 | 心跳刷新规则口径 | ✅ 已关闭 | README 已明确：`last_seen_at`/在线态刷新以 `heartbeat` 为主；仅将 pull/ack 也计入活跃需代码配合再升级（2026-07-13） |
| D7 | README 文档边界 | ✅ 已关闭 | README 已收口为项目介绍与使用说明，不再承载阶段性执行结论 |
| D8 | PS 5.1 脚本 UTF-8 编码规范未沉淀到 skills | ✅ 已关闭 | 5 份 `helloai-preflight` SKILL.md（.agents/.qoder/.trae/.cursor/.claude）+ `AGENTS.md` 同步落地“脚本必须显式声明 UTF-8 编码”规则，覆盖运行时输出编码（[Console]::OutputEncoding / $OutputEncoding）、源文件 BOM（UTF-8 with BOM + `Parser.ParseFile` 自检）、管道原始字节传输（`cmd /c type` / `[Diagnostics.Process]`）、here-string 串入 U+FEFF 隐限（入口 `TrimStart([char]0xFEFF)`）四个子项，并于 2026-07-16 补第 5 子项：**双引号内 CJK 导致 PS 5.1 解析器提前闭合字符串（全角括号 `（）` 尤甚，抛 `Unexpected token '}'` / `字符串缺少终止符`）→ 修复范式统一为"单引号 + `+` 拼接，runtime 字面量纯 ASCII、中文只留注释"**（落地参考 `verify-execution-dispatch-guard.ps1`）。以后任何新增 verify-*.ps1 / start-*.ps1 / test-*.ps1 / hook / CI 脚本都应遵循（2026-07-16） |

---

## 4. 路线图项差距（N）

| 编号 | 主题 | 当前状态 | 差距定义 | 处理建议 |
|---|---|---|---|---|
| N1 | Outbox / 命令可靠投递底座 | ②a + ②b 已落地 | `AgentOutboxService`（SubTask 状态变更事件）已具备事务性 Outbox 能力；**Phase 2H ②a** 新增 `agent_command_outbox` 表 + `AgentCommandOutboxService`（5 个最小方法：createPending / listReadyForRelay / markSent / markFailed / markFinalFailed）+ `OutboxRelayTask`（helloai-job，Redis 锁 + 应用侧指数退避）+ `AgentCommandOutboxRelayProperties`（`enabled/interval-ms/batch-limit/max-retry/base-backoff-seconds`）。`ExecutionCommandService` 在 `dispatch-mode ∈ {MQ,BOTH}` 时改为同事务写 outbox，OutboxRelay 异步取行调 `ExecutionCommandMqPublisher` 真正投递；`aggregate_type` 固定 `EXECUTION_COMMAND` 防止后续统一 outbox 语义发散。**Phase 2H ②b（2026-07-15）扩展为可 CONFIRMED + 可对 NACK/超时回退重试：** Flyway V20 将 `status` 由 VARCHAR(32) 收敛为 SMALLINT（兼容 `PENDING/SENT/FAILED/CONFIRMED` ↔ `0/1/2/3`），新增 `last_sent_at` / `confirmed_at`，CHECK 约束扩展到 `0/1/2/3`，重建 PENDING 部分索引并新增 `idx_agent_command_outbox_sent_scan` SENT 部分索引；`AgentCommandOutboxStatus` 新增 `CONFIRMED(3)`，`AgentCommandOutboxService` 新增 `listExpiredSentForRetry / markSent(id,sentAt) / markConfirmed(id,confirmedAt) / markFailedFromSent / markFinalFailedFromSent`；`ExecutionCommandMqPublisher` 新增 `publishWithCorrelation(command, correlationKey)` 返回 `CorrelationData`，`OutboxRelayTask` 在 `publishWithCorrelation` 后异步注册 confirm 回调，区分 ACK/NACK/return/超时四种路径，命中 ACK 且无 return 时 `markConfirmed`，其它路径走 SENT→PENDING 指数退避回退或 SENT→FAILED 终态；`application.yml` 开 `publisher-confirm-type: correlated` + `publisher-returns: true`，`RabbitMQConfig` 注册 `ConfirmCallback`/`ReturnsCallback` 并 `setMandatory(true)`；`OutboxRelayTaskTest` 用例从 5 扩到 7（新增 Confirm NACK、SENT 超时）。本轮明确不做：Poller 降级、`OutboxCompensationTask` 新增调度（沿用 `OutboxRelayTask`）、DLQ、per-event 业务级熔断、旧 `ExecutionCommandMqPublisher.publish()` 静默丢失 confirm future 的清理（单独立项 R2） | 继续推 T4 RabbitMQ E2E 失败可恢复 → T5 Poller 降级；T6 §5.2 后置 |
| N2 | 可配置工作流模板 | 未落地 | 缺模板表、模板管理、模板化调度入口与 Team 编排 | 后续独立迭代 |
| N3 | MCP Server 工具集 | 已交付 | MCP SSE 主通道工具链已可用，已具备外部 Agent 最小执行闭环能力：`pullTasks/ack/claimSubTask/heartbeat/uploadArtifact/submitResult/reportBlocked/getAgentStatus`（其中 `submitResult` 对接统一回写入口，`reportBlocked` 记录阻塞原因证据链） | 保持现状 |
| N4 | 心跳与在线判定 | 已交付 | 在线态三件套、在线计算态与巡检收敛已具备 | 保持现状 |
| N5 | 熔断降级 | 已交付 | per-agent 熔断与同角色替补策略已具备 | 保持现状 |
| N6 | 执行命令消费与结果回写 | 已交付（含 T5 Poller 兜底 + Validator 启动期 fail-fast 闭环） | MQ 主链已连通：Phase 2D MQ Consumer 骨架 / Phase 2E 生产端接入（`ExecutionCommandMqPublisher` + `dispatch-mode` 四挡 + 双开关 + DispatchValidator producer-fail-fast）/ Phase 2F 两个阻断性修复（事务时机 `afterCommit` + 显式 JSON 序列化）/ Phase 2G 双路消费 E2E（DB CAS + Redis + `event_consumption_log` 三层幂等抵消）/ Phase 2H ②a Outbox 最小闭环（`agent_command_outbox` 同事务写入 + `OutboxRelayTask` 1000ms 扫描 + Redis 锁）/ Phase 2H ②b Confirm/Retry（`PENDING/SENT/CONFIRMED/FAILED` 四态 + `CorrelationData` + publisher confirms + Confirm-aware Retry + SENT 超时回退）/ T4 Outbox ②b E2E（S1 broker NACK / S2 mandatory return / S3 confirm timeout / S4 control happy path 四场景实测，N1 可标已交付证据）。**T5（2026-07-15）Poller 兜底重塑 + Validator 闭环：** `AgentExecutionProperties.ConsumerMode` 三种模式语义全部重塑为 "Poller 仅作孤儿/超时/补偿兜底"，主消费路径由 `MqExecutionCommandConsumer` 或本地事务事件承担；`ExecutionCommandPoller` 删除 `listAllPending` 分支，所有 consumer-mode 统一走 `listOrphanPending`，`trigger` 前缀恒为 `poll-recovery:`、timeline 事件恒为 `sub_task_execution_command_poll_recovery`；`ExecutionDispatchValidator` 新增 `consumer-mode ∈ {POLLER, BOTH}` + `consumer-enabled=false` 启动期 `IllegalStateException` fail-fast（阻止 "主消费路径全关但 Poller 仅兜底，PENDING 永远不被消费" 的事故形态），保留 `dispatch-mode ∈ {MQ, BOTH}` + producer 缺失 fail-fast 与 consumer=false WARN；`application.yml` `helloai.mq.execution-command.consumer-enabled` 默认值 `false → true` + 灰度节奏注释（MQ 环境就绪 → 主链稳定 → 可选退回 EVENT）；`AgentExecutionRecordService.listAllPending` 加 `@Deprecated` 兼容保留；`ExecutionCompensationTask` 保持独立（不并入 Poller，职责边界清晰）；`ExecutionCommandPollerTest` 删除 PollerMain 嵌套类（5 个 listAllPending 主路径用例）+ 新增 DowngradeConsistency 5 用例；`ExecutionDispatchValidatorTest` 新建 14 用例覆盖 DispatchModeFailFastOnProducer / DispatchModeFailFastOnRelay / ConsumerModeFailFast / DispatchWarnOnConsumerDisabled / ValidCombinationsAndPriority 五类场景；`scripts/powershell/verify-poller-e2e.ps1` 已升级到 v3.1，并在 **2026-07-16 最新实测 runTag=`20260716-174605` 下 S1-S5 = PASS 15 / FAIL 0**：pre-flight 增强为健康检查重试 + `listening=` 诊断；mock execution hard gate 前移到 prepare 之前；S2/S4/S5 改为独立 `ASSIGNED` sub_task 隔离以对齐 `startIfNeeded` 契约；S4 的 orphan age 调整为“超过 poller threshold 但不撞 30s timeout compensation”窗口，验证 `polled=3 / progressed=3 / poll_recovery distinct_sub_tasks=3`；S5 验证 `last_attempt_at`、`sub_task_execution_command_poll_recovery`、`poll-recovery:` trigger 与 `rogue_consume_events=0` 四项证据链均由 Poller 独自留下。结果回写入口收口为 `ExecutionResultHandler.handleReport(ExecutionResultReport)`，平台内执行链与外部 MCP `submitResult` 统一走该入口。`AsyncExecutionResultConsumer` 改造后置仍待后续独立轮次 | 保持现状；S5 最小等价验证已具备，且 **`scripts/powershell/verify-execution-dispatch-guard.ps1` 已实测 G1/G2/G3 = PASS 12 / FAIL 0**（G1/G2 fail-fast + exitCode 1 + 日志命中期望片段 + 6565 未 Listen，G3 `/api/health` 200）；S6 已从"手动 MQ-isolation 重启验证"重定义为独立的启动期 fail-fast 守卫脚本，不再塞进 Poller E2E——因为 T5 之后旧 S6 组合会被 `ExecutionDispatchValidator` `@PostConstruct` 直接拦截，属"被守卫拦截的非法部署形态"而非"能跑的验证"。**2026-08-03 依赖感知双轨上下文注入（§6.43）：** 执行链上下文升级为"直接前置的结构化摘要（`findRecord` 精确取单条）+ 完成内容本体（物化附件优先、`context.lastExecution.output` 回退）"双轨注入（`## 依赖产出参考（直接前置）` 章节，多前置按声明顺序 HashMap 全量收集防覆盖）；Phase A JSONB `appendExecutionRecord`/`initialize` 加 taskId 粒度分段锁（多前置并行回填不互覆，单实例安全/多实例需切 Phase B 或 Redis 锁）；Phase B `task_running_spec` 独立表行级天然无覆盖竞态；`buildExecutorPromptSection` 去掉全量 executionRecords 摘要段（仅 Baseline + ContextSummary）；`scripts/shell/verify-deps-context-e2e.sh` 真实环境 PASS——双前置并发完成后下游 prompt 同含两前置产出（`sub_task_spec_context_loaded` payload depCount=2/loadedCount=2/truncatedCount=0/degraded=false）。 |
| N7 | 健康检查改写 | 已交付 | Reconcile、离线重分配、兜底收敛已具备 | 保持现状 |
| N8 | 网页版 AI 浏览器接入 | 未落地 | 只有枚举与预留，没有真实接入模块 | 后续独立迭代 |
| N9 | Provider 配置与 ChatClient 复用 | 已交付 | Provider 配置入口已统一（`helloai.providers`）+ provider/model 解析已收口（`AgentProviderResolver`）+ `ProviderChatModelCache` 按 (provider, baseUrl, apiKey 指纹) 缓存 ChatModel 实例（避免每次 new DeepSeekChatModel）；`DeepSeekProviderChatClientFactory` 已接入缓存，SHA-256 指纹保证明文 API Key 不入 cache key。**2026-07-30 增量：** 修复 `AgentProviderProperties` 前缀绑定失效（`helloai.providers` 前缀 + `providers` 字段导致实际绑定路径为 `helloai.providers.providers.*`，yml 从未绑上；改为 `helloai` 前缀）；`ProviderConfig` 增加 `api-key`（配置即"已生效"）；新增 `LlmProviderCatalogService`（枚举已配置 provider 的可用性 = apiKeyConfigured && factorySupported）+ `GET /api/admin/agents/llm-providers` 目录接口 + 注册 API_KEY_LLM Agent 时平台密钥自动补绑 credential_vault（尽力而为、已有 ACTIVE 凭证不覆盖）；yml 预置 moonshot / minimax / dashscope 配置段（参考 springai 项目，缺 Factory 实现前目录标记不可用）。**2026-08-07 增量（平台配置动态化，§6.51）：** 4 个 provider 的 yml api-key 全部置空（含 spring.ai.deepseek），平台级 API Key 改由管理员在"系统设置"页动态配置/轮换（AES-GCM 加密写入 credential_vault PLATFORM 级，owner_id 固定 0，Flyway V45 放开 owner_type CHECK）；`PlatformProviderConfigService` 统一解析（API Key = vault ACTIVE > yml > null；baseUrl/默认模型 = sys_config > yml > Factory 内置默认），保存即 `ProviderChatModelCache.clear()` 实时生效；`LlmProviderCatalogService` 目录可用性改读配置服务；新增 `AdminProviderConfigController`（GET /api/admin/platform/providers/list + PUT /{provider}/api-key + PUT /{provider}/settings）+ 前端系统设置页"模型配置"区块；外网地址断层修复（`AgentBaseUrlResolver` 统一解析 sys_config["helloai.base-url"] > yml > 请求推导 > localhost，getMySkill/onboarding 共用）；排除 `DeepSeekChatAutoConfiguration` 修复 api-key 置空后的启动期 fail-fast；e2e 脚本 `scripts/powershell/verify-platform-config.ps1`（-ReadOnly 模式不写库；**2026-08-07 已在 local profile 完整写库实测 PASS 22 / FAIL 0**） | 保持现状；4 个 ProviderChatClientFactory 已全部存在且均为 §6.51 改造对象（DeepSeek 官方 SDK + Moonshot/DashScope OpenAI 兼容 + Minimax Anthropic 兼容） |
| N10 | 工牌模式 + `credential_vault` | 部分落地 | 最小模型、绑定与托管语义已具备；Agent API Key 的最小轮换语义已落地（`ACTIVE / EXPIRED` + 审计备注），但迁移、过渡期双活策略与权限颗粒度仍未收口。**2026-08-07 增量：** `owner_type` 放开 PLATFORM 级（Flyway V45），平台级凭证按 provider 唯一、owner_id 固定占位 0，支撑"先启动后配置"（§6.51） | 继续补功能 |
| N11 | 调度策略：外部优先 + 空闲优先 + LLM 保底 | 已交付 | 候选选择策略收口为可配置项（`preferExternal` / `requireIdle` / `forceAccessType` / `autoAssignOnCreate`），并已落地“外部 Agent 连续失败阈值后自动回退到平台内 API_KEY_LLM”闭环：V17 补 agent.consecutive_failure_count/last_failure_at/last_fallback_at + sub_task.external_fallback_count；`ExternalAgentFailureTracker` 在 `ExecutionResultHandler.handleReport` / `ExecutionCompensationTask` / `AgentHealthCheckTask` 三处统一累加与重置；`SubTaskDispatchService.redispatchForFallback` 绕过 `AgentSelector`（不被 preferExternal 影响）直接选同角色 API_KEY_LLM Agent；`ExternalAgentFallbackTask`（helloai-job，60s 周期 + Redis 锁）扫描超阈值 CLI_CLIENT Agent 触发重新分发；阈值与冷却期可由 `helloai.dispatch.fallback.{failure-threshold,cooldown-minutes}` 调节 | 保持现状 |
| N13 | AgentHub V3：长连接门铃通知通道（外部 Agent 秒级唤醒） | **已搁置（2026-08-07）**：PR-1~PR-4 均已交付且 E2E 实测通过；技术瓶颈——外部 AI Agent（安装版 / CLI 版）均为单向执行器，无法处理平台推送的门铃信号且 Agent 端代码不可修改，通道无人消费故搁置；代码保留运行，SKILL/脚本已下线门铃内容 | 依 `doc/archive/HelloAI_门铃通知通道设计.md` 补一条“服务端 → 外部 Agent 单向 SSE 门铃”，把外部 `CLI_CLIENT` 感知时延从 0~30s 轮询级降到秒级；MCP 仍是唯一任务协议层，门铃丢失无损回退轮询。**PR-1 门铃内核（2026-07-17）：** `DoorbellProperties`（helloai-common，`helloai.doorbell.{enabled=true,emitter-timeout-ms=1_800_000,keepalive-interval-ms=15_000}`）+ `DoorbellSignal`（`type/eventType/refType/refId/serverTime` + 静态工厂 `connected()/keepalive()/inbox(...)`，`@JsonInclude(NON_NULL)`）+ `DoorbellRegistry`（进程内 `ConcurrentHashMap<Long,SseEmitter>`，register 关旧建新 / unregister 用 `remove(k,v)` 值条件删防误删新连 / get / isConnected / size）+ `DoorbellService`（`connect(agentId)` 校验 enabled、建 emitter 挂 onCompletion/onTimeout/onError 清理回调、register 后立即回推 `connected` 握手；`ring(agentId,signal)` 未连返回 false 尽力而为；`disconnect` / `connectionCount`；`doSend` 失败静默注销）+ `AgentDoorbellController`（helloai-api，`GET /api/agents/doorbell/sse`，`produces=text/event-stream`，`@RequestAttribute("_authId") Long agentId` 复用 AuthInterceptor `/api/**` Bearer 鉴权链）。**PR-2 响铃接线（2026-07-17）：** 一处收口——`AgentInboxService.send()`（三条通知路径 TaskController 直发 / SubTaskService 状态流转 / MQ NotificationConsumer 全经此方法）`save(inbox)` 成功后 `publishEvent(new InboxMessageCreatedEvent(agentId,eventId,eventType,refType,refId))`，`DuplicateKeyException`（`(event_id,agent_id)` 联合唯一）幂等分支 return 不发事件避免重复响铃；新增 `InboxMessageCreatedEvent`（helloai-core/event）+ `DoorbellRinger`（`@Async("doorbellExecutor") @TransactionalEventListener(phase=AFTER_COMMIT)` 调 `doorbellService.ring(...)`，异常静默靠轮询兜底，与 Outbox / 本地执行事件 AFTER_COMMIT 时序哲学一致，保证“先落库后响铃”）+ `DoorbellExecutorConfig`（helloai-start，`doorbellExecutor` 2/4/500 + `DiscardPolicy`，与 `executionCommandExecutor` 池隔离，响铃永不拖累主链路）。不新增 Flyway / 表 / MQ 队列 / WebSocket 依赖（门铃是纯运行时连接态，`SseEmitter` 为 WebMVC 原生零新增依赖）。单测 17 例全绿：`DoorbellRegistryTest`(5) / `DoorbellServiceTest`(6) / `DoorbellRingerTest`(4) / `AgentInboxServiceTest`(2，`spy(new AgentInboxService(publisher))` 桩 `save` 验证发/不发事件)，全 reactor MAIN `install` BUILD SUCCESS。**PR-3 值班鉴权收口（2026-07-17）：** `DoorbellService` 构造注入 `AgentDutyLeaseService`，`connect(agentId)` 在 enabled 校验后前置 `isOnDuty(agentId)` 闸门（无 ACTIVE 值班租约抛 `BizException(500)` 拒连，先打卡再接电话，设计 §6.1）；反向断连用事件解耦规避 `DoorbellService ↔ AgentDutyLeaseService` 构造循环——新增 `DutyLeaseClosedEvent`（helloai-core/event），`AgentDutyLeaseService` 加 `@RequiredArgsConstructor` + `ApplicationEventPublisher`，`closeLease`（覆盖 checkOut）与 `expireLeases`（覆盖租约到期）在关闭行数 >0 时发事件，`startLease` 防御性关旧不发（避免刚 checkIn 即断连）；`DoorbellDutyListener`（`@Async("doorbellExecutor") @TransactionalEventListener(AFTER_COMMIT)`，与 `DoorbellRinger` 对称）监听后 `disconnect(agentId)`，异常静默。端到端脚本 `scripts/powershell/verify-doorbell-e2e.ps1`（S1 未在岗建连→HTTP 500 code=500 / S2 在岗建连→HTTP 200 + `event:connected` 握手帧 / S3 租约到期→`DutyLeaseExpirationTask` 翻 EXPIRED→事件驱动主动断门铃、SSE 后台 job 结束），遵循规则 6 UTF-8 with BOM + 单引号拼接 + runtime 纯 ASCII，`Parser.ParseFile` 自检 PARSE-OK。单测 22 例全绿（新增 `DoorbellDutyListenerTest`(4) + `DoorbellServiceTest` 补未在岗拒连用例至 7），全 reactor MAIN `install` BUILD SUCCESS。**PR-4 保活帧调度 + 双心跳（2026-07-17）：** `DoorbellKeepaliveTask`（helloai-core，`@Scheduled(fixedRateString=helloai.doorbell.keepalive-interval-ms 默认15s)`）本地无锁遍历 `DoorbellRegistry` 对所有活跃连接广播 `keepalive` 帧穿透反代空闲超时——**与 `DutyLeaseExpirationTask` 的 Redis 选主锁相反**：SSE 连接是进程内状态，每实例必须保活自己 Registry 里的连接，选主只让一台跑会导致其他实例连接被反代掉断；新增 `DoorbellRegistry.forEach(BiConsumer)`（弱一致遍历）+ `DoorbellService.broadcastKeepalive()`（坏连接由 `doSend` 静默注销，返回送达数）。双心跳：新增开关 `helloai.doorbell.refresh-heartbeat`（默认 false，保守——SSE 单向 TCP 存活≠Agent 进程健康，避免僵尸连接掩盖真实离线），开启后仅 `connect()` 建连顺带调一次 `HeartbeatService.seen(agentId)` 刷 `last_seen_at`（keepalive 轮不刷），失败静默不阻断建连；`DoorbellService` 构造注入 `HeartbeatService`（→AgentMapper/Redis 不回指，无循环依赖）。单测 33 例全绿（新增 `DoorbellKeepaliveTest`(4) + `DoorbellRegistryTest` 5→7 + `DoorbellServiceTest` 7→12），全 reactor MAIN `install` BUILD SUCCESS | **2026-08-07 搁置**：任务感知一律改走 `pullTasks` 轮询，Redis Pub/Sub fanout 演进项不再推进（详见迭代记录 §6.50）。历史证据保留：**PR-3 已交付（2026-07-17）**：`isOnDuty` 建连闸门 + `checkOut`/租约到期事件驱动主动断连 + `verify-doorbell-e2e.ps1` 三场景验证脚本，单测 22/22 全绿。剩：运行时端到端冒烟已于 2026-07-17 真实环境实测 **ALL PASSED**（agentId 2077974111691915266：S1 未在岗建连 HTTP 500 code=500 / S2 在岗建连 HTTP 200 + `event:connected` 握手帧 / S3 租约到期翻 `EXPIRED,lease_expired` + 事件驱动主动断连、SSE 后台 job Completed）；仅余多实例 Redis Pub/Sub fanout（设计 §12 演进项，单实例部署下用不到）。**PR-4 已交付（2026-07-17）**：保活帧调度（每实例本地无锁广播 keepalive）+ 双心跳（`refresh-heartbeat` 默认关，建连刷 `last_seen_at`），单测 33/33 全绿 |
| N12 | AgentHub V1 P0：值班租约闭环 + 值班优先调度 | 已交付 P0 + P1 + R2/R3 验证回执 + A2 第 1 段（STRICT 独占报锁） | 2026-07-16 落地 AgentHub V1 P0 三件（checkIn / checkOut / DutyLeaseExpirationTask）真实环境 E2E：`agent_duty_lease` 表（`V1__init_all.sql` 第 1508 行，AgentHub V1 T3 建表；**注：早期文档误记为 V18，V18 实为 `event_consumption_log`**）承载 Agent 值班租约，状态机 `ACTIVE / CLOSED / EXPIRED`（`uk_duty_lease_agent_active` partial unique index 防止同一 Agent 多条 ACTIVE 行）；`AgentDutyLeaseService.checkIn` 赋予 Agent 值班权（workMode=NORMAL/STRICT/maxConcurrent/ttlMinutes）+ heartbeat 联动；`checkOut` 主动闭锁并记录 closeReason（服务端参数名 `closeReason` 为准，旧字段名 `reason` 兼容）；`DutyLeaseExpirationTask`（helloai-job，`@Scheduled fixedRate=30_000` + Redis Lua 锁）扫描过期 ACTIVE 租约翻为 `EXPIRED`，close_reason=`lease_expired`；`AgentSelector.pickAlternative` 增加 `dutyRank` 排序（值班中的 Agent 优先于未值班），多候选用例排序时调用 `agentDutyLeaseService.isOnDuty(agentId)`，`lenient()` mock 避免单候选用例 UnnecessaryStubbing。`MyBatisPlusMetaObjectHandler.insertFill` 补 `consecutiveFailureCount=0` 默认填充（v2.4 N11 字段遗漏修复）。`McpMcpServer` 补 `checkIn` / `checkOut` 工具（`@Tool` + `@ToolParam`），认证上下文 `requireAuthId(sessionId,_sessionId)` 覆盖客户端传的 agentId。`agent_mcp_server` 表 V21 seed 新建 Agent 自动启用 `checkIn/checkOut`（partial unique index `idx_ams_agent_tool WHERE deleted=0`，ON CONFLICT 子句必须显式带 partial 条件）。E2E 脚本：`verify-agenthub-duty-e2e.ps1`（S1 checkIn / S2 checkOut / S3 Lease 过期扫描 3 场景实测通过 ALL PASSED）。skill 规则 6 + D8 同步落地 PS 5.1 脚本编码防护四原则。**P1 值班只读报表接口已交付**（`AgentDutyLeaseController`：`GET /api/admin/duty-leases` 分页列表 + `GET /api/admin/duty-leases/overview` 状态概览，`DutyLeaseResponse`/`DutyOverviewResponse` DTO，`AgentDutyLeaseService.listLeases/countByStatus` 只读查询，列表 agentName 用 nameCache 避免 N+1）。**P1 dashboard 前端接入 + R2/R3 收尾已落地（2026-07-16）**：`helloai-ui/src/api/duty.ts` + `types/duty.ts` + `views/duty/DutyLeaseList.vue`（分页表 + 状态/Agent ID 过滤）；`Dashboard.vue` 加 “Agent 值班概览”区块（4 个 stat 卡 + “查看全部租约 →” 链接，失败仅 console.warn 不阻断主图）；`router/index.ts` 注册 `/duty-leases`、`MainLayout.vue` 增加 `Clock` 图标菜单项；`ExecutionCommandMqPublisher.publish(ExecutionCommand)` 旧入口删除（无调用方、消除第二套时序假设），单测同步删除 AFTER_COMMIT 用例、改走 `publishWithCorrelation`；V22 `agent_command_outbox_backfill_timestamps` 回填 status=1/3 历史行 `last_sent_at`/`confirmed_at`（用 `update_time` 保守近似，IS NULL 守卫保证幂等）。验证脚本 `scripts/powershell/verify-dashboard-duty-leases.ps1`（S1 overview 字段齐 / S2 list 分页结构 / S3 status=ACTIVE 过滤生效 / S4 V22 backfill 抽查）。**A2 第 1 段 STRICT 独占报锁已交付（2026-07-17）**：`WorkMode` 枚举（`AUTO/STRICT` + `lenientParse`/`strictParse` 双策略，DB 读取宽容/MCP 入参严格校验非法值→BizException 拒绝）；`AgentSelector.pickFromCandidates` 加 `.filter(a -> !isOnStrictDuty(a.getId()))`（STRICT Agent 退出别人失败后的 pickAlternative 替补池抢派）+ `isOnStrictDuty` 私有方法用 `getActiveLease` + `lenientParse` 判定；`McpToolService.checkIn` 用 `strictParse` 严验入参、落库用 `mode.name()` 字符串；`AgentSelectorTest` 新增 5 个 STRICT 用例（19/19 全绿）覆盖跳过 STRICT 在岗 / 全部 STRICT 时返空 / 无租约当 AUTO / 脏值宽容 / 异常回退 AUTO；`verify-agenthub-duty-e2e.ps1` 追加 S6.1/S6.2/S6.3 三子场景（**真实环境实测 ALL PASSED**，S6.1 workMode=STRICT→DB work_mode=STRICT、S6.2 workMode=strict 小写→DB work_mode=STRICT 大小写不敏感、S6.3 workMode=BOGUS_VALUE→BizException 拒绝、lease count 不增）。本轮明确不做：A2 第 2 段（动态 TTL 自适应）、A2 第 3 段（concurrency 预扣）、调度重新计入值班权重以外的二次排序 | **已交付 P0 + P1 + R2/R3 + A2 第 1 段（STRICT 独占报锁）**；剩余 **A2 第 2 段（动态 TTL 自适应）** 与 **A2 第 3 段（concurrency 预扣）** 待续；E2E 脚本 `verify-agenthub-duty-e2e.ps1` 已可重复回归 |
| N14 | 真实 AI Agent 子任务自主闭环 | 脚本化已测 / 真实 AI 待测 | 2026-07-17 脚本化五步闭环（verify-onboarding-submit.ps1 PASS=12）证明 API 链路通；但“真实外部 AI 自主理解 SKILL.md、按规则 claim/执行/submit”未实测——脚本按固定顺序调 API，不验证 AI 的规则理解。当前状态（2026-07-18）：qoder/trae 注册、MCP 初始化、checkIn、超时重连已实测通过；子任务闭环联调未开始 | 按《项目进度》M5 场景矩阵执行，场景 1~3 通过后改标“已交付” |
| N15 | Controller 分层红线收口（§6.3 + 3.x 包归位） | 已交付（2026-07-20） | 按 `doc/HelloAI_CODE_STYLE.md §6.3` 第 1 条「禁止注入 Mapper」与第 2 条「禁止书写 SQL/QueryWrapper 条件」强制收口 6 个历史违规 Controller（ActivityController / AdminDashboardController / AgentDutyLeaseController / AttachmentController / DashboardController / FeedController）：删除 Mapper 注入、改为构造器注入 Service、查询与 QueryWrapper 全部下移至对应 Service 新增方法（ActivityLogService / AdminDashboardService / AgentDutyLeaseService / AttachmentService / DashboardService / FeedService）；`AdminDashboardService` 与 `DashboardService` 不继承 ServiceImpl（跨 Mapper 聚合，返回 Map 避开 core→api DTO 依赖，§6.7 聚合看板语义）。同步完成两项包归位（git mv 保留历史）：`com.helloai.config` 2 个类（MyBatisPlusMetaObjectHandler / AdminInitializer）并入 `com.helloai.start.config`；`helloai-start/.../chat/DeepSeekProviderChatClientFactory` 移至 `helloai-core/.../core/agent/chat/provider`，`helloai-core/pom.xml` 补 `spring-ai-starter-model-deepseek` 依赖。CODE_STYLE.md §6.3 待收口清单与 3.x start 配置类待收口段落同步收口。`mvn -DskipTests clean compile` 7 模块全 SUCCESS；迭代记录详见 `doc/log/HelloAI_迭代执行记录.md` §2026-07 Controller 分层红线收口 | 保持现状 |
| N16 | Planner 平台内自动拆解（需求→草案→确认→既有分发链） | 已交付（2026-07-28） | V26 落地“需求 → LLM 自动拆解 → 用户确认/拒绝 → 进入既有分发链”闭环：`SubTaskStatus` 新增 `PENDING_PLAN_REVIEW` 草案态（不参与 claim/分发/超时回收/统计，状态机仅允许 → PENDING/CANCELLED 且任何状态不可转入）+ `TaskStatus` 新增 `PLANNING`（防重复触发）+ Flyway `V26__planner_plan_review_status.sql` 重建两张表 CHECK 约束；`PlannerAnalysisService`（core/planner 包，编排收口 core 对齐 §6.3）：decompose（校验 PENDING + 已有非 CANCELLED 子任务拒绝 + CAS lambdaUpdate 推 PLANNING 防并发 + `AgentSelector.pickPreferred(PLANNER)` 选 API_KEY_LLM Agent + `prompts/planner-decompose.md` 模板渲染 + `PlatformAgentExecutionService.executeSync` 调 LLM + strip markdown fence 容错解析 JSON 数组（title 必填/数量≤10/priority 归一化）+ saveBatch 草案落库 + timeline `task_plan_generated`；失败路径 CAS 回退 PENDING + `task_plan_failed`）/ listDrafts / confirmPlan（草案逐条 changeStatus → PENDING，Task → IN_PROGRESS，按 `autoAssignOnCreate` 逐条 `dispatchPendingSubTaskAuto`，与手工创建子任务分发路径同构）/ rejectPlan（草案翻 CANCELLED 保留审计，Task 回退 PENDING 可重新拆解）；`TaskController` 四个薄入口 `POST/GET /{id}/plan` + `/plan/confirm` + `/plan/reject`；旁路排查确认全链路安全（job 定时任务/redispatch/XML mapper/dashboard 统计均精确匹配既有状态，草案态天然隔离）；外部 Planner `skills/planner/SKILL.md` 升级至 executor 版同等水准（MCP 全套 10 工具 + 门铃 + 常驻打卡 + 拆分四要素 + 六步排障闭环）；单测 `PlannerAnalysisServiceTest`(13) + `SubTaskStateMachineTest` 补 V26 用例(3)，helloai-core 244 全绿；端到端脚本 `scripts/powershell/verify-planner-decompose.ps1`（拆解/确认/拒绝/重复拆解拒绝四路径，待真实环境实测）。本轮明确不做：前端规划确认页、planner 专用 MCP 工具（decomposePlan）、子任务依赖 DAG；不改 `SubTaskAutoExecutionDispatcher` accessType 过滤 | 保持现状；~~前端规划确认页后续独立迭代~~ → 2026-07-29 已交付（拆解链前端补全，见 §6.18）；~~端到端脚本与浏览器闭环实测待真实环境回归~~ → 2026-07-29 真实环境已收口（zsh 版 `verify-planner-decompose.sh` e2e 12 步全绿 + 浏览器 UI 闭环实测通过，见 §6.18）。**2026-07-30 增量**：`PlannerAnalysisService.orderByDependency`（稳定 Kahn 入度拓扑排序）使草案审阅/分发按依赖正序展示（根节点在前，dependsOn 恒指向更靠前行），见 §6.27 | 保持现状 |
| N17 | Planner 对话式需求澄清（模糊想法→多轮追问→终稿→建任务→顺路拆解） | 已交付（2026-07-29） | V29 落地"第二步立项"入口：新表 `requirement_conversation`（title/status ACTIVE/FINALIZED/ABANDONED + CHECK、task_id **软引用无 FK**（不加入 deleteTaskCascade 引用面，删任务允许悬挂）、final_title/final_description 终稿暂存、round_count）+ `requirement_message`（conversation_id/role user·assistant/content/seq），照 V19 范例全审计列 + 触发器 + 逐列 COMMENT；`RequirementClarifyService`（core/planner，**完整复用 PlannerAnalysisService 五段式**：pickPlannerAgent 12 行刻意复制不抽象 + classpath 模板 `prompts/requirement-clarify.md`（占位符 `{{CONVERSATION_HISTORY}}` transcript 文本，协议严格 JSON 单对象 `{"type":"question"}` / `{"type":"final","title","description"}`）+ executeSync（context 带 conversationId + scene=requirement_clarify）+ `stripToJsonObject`（照 stripToJsonArray 改花括号版）+ BizException；类不加事务，LLM 耗时不占 DB 事务）：create/sendMessage（轮数硬上限 20，超限引导手动建任务；LLM 失败 user 消息保留可重发）/finalize（校验终稿非空 → 建 Task PENDING + best-effort 通知 PLANNER 写收件箱（照 TaskController.create 通知段搬 core）→ 会话回填 taskId+FINALIZED → timeline `task_created_from_clarify`）/abandon/listConversations（LIMIT 50）/detail；`RequirementConversationController` 六薄端点 `/api/requirement-conversations`；@MapperScan 补 `com.helloai.core.planner.mapper`；前端独立页 `/requirement-chat`（会话列表 + 气泡流 + 终稿卡片「创建任务并自动拆解」→ finalize → 前端调既有 `taskApi.plan` → 跳 `/tasks?review={taskId}` 自动开草案审阅弹窗；FINALIZED 只读 + 查看任务链接）+ 菜单/路由/TaskList review query 接线；单测 `RequirementClarifyServiceTest`(13) 全绿（question/final 双路径、fence 容错、轮数上限、finalize 语义）；zsh 脚本 `scripts/shell/verify-requirement-clarify.sh` 真实环境 10 步全绿（创建→推进终稿→finalize 建任务→FINALIZED 拒发→plan 草案生成→abandon 回归）；浏览器闭环实测 8 步全过 console 0 error。本轮明确不做：SSE 流式（Doorbell SSE 是 Agent 侧信号通道不可蹭）、终稿手动编辑（不满意继续对话让 LLM 修正）、会话删除（仅 abandon）、列表分页 | 保持现状；演进项：SSE 流式输出、终稿人工微调、会话分页。**2026-07-30 增量**：`RequirementClarifyService.regenerate` 补"会话已 FINALIZED 且原任务已被删除"的悬挂恢复路径（复用终稿重建 PENDING Task，不重跑 LLM，抽出 `buildTaskFromDraft` 统一建任务逻辑），见 §6.27。**2026-07-31 增量（V33 结构化选项式澄清）**：追问从纯文本升级为双模协议（`mode: structured / freeform`）——LLM 可输出结构化问题卡（每轮 ≤2 问、每问 2~4 选项、单选/多选/自定义补充、recommended 推荐标记）+ `progress` 0~100 完成度自评（仅前端进度条展示，无业务分支）；Flyway V33 `requirement_message` 加 `payload` TEXT 列一列两用（assistant 行存问题 JSON / user 行存 `selections` 选择快照 / 纯文本 NULL，与 V32 同款 TEXT 约定）；解析降级为一等公民（非 JSON 且不含 `"type"` → 原文作 freeform 追问落库不报错；structured 校验失败 → 降级 freeform 丢弃 questions）；content/payload 职责分离（content 由引导语+问题+选项合成可读文本保 LLM transcript 完整）；prompt 升级双模协议 + 五维度自检清单（业务场景/功能范围/性能并发/安全合规/交付预算）；前端 `StructuredQuestionCard.vue` 选项卡片 + 顶部进度条 + 选项提交走 `selectedOptions`。单测 21 例全绿（+4：structured 落库/校验降级/非 JSON 降级/选项快照）+ vue-tsc 0 错 + `verify-requirement-clarify-structured.ps1` 真实环境实测 PASSED（structured 追问 progress=25 两问结构完整 → 选项提交 → selections 快照落库 → abandon）。见 §6.32。**2026-08-03 增量（V39 Planner 对话双模式）**：单会话 `mode` 字段（CHAT 自由对话 / CLARIFY 方案澄清，NULL 老数据=CLARIFY 兼容），用户主导双向切换（to-clarify 置位落库 + 澄清模板基于全量历史重跑一轮；to-chat 仅置位不调 LLM），CHAT 走通用助手模板 `prompts/requirement-chat.md` 纯文本问答（无 JSON 协议、无 progress、无首轮联网搜索、独立 50 轮上限），意图词自动切换（「整理成方案/生成方案/落地实施」等命中即先落库切 CLARIFY、该条消息即澄清首轮、意图词永远放行保证转方案出口）；新会话缺省 CHAT，创建接口 `initialMode` 可快捷直达 CLARIFY；Flyway V39 `mode VARCHAR(16)` + CHECK 约束 + DO 验证块；`RequirementClarifyService` create 四参重载/sendMessage 上限按模式分派/doRound 意图置位/runLlmRound 按模式选模板与 scene/switchToClarify/switchToChat；`ClarifyMessageRequest.initialMode` + Controller 三端点；前端模式徽标 + 进度条与终稿卡条件化 + 「转为方案」按钮 + 新会话模式单选。单测 35/35 全绿（+14 ChatModeAndSwitch）+ vue-tsc 0 错 + `verify-planner-chat-dual-mode.sh` 真实环境 8 步 PASS（CHAT 纯文本 payload=NULL → 二轮仍 CHAT → to-clarify 产终稿 → finalize 建任务 → 意图词自动切 CLARIFY → to-chat 反向仅置位）。见 §6.44。**2026-08-03 增量（V40 意图词二次确认）**：去掉「转为方案」按钮，转方案收敛为「意图词触发 + 对话内二次确认」——CHAT 命中意图词（整理成方案/生成方案/落地实施等）不再自动切 CLARIFY，置 `pending_clarify_confirm` 标记并回复固定确认询问（不调 LLM、不加轮数）；用户回复确认词（确认/好的/开始吧等）或再次表达意图 → 转入 CLARIFY（该条消息即澄清首轮）并清标记；回复其他内容 → 清标记继续自由对话。Flyway V40 `pending_clarify_confirm SMALLINT NOT NULL DEFAULT 0`（按规范 §9.3 不用 BOOLEAN）+ DO 验证块；自定义 `SmallIntBooleanTypeHandler`（SMALLINT↔Boolean，@TableField 显式指定不注册全局映射）；`RequirementClarifyService` doRound 三段状态机 + sendMessage 上限分派放行确认词/意图词 + switchToClarify/switchToChat 防御性清标记；切换端点按规范 §8.2 整改 `POST /{id}/to-clarify`→`/toClarifyById/{id}`、`/{id}/to-chat`→`/toChatById/{id}`；前端移除「转为方案」按钮与 handleToClarify、clarify.ts 路径同步。单测 ChatModeAndSwitch 14→19 例全绿（2 例改待确认语义 + 1 例增强 + 5 例新增确认流）+ 修复既有 `PlannerAnalysisServiceTest.shouldDecomposeAndPersistDrafts` pre-existing Error（补 list(Wrapper) stub）+ helloai-core 全量 389/389 全绿 + vue-tsc 0 错 + `verify-planner-chat-dual-mode.sh` 改造后 8 步 PASS（STEP7 意图词→待确认→回复确认→CLARIFY 全流程断言）。见 §6.45。**V40.1 同日追加**：意图词正则扩展口语化话术（整理方案/出个方案/写方案/做个方案/做方案/方案整理，覆盖"帮我整理方案吧"）+ 澄清模板首轮强制 structured（进入 CLARIFY 后第一轮追问必有推荐选项卡片；用户实测"没看到推荐选项"根因即意图词未命中导致仍在 CHAT 纯文本模式）。单测 +1（colloquialIntentPhraseEntersPendingConfirm）全量 390/390 + E2E STEP7 意图词改口语化重跑 8 步 PASS。见 §6.45。**口嗨切换治理**：`requirement-chat.md` 第 4 条改为"系统自动处理切换、LLM 不得提及/预告/扮演方案整理模式"，消除 CHAT 模式 LLM 凭空宣称"切换到方案整理模式"的误导（实测数据库该会话全程 mode=CHAT，推荐卡片缺失根因即会话从未真正进入 CLARIFY）。见 §6.46。**2026-08-04 增量（V40.2 /planner 命令 + CHAT 追问卡片）**：意图词正则对口语话术仍敏感（「帮我整理成技术方案文档吧」不命中），新增斜杠命令 `/planner`（大小写不敏感，可带多行附加文本）为显式转方案入口——命令前缀不落消息、附加文本先落库 user 消息进 LLM 上下文再切 CLARIFY（`switchToClarify(Long,String)` 重载 + Controller `toClarifyById` body 改 `@RequestBody(required=false)` 兼容无 body）；CHAT 轮容错双模（`tryParseChatStructured` 宽松解析：仅 structured question 落 payload 出推荐卡片，其余一律纯文本降级零破坏），`requirement-chat.md` 新增「输出形态」节引导 LLM 追问时出 structured JSON（每轮 ≤2 题、每题 2~4 选项、recommended 每题至多 1 个）；前端 `PLANNER_COMMAND_RE` 命令识别 + `handlePlannerCommand`（新会话 initialMode=CLARIFY 直达 / 已有会话 toClarify 带附加文本）+ `activeStructured` 放开 CHAT 模式交互卡。单测 ChatModeAndSwitch 19→23 例全绿 + helloai-core 全量 394/394 + vue-tsc 0 错 + E2E 改造后全流程 PASS（STEP4.1 CHAT 轮宽松断言 / STEP5 toClarifyById 带附加文本断言 +2 消息）。见 §6.46 |
---

## 5. 当前建议优先级

1. ~~先继续关闭文档失真项：D1、D2、D5、D6~~ ✅ 全部已关闭（2026-07-13）
2. ~~D7、D8 已关闭（2026-07-13 / 2026-07-16）~~
3. 再继续夯实执行链：N1 ②a + ②b 已落地（最小闭环 + Confirm/Retry）→ T4 RabbitMQ 失败可恢复 E2E ✅ → T5 Poller 降级 + Validator 启动期 fail-fast 闭环 ✅；N6 标已交付（含 **`scripts/powershell/verify-poller-e2e.ps1` 最新 PASS 15 / FAIL 0** + **`scripts/powershell/verify-execution-dispatch-guard.ps1` PASS 12 / FAIL 0**）
4. AgentHub V1 P0 已交付（2026-07-16）：N12 值班租约闭环 + 值班优先调度；E2E 脚本 `verify-agenthub-duty-e2e.ps1` 可重复回归
5. **N12 P1 剩余三项（STRICT 独占报锁 / concurrency 预扣 / 动态 TTL）**：已拍板 A2（从轻到重三项顺序分 3 段 atomic round），**A2 第 1 段 STRICT 独占报锁 已交付（2026-07-17）**——`WorkMode` 枚举（`AUTO/STRICT` + `lenientParse`/`strictParse` 双策略）+ `AgentSelector.pickFromCandidates` 加 `.filter(a -> !isOnStrictDuty(a.getId()))` 过滤（STRICT Agent 退出别人失败后的 pickAlternative 替补池）+ `McpToolService.checkIn` 用 `strictParse` 严验入参（非法值 BizException 拒绝，不静默降级）+ `AgentSelectorTest` 新增 5 个 STRICT 用例（19/19 全绿）+ `verify-agenthub-duty-e2e.ps1` 追加 S6.1/S6.2/S6.3 子场景（**真实环境实测 ALL PASSED**，S6.1 STRICT 落库 / S6.2 strict 小写→DB 仍是 STRICT / S6.3 BOGUS_VALUE 被拒绝且 lease count 未增）。**A2 第 2 段（动态 TTL 自适应）与第 3 段（concurrency 预扣）待续**，迭代记录 §6.3 仍为其设计参考
6. **N13 AgentHub V3 门铃通知通道**：PR-1 内核 + PR-2 响铃 + PR-3 值班鉴权收口 + PR-4 保活帧/双心跳均已交付（2026-07-17，单测 33/33 全绿 + 全 reactor install BUILD SUCCESS）；**运行时端到端 `verify-doorbell-e2e.ps1` 真实环境实测 ALL PASSED（S1 拒连 500 / S2 200+connected / S3 到期翻 EXPIRED + 事件驱动断连）**；~~仅余多实例 Redis Pub/Sub fanout（§12 演进项）~~ → **2026-08-07 整体搁置**（外部 Agent 无法处理平台推送，任务感知改走 pullTasks 轮询，见 N13 状态列与迭代记录 §6.50）
7. **外部 Agent 一键接入全套化（2026-07-17，路 A 修产品）**：`checkIn`/`checkOut` 纳入 `DEFAULT_EXECUTOR_TOOLS`（8→10，靠 `isToolEnabled` 懒启用覆盖存量 Agent、无需 Flyway）；executor SKILL.md 由纯 REST 升级为含 MCP 全套 10 工具/打卡/门铃的完整说明书；新增第 2 步真实路径验证脚本 `verify-onboarding-doorbell.ps1`（注册→MCP checkIn→门铃 connected，**真实环境实测 ALL PASSED PASS=7 FAIL=0**）；新增第 3 步验证脚本 `verify-onboarding-pull.ps1`（门铃 inbox 信号→MCP pullTasks 取任务，**真实环境实测 ALL PASSED PASS=12 FAIL=0**，门铃 refId=pullTasks subTaskId=创建 ID 三处一致）；新增第 4 步验证脚本 `verify-onboarding-heartbeat.ps1`（双心跳：server→client 保活帧 `event:keepalive` + 连接不中断 / client→server `heartbeat` 刷 `last_seen_at`+在线态，**真实环境实测 ALL PASSED PASS=12 FAIL=0**）；新增第 5 步验证脚本 `verify-onboarding-submit.ps1`（注册→checkIn→ASSIGNED 子任务→`submitResult(success)`→子任务流转 `REVIEW` 完整闭环），并在验证中定位/修复 `submitResult` 成功路径**事务自死锁**（`ExecutionResultHandler.handleReport` 内 `heartbeatService.active()` 锁 agent 行 + `failureTracker.recordSuccess()` `REQUIRES_NEW` 改同一行 → 自死锁约 40s 超时；修复：failureTracker 计数挪到主事务 `afterCommit` 执行，仅 `CLI_CLIENT`+成功路径受影响，`ExecutionResultHandlerTest` 4/4），**真实环境实测 ALL PASSED PASS=12 FAIL=0**（submitResult 毫秒级 `status:applied`、子任务 `ASSIGNED`→`REVIEW`）；至此外部第三方 AI 接入完整闭环五步全通；`mvn -pl helloai-core -am compile` BUILD SUCCESS
7b. **当前下一里程碑：真实 AI 子任务闭环联调（M5）**——qoder/trae 打卡链路已通（2026-07-18），
    按《项目进度》M5 场景矩阵执行。~~场景 1~3 全绿前，planner 自动拆解（编排层）不启动~~
    → **2026-07-28 按用户决策提前启动 planner 编排层（V26 已交付，见 N16）**：拆解链与执行链通过
    草案态 `PENDING_PLAN_REVIEW` 硬隔离（确认前不进分发链），planner 缺陷与执行环缺陷可独立定位，
    原串行门禁的依赖前提已解除；M5 场景矩阵继续推进。
8. N9 / N10 / N11 维持现状
9. 最后推进产品化编排能力：N2、N8
10. §5.2 后置（WorkUnit / STOP/PAUSE/REPLAN / 用户输入可重入）—— 等可靠投递与兜底职责收紧之后再开

---

## 6. 本轮文档治理结论

- `v3.0` 已不再作为路线图继续维护，而是降级为《架构设计参考》
- `v2.4` 已归档为历史资产，不再作为事实源
- 后续若出现“设计理念、目标态、实现状态、执行记录”混写，优先拆回文档矩阵，而不是继续往单个文件里堆内容
- **2026-07-20 架构治理增量**：CODE_STYLE §6.3 待收口清单（6 个 Controller）与 3.x start 配置类待收口段落同步收口，本轮新增 N15（Controller 分层红线收口 + 两项包归位）记录在案
- **2026-07-20 调度链缺陷修复（v2.6 §4.1）**：`helloai-core/pom.xml` 新增 `spring-boot-starter-aop` + 新建 `helloai-common/.../config/AgentHealthProperties`（默认 `offlineMinutes=5`）统一心跳阈值；`AgentSelector`/`ExternalAgentFailureTracker`/`AgentHealthCheckTask` 全部接入共享阈值（消除 Java/SQL 漂移）；`AgentHealthCheckTask.reassignStaleTasks` 首选 `redispatchOfflineSubTask` 失败后二次走 `dispatchPendingSubTaskAuto`（原 Agent role 缺失回退 EXECUTOR）；`ExternalAgentFallbackTask.scan()` 拆分为阶段 A（N11 阈值回退）+ 阶段 B（`recoverPendingUnassigned` 全局 PENDING 兜底）；新增 `SubTaskMapper.selectPendingUnassignedWithoutActiveExecutionRecord`（PENDING + assigned_agent_id IS NULL + 有历史 record + 无活跃 record）；新增 `ResilientDispatcherAopIntegrationTest`（Spring AOP 织入真实验证 fallback）+ `AgentHealthCheckTaskTest`（11 个反射调 `reassignStaleTasks`）覆盖二次选人；顺手修复 5 个 Doorbell 测试 package/目录不一致；`mvn test` reactor 7 模块全 SUCCESS，helloai-core 216 + helloai-job 38 + 聚焦测试 96 个全绿；jar `helloai-start-1.0.0-SNAPSHOT.jar` 62MB 可构建（沙箱无 PG/Redis，真实断心跳链路验收需外部环境）。迭代详情 `doc/log/HelloAI_迭代执行记录.md` §2026-07-20 调度链缺陷修复（v2.6 §4.1）
- **2026-07-20 重分配熔断（V24）**：`V24__sub_task_reassign_attempt_count.sql` 新增 `sub_task.reassign_attempt_count` 列；`AgentDispatchProperties.maxReassignAttempts`（默认 5）控制熔断阈值；`SubTaskDispatchService.checkReassignCircuitBreaker()` 在所有 4 个重分配入口（`redispatchOfflineSubTask`/`redispatchAssignedTimeout`/`redispatchForFallback`/`dispatchBlockedSubTask`）前统一检查；达到阈值后标记子任务 CANCELLED + 记录 `sub_task_cancelled` timeline 事件，打破"Agent 全掉线→无限重分配"死循环。迭代详情 `doc/log/HelloAI_迭代执行记录.md` §2026-07-20 重分配熔断（V24）。**2026-07-28 V25 升级（旁路已封堵 + 死信兜底已交付）**：`dispatchPendingSubTaskAuto` 入口补 `checkReassignCircuitBreaker`（封堵 AgentHealthCheckTask 二次选人 / ExternalAgentFallbackTask.recoverPendingUnassigned / SubTaskPendingOrphanTask 三个定时任务的无计数旁路）；达阈值改置 `DEAD_LETTER`（V25 Flyway 重建 CHECK 约束，非终态，替代原 CANCELLED）+ timeline `sub_task_dead_letter`；新增人工兜底接口 `POST /api/sub-tasks/dead-letter/redispatch/{id}`（`redispatchDeadLetter` 清零计数 + 直接指派 ASSIGNED）；`ResilientDispatcher.assignNext` fast-fail 补心跳新鲜度检查（复用 `AgentSelector.isHeartbeatFresh`，消除"DB 仍 ONLINE 但 Agent 已死"约 5-6 分钟误派窗口）；ASSIGNED 超时阈值配置化 `helloai.dispatch.assigned-timeout-minutes`（默认 10）；验证脚本 `scripts/powershell/verify-subtask-deadletter.ps1`。迭代详情 §6.10
- **2026-07-20 M4.5 派发控制台**：落地 `doc/M4.5_派发控制台实施清单.md` 全量范围。`SubTaskController` 增 `POST /api/sub-tasks/batch`（复用 `SubTaskService.createBatch` 逐项隔离）与 `GET /api/sub-tasks/{id}/timeline`（复用 `TaskTimelineService.listBySubTaskId`）；新建 `helloai-api/.../dto/subtask/TaskTimelineItem.java` DTO；前端新建 `src/components/QuickDispatchDialog.vue` + `src/api/module.ts` + 注入 SubTaskList "快速派发"按钮 + SubTaskDetail "执行时间线"卡片 + 详情页 5s 轮询（终态自动停 + onBeforeUnmount 清理）；`helloai-ui` `npm run build` 通过；Controller 红线合规（无 Mapper 注入）。迭代详情 `doc/log/HelloAI_迭代执行记录.md` §6.9 M4.5 派发控制台
- **2026-07-28 任务-子任务关联打通 + 子任务列表真分页**：断点在前端（`SubTaskList.vue` 不读 `route.query.taskId`）而非后端；前端打通 taskId 过滤 + 主任务信息条 + 真分页；后端 `SubTaskService.list(taskId,status,assignedAgentId,page,pageSize)` 按 §6.3 从 Controller 收口条件构造，`GET /api/sub-tasks` 新增可选 `page`/`pageSize`（不传 page 保持纯数组，SKILL.md 外部 Agent 契约零破坏）；`SubTaskResponse` 新增 `taskTitle`（Controller `attachTaskTitles` 批量回填防 N+1）。`/available`、`/mine` 仍在 §6.3 待收口清单。helloai-core 228 测试全绿 + vue-tsc 0 错。迭代详情 §6.11
- **2026-07-28 任务管理 CRUD 收口 + 级联删除（竞态免疫）**：`TaskController` 新增 `POST /{id}/republish`（DONE 禁发，重置 PENDING + 新 eventId 重通知全部 PLANNER，不动子任务）、`GET /{id}/related-counts`、`DELETE /{id}`（body 传 confirmTitle 精确匹配，照 Agent confirmName 范式）；`TaskService.deleteTaskCascade` 事务内按序物理删 inbox→execution→review→timeline→sub_task→module→task（前三者 SQL 依赖 sub_task 子查询先删，规避 `@TableLogic` 软删，7 个 Mapper 补 `@Delete` 注解 physicalDeleteXxx）；竞态免疫依赖"DB 唯一事实源"：删后所有消费端回查 not_found 丢弃，另在 `NotificationConsumer` 补 `refTargetExists` 防御分支兜底在途 MQ 通知；前端 `TaskFormDialog`/`TaskDeleteDialog`（影响面统计+在途警示+标题确认）+ TaskList 新建/编辑/重新发布/删除按钮。改派竞态 4 漏洞（旧 inbox 不作废/handleReport 不校验 agentId/旧 PENDING 执行记录不取消/Poller 重放）本轮未修待下轮。mvn 56 测试全绿 + vue-tsc 0 错。迭代详情 §6.12
- **2026-07-28 值班租约列表改 Agent 维度展示 + 历史记录分页对话框**：值班租约页从租约平铺改为 Agent 维度分组——每 Agent 一行展示最新租约 + 租约总数，点"更多"弹窗分页查看该 Agent 全部历史，主列表也分页。后端 `AgentDutyLeaseMapper` 新增 `selectLatestPerAgent`（PG `DISTINCT ON (agent_id)` 取组内最新 + JOIN COUNT 子查询带出 lease_count）与 `countDistinctAgents`；`AgentDutyLeaseService.listLatestPerAgent` 手工拼 Page（自定义 SQL 不走分页插件）；新增 `GET /api/admin/duty-leases/by-agent` 端点 + `DutyAgentLatestResponse`/`AgentDutyLeaseLatestRow`（extends 冗余 leaseCount）。前端新建 `DutyLeaseHistoryDialog.vue`（复用既有 list 端点 agentId 过滤，零新增历史查询端点）+ `DutyLeaseList.vue` 重写；顺手修复 `duty.ts` `list.agentId` 参数 `number → LongId` 雪花 ID 精度隐患。既有 list/overview 端点零改动，Dashboard 不受影响；DB 无变更。helloai-core 228 测试全绿 + vue-tsc 0 错 + DISTINCT ON SQL 经 postgres_helloai MCP 真库验证。迭代详情 §6.13
- **2026-07-28 Planner 平台内自动拆解（V26）**：落地"需求 → LLM 自动拆解 → 用户确认/拒绝 → 进入既有分发链"闭环（详见 N16）。`SubTaskStatus.PENDING_PLAN_REVIEW` 草案态 + `TaskStatus.PLANNING` + Flyway V26 重建 CHECK 约束；`PlannerAnalysisService`（core/planner，编排收口 core 对齐 §6.3）decompose/listDrafts/confirmPlan/rejectPlan 四方法，CAS 防并发 + strip fence 容错解析 + 失败 CAS 回退；`TaskController` 四个薄入口；`skills/planner/SKILL.md` 升级至 executor 版同等水准；单测 `PlannerAnalysisServiceTest`(13，项目内首例 lambdaQuery/lambdaUpdate 链式 mock) + 状态机 V26 用例(3)，helloai-core 244 全绿 + 全模块 compile OK；`verify-planner-decompose.ps1` 语法自检通过、待真实环境实测。原 §5 7b "场景 1~3 全绿前不启动 planner 编排层"门禁按用户决策解除（草案态硬隔离使 planner 缺陷与执行环缺陷可独立定位）。迭代详情 §6.16
- **2026-07-29 任务级联删除 FK 违反修复 + 拆解链前端补全（N16 前端遗留收口）**：阶段 0 修复 §6.12 级联删除遗漏——`V1__init_all.sql` 中 FK 引用 `sub_task(id)` 的表共 6 张，`deleteTaskCascade` 只删了 2 张，子任务带附件/巡检/会话数据时删任务必炸 `attachment_sub_task_id_fkey`；`AttachmentMapper`/`PatrolRecordMapper`/`ConversationArchiveMapper`/`ConversationMessageMapper` 各补 `physicalDeleteByTaskId`（sub_task 子查询物理删，照 ReviewRecordMapper 范式）并接入 `TaskService.deleteTaskCascade` 删除序。拆解链前端（纯 helloai-ui，后端零改动）：`types/index.ts` 补 `PENDING_PLAN_REVIEW`/`PLANNING` 枚举 + SubTask 补 deliverable/acceptance/priority + 新增 `TASK_STATUS_MAP`；`api/task.ts` 补 plan（单请求 timeout 120s）/planDrafts/confirmPlan/rejectPlan；新组件 `PlanReviewDialog.vue`（草案表格含依赖序号映射 + 确认分发/拒绝重拆双动作 + 空草案兜底）；`TaskList.vue` 状态列改 TASK_STATUS_MAP + PENDING 态"AI 拆解"按钮（loading + 成功直接开审阅）+ PLANNING 态"审阅草案"按钮。helloai-core mvn test 全绿 + vue-tsc 0 错 + build 通过；浏览器闭环实测与 `verify-planner-decompose.ps1` 回归待真实环境（本轮后端未启动）。迭代详情 §6.18
- **2026-07-29 拆解链真实环境回归收口（同日第二轮）**：`verify-planner-decompose.ps1` 等价迁移为 macOS zsh 版 `scripts/shell/verify-planner-decompose.sh`（curl+jq，照 verify-dashboard-duty-leases.sh 模板；补原 ps1 缺失的托管凭证绑定步骤 STEP2.1——新注册 planner Agent 无 deepseek 凭证时拆解必 500「Agent 未配置启用态托管凭证」，对齐 verify-inner-loop-e2e.ps1 做法绑定 `/api/credentials/agents/{id}/api-key`），真实环境 e2e 12 步全绿（confirm 路径 5 草案 PENDING_PLAN_REVIEW 转正 + Task IN_PROGRESS / reject 路径草案作废回退 PENDING / 重复拆解拒绝）；浏览器 UI 闭环实测通过（confirm 路径 6 草案确认分发→「进行中」、reject 路径 4 草案作废→回「待规划」可重拆、console 0 error）。N16 全部遗留收口；带附件子任务删除回归仍待造数验证。迭代详情 §6.18
- **2026-07-29 对话式需求澄清窗口（V29，同日第三轮）**：新增 N17——"第二步立项"入口，模糊想法在 `/requirement-chat` 独立页与 LLM 多轮对话澄清（追问每轮最多 3 问，轮数硬上限 20），LLM 产出终稿后一键「创建任务并自动拆解」（finalize 建 Task PENDING + 通知 PLANNER + timeline，前端顺路调既有 plan 接口，跳任务列表自动开草案审阅弹窗）。Flyway V29 两张新表（task_id 软引用无 FK，deleteTaskCascade 零改动）+ `RequirementClarifyService`（复用 PlannerAnalysisService 五段式范式）+ 六端点 Controller + 前端会话列表/气泡流/终稿卡片。单测 13 例全绿 + zsh 脚本 `verify-requirement-clarify.sh` 真实环境 10 步全绿 + 浏览器闭环实测 8 步全过。迭代详情 §6.19
- **2026-07-30 子任务详情展示优化（方案1）+ 拆解/澄清链配套 + 执行产出物化方案设计**：前端（helloai-ui，纯前端）新增 `MarkdownView.vue`（markdown-it + dompurify）/ `ReviewVerdictView.vue`（核验分析结构化卡片），`SubTaskDetail.vue` 执行对话流 Markdown 富文本渲染 + 时间线事件人话化（`EVENT_META`）+ Agent 注册名映射 + 返回列表归属主任务 + 产出复制/导出 .md（方案1）+ 核验请求剥离 HTML 注释。后端配套：`PlannerAnalysisService.orderByDependency`（Kahn 拓扑正序）+ `RequirementClarifyService.regenerate`（悬挂任务重建）。**新增设计文档 `doc/design/HelloAI_执行产出物化与结构化多文件产出方案.md`（方案2/3，仅设计草案、后端代码未实现）**：本地文件系统存储 + `ArtifactStorage` 抽象（config 门控 `helloai.storage`）、方案2 是方案3 降级形态、物化 `afterCommit` best-effort、下载 `local://` 流式改造；待后续落地时回填"`attachment` 表从 0 写入 → 内置执行链产出物化"状态变化。工程：新增 `docker-compose.server.yml` / `nginx.server.conf` + `.gitignore` 忽略 `.tmp/`。迭代详情 §6.27
- **2026-07-30 澄清对话重试按钮 + Planner 手动选择下拉选**：新增 `PlannerAgentPicker`（core/planner）收编澄清/拆解两处重复选型逻辑（等权重优先空闲；pinned 失效宽松回退自动）；`requirement_conversation` 新增 `planner_agent_id` 钉住字段（Flyway V31），手动选中的 Planner 从澄清跟到拆解（`pickForTask` 经 task_id 软引用反查）；新端点 `GET /requirement-conversations/planner-options`（内部 PLANNER 可选 + 在班外部 Agent 置灰）与 `POST /{id}/retry`（仅最后一条为 user 消息时，不增消息不加轮次）；前端 RequirementChat 新会话 Planner 下拉选 + 失败重试条（canRetry 数据驱动，刷新后仍可重试）+ create 失败按 title 找回已落库会话。helloai-core 312 测试全绿 + vue-tsc 本轮文件 0 错；真实环境验证待后端重启（V31 自动迁移）。迭代详情 §6.29
- **2026-07-31 方案2 执行产出物化 + 主任务交付物实时聚合 zip 下载**：§6.27 设计文档的方案2 与主任务打包下载两层同轮落地，「仅设计草案、后端代码未实现」状态翻转（设计文档头部状态行已同步更新，方案3 LLM manifest 仍为遗留）。后端物化链：`ArtifactStorageProperties`（`helloai.storage.{enabled,provider,local.base-path,max-artifact-bytes}`）+ `ArtifactStorage`/`LocalArtifactStorage`（`local://{bucket}/{objectKey}`，normalize+startsWith 穿越防护）/`StoredArtifact` 存储三件套 + `ExecutionOutputParser`/`ParsedOutput`/`ArtifactFile` 解析三件套（纯文本→单 .md，方案3 扩展位已预留）+ `ExecutionArtifactService.materialize`（best-effort，register 传 assignedAgentId 过归属校验，时间线 `sub_task_artifact_materialized`）挂接 `ExecutionResultHandler` 成功分支 afterCommit；`AttachmentService.loadContent` + `AttachmentController.download` 流式改造（`local://` 走后端流式输出，非 local 保持 302）。主任务 zip：`SubTaskDependencyOrder`（Kahn 拓扑）+ `TaskDeliverableService.buildZip`（内存组包 UTF-8，`00-任务概览.md` 子任务完成情况表 + 拓扑序 DONE 产出，优先物化附件、无可读附件回退 `context.lastExecution.output`，排除草案/取消，重名后缀）+ `GET /api/tasks/{id}/deliverables/download`（RFC 5987 中文文件名）。前端：TaskList「交付物」按钮 + SubTaskDetail「产出附件」卡片（轮询刷新/无附件隐藏）+ `utils/download.ts` blob 落盘。mvn 333 测试全绿（+21：parser 5/storage 6/artifact 5/deliverable 5）+ vue-tsc TSC-OK（顺带修复 `tsconfig.node.json` 缺 skipLibCheck 阻断 `-b` 构建）。历史任务（attachment 0 条）走 context 回退可立即下载；重启后端生效（无 Flyway）；真实环境 E2E 回归待做。迭代详情 §6.30
- **2026-07-31 任务最终整合报告（V32，Planner 整合全部子任务产出）**：交付物 zip 各子任务产出分立的后续补齐——任务收口后由 Planner（pickForTask 钉住，澄清→拆解→整合同一人）把全部 DONE 子任务产出整合为一份连贯报告（执行摘要+重组正文+结论）。Flyway V32 task 专列三列 TEXT（final_report/final_report_agent_id/final_report_time，避开 JSONB 写入需 XML 覆盖的改造）；`TaskFinalReportService.generate`（仅 DONE，取数与 zip 同源拓扑序，单段 8000 字符截断，timeline 记 llm_call_start/generated/failed）；自动触发：`SubTaskCompletionListener.tryCloseTask` CAS 赢家分支发 `TaskAutoCompletedEvent`（赢家唯一防重），服务 `@Async+@EventListener` 承接（发布点无事务），开关 `helloai.dispatch.auto-final-report-enabled`（默认 true），失败吞掉不阻断收口；手动：`GET/POST /api/tasks/{id}/final-report`（历史任务补生成/覆盖重生成）；zip 有报告时置顶 `01-最终整合报告.md` 子任务顺延 02- 起；前端 TaskList「报告」按钮（仅 DONE）+ `FinalReportDialog`（MarkdownView + 生成/重生成 + 复制/导出 .md）。mvn 343 全绿（+10）+ TSC-OK；重启后 V32 生效，真实环境 E2E 待回归。迭代详情 §6.31
- **2026-07-31 结构化选项式需求澄清（V33，同日第二轮）**：N17 澄清链路交互升级——追问由纯文本升级为「选项点选为主、自由输入兜底」的双模协议，用户面对模糊需求不再需要打字长文回答。Flyway V33 `requirement_message.payload` TEXT 一列两用（assistant=结构化问题 JSON / user=选择快照 / 纯文本 NULL）；prompt 三形态输出（structured question / freeform question / final+progress:100）+ 五维度自检清单；`RequirementClarifyService` 扩展 `sendMessage(id,message,selections)` 重载 + `parseReply` 降级 freeform 一等公民（判据 `rawOutput.contains("\"type\"")`）+ `normalizeQuestionReply`/`isStructuredValid`/`fillStructuredDefaults`/`composeAssistantContent`（structured content 合成可读文本，payload 丢失不影响 LLM 上下文）+ `ClarifyQuestion`/`ClarifyOption`（weight 预留字段无业务消费）/`ClarifySelection` 三嵌套类；`RequirementMessageService.addMessage` 4 参重载（3 参委托保兼容）；前端 `StructuredQuestionCard.vue`（选项 chip + recommended 标记 + 可多选 + 自定义补充 + 卡片 `:key` 绑最后消息 ID 换轮重置）+ RequirementChat 顶部进度条（FINALIZED 直接 100）。progress 仅展示无业务分支；旧消息 payload NULL 自然走纯文本气泡向后兼容。`RequirementClarifyServiceTest` 21/21 全绿（原「非 JSON 报错」用例语义更新为降级断言 + 新增 4 例）+ vue-tsc 0 错 + `verify-requirement-clarify-structured.ps1` 真实环境实测 PASSED（脚本修复一处：中文 JSON 体须 UTF-8 字节发送，否则触发后端 Invalid UTF-8 middle byte）。本轮明确不做：weight 权重业务消费、多轮对话策略（P1）、progress 驱动业务分支。迭代详情 §6.32
- **2026-08-01 对话式需求澄清联网搜索开关（V34）**：N17 增量——首轮 LLM 调用前按用户问题预检索行业资料/竞品/技术方案注入 Prompt 增强拆解质量，仿腾讯 ima copilot 与 DeepSeek/Kimi 网页版的"联网搜索"开关。会话级开关（NULL/true=默认开启 / false=关闭），前端仅在新建会话前可设（已存 ACTIVE 会话锁定）。Flyway V34 `requirement_conversation.web_search_enabled BOOLEAN`（COMMENT 明示 NULL=默认开启）；`WebSearchProperties`（helloai-common，`helloai.web-search.{enabled=true, provider=bocha 默认, timeout-ms=3000, max-results=5, max-snippet-chars=200, query-keyword-limit=40, bocha-*, tavily-*}`）+ `WebSearchService` 接口（`provider()` + `search(query,maxResults)`）+ `WebSearchServiceRouter`（@Primary，`ObjectProvider<WebSearchService>` 解析候选）+ 供应商实现 `BochaWebSearchService`（`@ConditionalOnProperty(provider=bocha, matchIfMissing=true)` 默认激活，国内 `https://api.bochaai.com/v1/web-search`）/ `TavilyWebSearchService`（`@ConditionalOnProperty(provider=tavily)`，海外 `https://api.tavily.com/search`）；用户服务器在境内 → 博查默认。`RequirementClarifyService.doRound` 首轮（`rounds==0`）+ 开关开启时按首条消息截前 40 字调一次 `webSearchService.search`（关键词截断由 `queryKeywordLimit` 控制），失败一律 warn log + 返回空串（不阻断主流程）；`renderPrompt` 替双占位符 `{{CONVERSATION_HISTORY}}` + `{{WEB_SEARCH_CONTEXT}}`（无资料时占位为"（无可用联网资料）"保 Prompt 语义节稳定）；`prompts/requirement-clarify.md` 新增「联网检索资料」节明确"核心需求仍以用户描述为准"/"不要在 JSON 字段引入'参考资料'键"原则。`ClarifyMessageRequest` 加 `Boolean webSearchEnabled`；Controller 透传三参至 Service；旧 `create(message,plannerAgentId)` 重载保留保兼容。前端：`RequirementChat.vue` `webSearchEnabled` ref 默认 true；输入栏左侧「联网搜索」+ Connection 图标 + tooltip + `el-switch` inline-prompt（开/关）；`activeId==null` 时可改，已有 ACTIVE 会话置灰（开关仅建会话生效）；`watch(detail.conversation.webSearchEnabled)` 已存会话同步原值（不可改）；`handleSend` 新会话分支透传 `webSearchEnabled.value`；`types/RequirementConversation` 加 `webSearchEnabled?:boolean|null`；`clarifyApi.create` 加 `webSearchEnabled` 第三参。设计取舍：① 会话级而非回合级（避免每次重新检索副作用）；② 首轮注入而非多轮（避免 token 爆炸与上下文漂移）；③ 失败降级而非事务回滚（联网是增强非核心）；④ 接口 + Router（业务侧零供应商感知，切换/新增零业务改动）；⑤ 默认 bocha 而非 Tavily（境内可用性）。`scripts/shell/verify-websearch-e2e.sh`（UTF-8 头 + set -euo pipefail + curl + jq）覆盖三条路径——关（`webSearchEnabled:false` 落库 false + roundCount=1）/ 开（`true` 落库 true + roundCount=1）/ 不传（落库 null 保默认开启兼容）；`bash -n` + `zsh -n` 语法自检通过；`mvn compile` 沙箱无 mvn 需 IDE 验证；单测增量（联网降级 + 占位符替换 + Provider 路由）待补。本轮明确不做：每轮重新检索、多供应商 failover、按角色区分检索策略、检索词 LLM 改写。迭代详情 §6.38
- **2026-08-03 Planner 对话双模式（V39，N17 升级）**：需求澄清会话升级为 Kimi/DeepSeek 式双模式——CHAT 自由对话（通用助手模板纯文本问答，无 JSON 协议、无 progress、无首轮联网搜索、独立 50 轮上限）+ CLARIFY 方案澄清（沿用既有全部行为：首轮联网搜索 + progress 自评 + JSON 三选一协议）。单会话 `mode` 字段（NULL 老数据=CLARIFY 兼容），用户主导双向切换（to-clarify/to-chat 两端点），CHAT 命中意图词（整理成方案/生成方案/落地实施等）自动单向切 CLARIFY 且该条消息即澄清首轮、意图词永远放行。Flyway V39 `requirement_conversation.mode VARCHAR(16)` + CHECK 约束 + DO 验证块；`RequirementClarifyService` 双模式分派（create 四参重载带 initialMode / sendMessage 上限按模式分派 / doRound 意图词置位 / runLlmRound 按模式选模板与 scene / switchToClarify / switchToChat）；`prompts/requirement-chat.md` 通用助手模板；`ClarifyMessageRequest.initialMode` + Controller 三端点；前端模式徽标 + 进度条与终稿卡条件化 + 「转为方案」按钮 + 新会话模式单选。单测 35/35（+14 ChatModeAndSwitch）；`verify-planner-chat-dual-mode.sh` 真实环境 8 步 PASS（CHAT 纯文本 payload=NULL → 二轮仍 CHAT → to-clarify 产终稿 finalTitle → finalize 建任务 PENDING+回填 → 意图词新会话自动切 CLARIFY → to-chat 仅置位不加消息）。迭代详情 §6.44
- **2026-08-03 意图词二次确认（V40，N17 升级）**：去掉「转为方案」按钮，转方案改为「意图词触发 + 对话内二次确认」——CHAT 命中意图词不再自动切 CLARIFY，置 `pending_clarify_confirm` + 回复固定确认询问（不调 LLM、不加轮数）；回复确认词或再次意图词 → 转入 CLARIFY（该条消息即澄清首轮）；回复其他内容 → 清标记继续自由对话。Flyway V40 `pending_clarify_confirm SMALLINT NOT NULL DEFAULT 0`（§9.3 不用 BOOLEAN）+ DO 验证块；自定义 `SmallIntBooleanTypeHandler`（SMALLINT↔Boolean）；doRound 三段状态机 + sendMessage 上限放行确认词/意图词；切换端点按 §8.2 整改 `toClarifyById`/`toChatById`；前端移除「转为方案」按钮。单测 ChatModeAndSwitch 19 例全绿 + 修复 `PlannerAnalysisServiceTest.shouldDecomposeAndPersistDrafts` pre-existing Error（补 list(Wrapper) stub）+ helloai-core 全量 389/389 全绿 + vue-tsc 0 错 + `verify-planner-chat-dual-mode.sh` 改造后 8 步 PASS（STEP7 意图词→待确认→回复确认→CLARIFY 全流程断言）。迭代详情 §6.45
- **2026-08-01 执行链依赖上下文注入（V35，执行 Agent 参考上游产出，同日第二轮）**：N6 执行链补缺——此前 `depends_on` 只做调度排序（解锁下游/拓扑/ready 守卫/跳过分发），执行 Agent 的 Prompt 不含任何上游产出，依赖关系“只排序不传上下文”（用户审查时序图发现：只见领取/执行，不见参考依赖）。本轮在纯执行入口 `SubTaskExecutionService.executeOnce` 中新增 `loadDependencyContext`：按 `dependsOnIdList` 声明顺序用 `SubTaskService.listByIds` 批量查前置，DONE 且 `context.lastExecution.output` 非空 → 以 `## 上游产出参考` 独立章节注入 `buildUserPrompt`（前置标题/状态 + 产出正文，单条超 4000 字符截断并显式标注）；DONE 无产出 / 非 DONE（死信指派等旁路）→ 注入状态说明占位；查询或渲染异常一律 warn + 返回空上下文（产出参考是增强信息不是交付门槛，沿用 V34 联网搜索降级哲学）。可观测：声明依赖时记录新 timeline 事件 `sub_task_deps_context_loaded`（payload：depCount/loadedCount/truncatedCount/degraded，无依赖零噪音）。抽公共工具 `SubTaskOutputExtractor`（shared/util，静态方法读 `context.lastExecution.output`）并替换 `TaskFinalReportService` / `TaskDeliverableService` 两处同款私有实现，消除多消费方先例漂移。前端：`sequenceFlow.ts` LABEL 加 `sub_task_deps_context_loaded: '装配依赖产出'` 且泳道归 EXT（执行 Agent）；`SubTaskDetail.vue` EVENT_META 加同 key（`参考上游产出` + 描述），时序图/时间线能看到“读取上游产出”环节。测试：`SubTaskExecutionServiceTest` 新增 5 例（无依赖不查库 / 有依赖注入产出 + timeline 事件 / 前置无产出占位 / 超长截断 / 降级不阻断且 degraded=true），16/16 全绿；全模块 `mvn compile` SUCCESS + `vue-tsc --noEmit` 0 错。无 Flyway 无配置项；真实环境 E2E 待回归。本轮明确不做：产出摘要化/向量化、跨任务依赖上下文、按依赖层级多级透传（仅直接前置）。迭代详情 §6.39
- **2026-08-02 子任务 LLM 对话消息可视化 + reviewHistory 多轮累积（V38，§6.41）**：N6 可观测性子增强——针对用户发现的两个互补缺环加近："对话流黑箱"（实际送给 LLM 的 user prompt 未落库）与"单轮驳回信息丢失"（`context.lastAutoReview` 单 Map 覆盖式写入导致历史 reviewer issue 被静默替换）。本轮同时落地两件：**（a）LLM 对话消息可视化**——`SubTaskExecutionService` 构造器注入 `ConversationService`（同 `ExecutionResultHandler` 同款）；`executeOnce` 在 `recordEvent(sub_task_llm_call_start)` 后、`executeSync` 前插入 `try { conversationService.addMessage(subTaskId, agent.getId(), "user", "agent", task.getUserPrompt(), "sub_task_execute_user_prompt"); } catch { warn }`；与 V28 既有 `sub_task_execute` 输出对偶形成 user→assistant 完整对话；异常路径（LLM 拋错）prompt 仍留库，与 `ExecutionResultHandler.handleFailure` 写入 `sub_task_execute_failed` 互补（前者保输入后者保错误）；不入池 `ApiKeyAgentExecutor` / `AgentChatClientService`，拦截点保持唯一。**（b）reviewHistory 多轮累积**——`sub_task.context.lastAutoReview` (Map) 重构为 `context.reviewHistory` (List<Map>)，每轮 `{round, ts, reviewerAgentId, issues, comment, score, executorDoneIssues}`；`SubTaskReviewService.rejectAndRework` 从覆盖式改为 append（读已有 List 或把旧 lastAutoReview 包成首轮），同时写 `reviewHistory` 与 `lastAutoReview`（最新值）保完全向后兼容；`appendReworkContext` 按 `### 第 N 轮` 铺开（优先读 reviewHistory，缺失时回退 lastAutoReview 包成单轮，issues 字段同时支持 List 新格式与 String 旧格式）；`executorDoneIssues` 字段预留读取但本轮不主动写（语义相似度对比留作后续 hook）。**数据库**：Flyway V38 `V38__review_history_backfill.sql` 幂等回填——`reviewHistory` 为空但 `lastAutoReview` 非空的子任务统一包成 `reviewHistory[1]`（`round=1` + `ts=update_time::text` 兜底 + `executorDoneIssues=[]`），保证新增读路径不因历史数据残缺而静默丢失。**前端**：`SubTaskDetail.vue` `CONV_TAG_MAP` 新增 `sub_task_execute_user_prompt: { label: '执行请求', type: 'info' }`；现有「执行对话流」组件按 toolName 自动渲染气泡 + 折叠 + MarkdownView，零新增卡片/tab。**验证**：`SubTaskExecutionServiceTest` 新增 `@Nested ExecuteOnceUserPromptAndReworkHistory` 5 例（user prompt 落库 / 异常路径 / reviewHistory 双轮铺开 / 全空跳过 / legacy lastAutoReview 兼容）+ `SubTaskReviewServiceTest` 新增 4 例（首次驳回 round=1 / 二次驳回 round=2 / 兼容 / executorDoneIssues=[]），**9/9 全绿**；`mvn -pl helloai-core -am compile` SUCCESS + `vue-tsc --noEmit` 0 错 + `npm run build` 产出 `SubTaskDetail-COMACOSA.js` 含新映射键；PS1 验证脚本 `scripts/powershell/verify-llm-conversation-stream.ps1` 新建覆盖 S1~S5 场景（对话流双气泡 / 首次驳回 reviewHistory=1 / 二次驳回 reviewHistory=2+userPrompt>=3 / dist 含标签键 / V38 回填由调用方用 MCP postgres_helloai 验证），`Parser.ParseFile` 静态自检 PARSE_OK。**遗留**：① `executorDoneIssues` 自动回填 hook（语义相似度对比留作专门迭代）；② PLANNER 对话流（PLANNER 走 `requirement_message` V29-V33，不混用 conversation_message）；③ ApiKeyAgentExecutor / `AgentChatClientService` 下沉（拦截点 executeOnce 已足够）；④ user prompt 流式预览（一次 4-8KB TEXT 字段够用）；⑤ review_record 表改动（既有 round 足够，审计链不破）。N6 仍标"已交付"，本轮为子增强不在 N 列表新开条目。迭代详情 §6.40
- **2026-08-04 子任务分发失败快速兜底 + 整合报告生成状态防重（V41）**：用户实测两问题的收口——①「10人小团队企业OA系统」任务子任务 #7 无人领取根因闭合（瞬时全员忙碌 + requireIdle 过滤致 pickPreferred 抛异常 + unlockDownstream 仅 warn 吞掉 + 孤儿巡检 30 分钟窗口过长），兜底：孤儿 PENDING 巡检阈值 30→5 分钟（`AgentExecutionProperties.pendingOrphanThresholdMinutes`，isReady 守卫保证不误伤）+ `SubTaskCompletionListener` 解锁失败写 `sub_task_dispatch_deferred` timeline 事件（异常不再静默）；② 报告按钮可重复点击导致重复生成，落地 `FinalReportStatus` 四态（NONE/GENERATING/DONE/FAILED，与 TaskStatus 解耦）+ Flyway V41 `task.final_report_status`（存量回填 DONE）+ `TaskFinalReportService.generate` CAS 防重入（生成中拒绝重复调用，成功 DONE、失败 FAILED 可重试）+ 前端按钮禁用/「报告生成中」状态展示/5s 轮询。helloai-core 单测 397/397 全绿 + vue-tsc 0 错 + 真实环境并发防重全链路冒烟 PASS（GENERATING→第二次被拒→DONE|19091）。迭代详情 §6.47
- **2026-08-02 Snowflake ID 全链路字符串化修复**：用户在使用"对话新建 → 终稿确认 → 查看任务/自动拆解"链路时，遇到 `GET/POST /api/requirement-conversations/{id}` 与 `/api/tasks/{id}/plan` 返回 `400 Bad Request`。根因是 Snowflake 长整型 ID（如 `2083818000152453122`）超出 JavaScript `Number` 安全整数范围（`2^53-1`），前端 JSON 解析后精度截断，再传回后端 URL 路径参数时 Spring 解析失败。本轮全链路加固：**后端**——`BaseEntity.id` 加 `@JsonSerialize(using = ToStringSerializer.class)`，所有继承实体（Task/SubTask/RequirementConversation/RequirementMessage/ConversationMessage 等）的 `id` 统一序列化为字符串；`helloai-common/pom.xml` 新增 `jackson-databind` 依赖以支撑注解；`RequirementConversation.taskId`/`plannerAgentId`、`RequirementMessage.conversationId` 加同样注解；关键 DTO（`TaskResponse`、`SubTaskResponse`、`AgentResponse`、`ReviewResponse`、`ModuleResponse`、`ConversationMessageItem`、`TaskTimelineItem`）的所有 `Long` 型 ID 字段加 `@JsonSerialize`/`@JsonSerialize(contentUsing)`。**前端**——`clarifyApi` 所有 URL 拼接处 `String(id)`；`RequirementChat.vue` 的 `activeId` 与所有会话/任务 ID 交互强制 string；`TaskList.vue`、`PlanReviewDialog.vue`、`FinalReportDialog.vue`、`TaskDeleteDialog.vue`、`TaskFormDialog.vue` 中所有 `row.id` / `props.task.id` 传 API 或拼 URL 处加 `String()` 防御。**可观测性增强**——`SubTaskReviewService` 新增 `subtask_review_result` 对话消息（通过/驳回、评分、问题、评语），前端 `SubTaskDetail.vue` 把执行对话流按「执行轮次 / 核验轮次」分组展示，user prompt 与 assistant 返回成对可见，返工轮次可展开查看完整 Prompt 含历史审核意见。**验证**：`mvn clean compile -pl helloai-common,helloai-core,helloai-api,helloai-start -am -DskipTests` SUCCESS；`npm run build` SUCCESS。**遗留**：后端必须重启后 `BaseEntity` 注解与 DTO 注解才生效；已运行的旧会话/子任务历史数据中的 ID 仍是 number，新创建的对象走字符串协议。迭代详情 §6.41
- **2026-08-07 LLM Provider 动态化方案B（V46，N9 §6.51 后续）**：上一轮把「平台级 API Key + 外网地址」从 yml 抽到 DB/runtime，但 Provider 本身（baseUrl / defaultModel / protocol / 启用状态）仍依赖 yml 硬编码 4 家。本轮交付 Codex++ 风格的「动态添加供应商」能力——任何内置 / 自定义 OpenAI / Anthropic 兼容厂商不再需发版。Flyway `V46__llm_provider_table.sql` 新增 `llm_provider` 表（provider_code/provider_name/protocol_type OPENAI_COMPATIBLE|ANTHROPIC_COMPATIBLE/base_url/default_model/enabled/builtin/sort_order/extra_config JSONB） + `update_update_time` 触发器 + `idx_llm_provider_enabled` 部分索引 + 幂等写入 4 家 builtin（deepseek/moonshot/minimax/dashscope）；后端 `system.entity.LlmProvider` + `LlmProviderMapper` + `LlmProviderService`（ServiceImpl，code 校验：正则 `[a-z0-9][a-z0-9-]{1,63}` + 唯一性 + builtin 不可改 code + builtin 不可删 + toLowerCase 归一化提前到 validateCode 之前 双重防御） + `LlmProviderQueryService.findByCode/listEnabled/listAll/getBaseUrlWithFallback`；通用厂牌族：`OpenAiCompatibleProtocolFactory`（原 Moonshot/DashScopeFactory + 新增自定义场景）+ `AnthropicCompatibleProtocolFactory`（原 Minimax 抽出来）+ `LlmProviderChatClientFactoryRegistry`（按 `provider.protocolType` 分发到对应 ProtocolFactory，deepseek 走官方 SDK 优先匹配）。`ProviderChatModelCache.buildKey` 扩展为 `(provider, apiKey, baseUrl, protocolType)` 四参数防 OpenAI/Anthropic 实例串错。3 个专用 Factory（Moonshot / DashScope / Minimax）+ 抽象基类 `AbstractOpenAiCompatibleProviderChatClientFactory` 全部删除（残留检查在 §3.3）；`LlmProviderCatalogService` 从 DB listAll 枚举 + `PlatformProviderConfigService` 三层 fallback（DB > sys_config > yml）收口 API Key 与 baseUrl 读取。`AgentChatClientService` 构造参数由 `ObjectProvider<List<ProviderChatClientFactory>>` 改为 `LlmProviderChatClientFactoryRegistry` 注入。New `AdminLlmProviderController`（`/api/admin/llm-providers`，`list/getById/toggleById/{id}/deleteById/updateById/{id}` + api-key 走 `PlatformProviderConfigService`）+ `CreateLlmProviderRequest/UpdateLlmProviderRequest/LlmProviderResponse`。前端 `settings.ts` 增 `llmProviderApi` + `LlmProviderResponse/CreateLlmProviderRequest` 接口；`Settings.vue` 重写为 Codex++ 风格「左侧供应商列表 + 右侧详情面板」布局 + 「+ 添加供应商」对话框（名称 / 协议下拉 / Base URL / 默认模型 / API Key）+ builtin 标签 + 启用开关 + 删除按钮（builtin 不显示）+ ServerURL 用途调整为“生成 SKILL 接入内容”。外部访问地址 `sys_config["helloai.base-url"]` 不动。单测：`LlmProviderServiceTest` 9 例全绿（重点修补：`ServiceImpl.baseMapper` 擦除类型为 BaseMapper，不带 type 参数的 `ReflectionTestUtils.setField` 注入 + 补齐 MIXED-Case 归一化 / Bad@Code 非法字符 / 单字长度的三段校验）+ `PlatformProviderConfigServiceTest` 加 DB 优先/兜底用例 + `AgentChatClientServiceTest`/`PlatformAgentExecutionServiceTest` 改造从 ObjectProvider 改为 Registry mock；`mvn -pl helloai-core test` 416/416 全绿 + `mvn clean package -DskipTests` 7 模块 BUILD SUCCESS。文档：CODE_STYLE V1.6 增补 3.x 业务域分包「system = ...,LLM Provider」职责补丁与「新增 LLM 厂商仅动 llm_provider 表 + chat.provider」动限范围。本轮明确不做：拖拽排序前端实现、Provider 粒度限流 / 配额、`from external import` 第三方批量导入、事件总线配置变更广播、Provider 配置变更审批流。迭代详情 §6.52
