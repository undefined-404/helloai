# V23 字段命名规范化：迁移脚本 + 全量同步清单

> 范围：**20 列** = 16 个 `_at` 时间字段 + 3 个 agent 引用（补 `_id` 后缀）+ 1 个关键字 `trigger`。
> 已核实：PostgreSQL `RENAME COLUMN` 会**自动更新**引用该列的索引/约束定义（本项目涉及 idx_agent_command_outbox_pending_scan、idx_agent_command_outbox_sent_scan、idx_exec_record_pending_attempt、idx_agent_external_failure_scan 共 4 个索引，无需重建）；列注释随列保留。
> 纪律：与包重构**分开提交**；本变更一个分支 `refactor/db-field-naming`，一次 V23 + 一轮代码同步。

---

## 一、V23 迁移脚本（直接落盘）

文件：`helloai-start/src/main/resources/db/migration/V23__field_naming_normalization.sql`

```sql
-- ============================================================
-- V23__field_naming_normalization.sql
-- 用途：字段命名规范化 —— 统一 xxx_time 时间后缀 / xxx_id 外键后缀 / 避开关键字
-- 涉及 20 列：16 个 _at 时间字段 + 3 个 agent 引用 + 1 个关键字 trigger
-- 说明：
--   1. PostgreSQL RENAME COLUMN 自动更新引用该列的索引/约束定义，无需重建：
--      idx_agent_command_outbox_pending_scan (next_retry_at)
--      idx_agent_command_outbox_sent_scan    (last_sent_at)
--      idx_exec_record_pending_attempt       (last_attempt_at, create_time)
--      idx_agent_external_failure_scan       (consecutive_failure_count, last_fallback_at)
--   2. 列注释随列保留，无需重建。
--   3. 本脚本必须与代码同步发布：entity 字段改名、
--      AgentMapper.xml / AgentDutyLeaseMapper.xml / SubTaskMapper.xml 裸列名、
--      AgentHealthCheckTask 中的 UpdateWrapper 字符串列名。
-- ============================================================

-- ---------- 1. agent：5 个时间字段 ----------
ALTER TABLE agent RENAME COLUMN last_seen_at     TO last_seen_time;
ALTER TABLE agent RENAME COLUMN last_active_at   TO last_active_time;
ALTER TABLE agent RENAME COLUMN offline_at       TO offline_time;
ALTER TABLE agent RENAME COLUMN last_failure_at  TO last_failure_time;
ALTER TABLE agent RENAME COLUMN last_fallback_at TO last_fallback_time;

-- ---------- 2. agent_execution_record：1 时间 + 1 关键字 ----------
ALTER TABLE agent_execution_record RENAME COLUMN last_attempt_at TO last_attempt_time;
ALTER TABLE agent_execution_record RENAME COLUMN trigger         TO trigger_type;

-- ---------- 3. agent_command_outbox：3 个时间字段 ----------
ALTER TABLE agent_command_outbox RENAME COLUMN next_retry_at TO next_retry_time;
ALTER TABLE agent_command_outbox RENAME COLUMN last_sent_at  TO last_sent_time;
ALTER TABLE agent_command_outbox RENAME COLUMN confirmed_at  TO confirmed_time;

-- ---------- 4. agent_duty_lease：3 个时间字段 ----------
ALTER TABLE agent_duty_lease RENAME COLUMN started_at      TO start_time;
ALTER TABLE agent_duty_lease RENAME COLUMN last_renewed_at TO last_renew_time;
ALTER TABLE agent_duty_lease RENAME COLUMN expires_at      TO expire_time;

-- ---------- 5. agent_inbox：2 个时间字段 ----------
ALTER TABLE agent_inbox RENAME COLUMN read_at    TO read_time;
ALTER TABLE agent_inbox RENAME COLUMN expires_at TO expire_time;

-- ---------- 6. credential_vault：1 个时间字段 ----------
ALTER TABLE credential_vault RENAME COLUMN expires_at TO expire_time;

-- ---------- 7. sub_task：1 时间 + 1 agent 引用 ----------
ALTER TABLE sub_task RENAME COLUMN completed_at   TO complete_time;
ALTER TABLE sub_task RENAME COLUMN assigned_agent TO assigned_agent_id;

-- ---------- 8. review_record / patrol_record：agent 引用 ----------
ALTER TABLE review_record RENAME COLUMN reviewer_agent TO reviewer_agent_id;
ALTER TABLE patrol_record RENAME COLUMN patrol_agent   TO patrol_agent_id;

-- ---------- 注释同步（原注释随列保留，这里补充改名溯源） ----------
COMMENT ON COLUMN agent_execution_record.trigger_type IS '命令触发来源：assigned / reassigned / retry / poll-recovery（原 trigger 列，避开关键字）';
COMMENT ON COLUMN sub_task.assigned_agent_id          IS '被分配的 Agent ID（原 assigned_agent，统一 xxx_id 后缀）';
COMMENT ON COLUMN review_record.reviewer_agent_id     IS '评审 Agent ID（原 reviewer_agent）';
COMMENT ON COLUMN patrol_record.patrol_agent_id       IS '巡检 Agent ID（原 patrol_agent）';

-- ---------- 验证：新列名应命中 20 ----------
DO $$
DECLARE
    renamed INTEGER;
BEGIN
    SELECT COUNT(*) INTO renamed
    FROM information_schema.columns
    WHERE (table_name = 'agent'                  AND column_name IN ('last_seen_time','last_active_time','offline_time','last_failure_time','last_fallback_time'))
       OR (table_name = 'agent_execution_record' AND column_name IN ('last_attempt_time','trigger_type'))
       OR (table_name = 'agent_command_outbox'   AND column_name IN ('next_retry_time','last_sent_time','confirmed_time'))
       OR (table_name = 'agent_duty_lease'       AND column_name IN ('start_time','last_renew_time','expire_time'))
       OR (table_name = 'agent_inbox'            AND column_name IN ('read_time','expire_time'))
       OR (table_name = 'credential_vault'       AND column_name =  'expire_time')
       OR (table_name = 'sub_task'               AND column_name IN ('complete_time','assigned_agent_id'))
       OR (table_name = 'review_record'          AND column_name =  'reviewer_agent_id')
       OR (table_name = 'patrol_record'          AND column_name =  'patrol_agent_id');
    RAISE NOTICE '[V23] 字段命名规范化完成，新列名命中数 = %（预期 20）', renamed;
END $$;
```

---

## 二、Entity 字段改名（9 个文件，IDEA Rename Field，getter/setter 与调用点自动联动）

> 项目实体以驼峰自动映射为主（`map-underscore-to-camel-case: true`），所以 **DB 列改名后 Java 字段必须同步改名**，不能靠 `@TableField` 挂旧名——那等于把不一致从 DB 搬到了 Java。

| Entity 文件 | 字段改名（改前 → 改后） |
|---|---|
| `Agent.java` | `lastSeenAt→lastSeenTime`、`lastActiveAt→lastActiveTime`、`offlineAt→offlineTime`、`lastFailureAt→lastFailureTime`、`lastFallbackAt→lastFallbackTime` |
| `AgentExecutionRecord.java` | `lastAttemptAt→lastAttemptTime`、`trigger→triggerType` |
| `AgentCommandOutboxEvent.java` | `nextRetryAt→nextRetryTime`、`lastSentAt→lastSentTime`、`confirmedAt→confirmedTime` |
| `AgentDutyLease.java` | `startedAt→startTime`、`lastRenewedAt→lastRenewTime`、`expiresAt→expireTime` |
| `AgentInbox.java` | `readAt→readTime`、`expiresAt→expireTime` |
| `CredentialVault.java` | `expiresAt→expireTime` |
| `SubTask.java` | `completedAt→completeTime`、`assignedAgent→assignedAgentId` |
| `ReviewRecord.java` | `reviewerAgent→reviewerAgentId` |
| `PatrolRecord.java` | `patrolAgent→patrolAgentId` |

（若包重构已合并，上述文件位于 `core/agent/entity`、`core/task/entity`、`core/system/entity` 下，按新路径找。）

## 三、手工必改（IDEA 重构管不到，共 4 个文件）

### ① `helloai-core/src/main/resources/mapper/AgentMapper.xml`

⚠️ 双重修改：SQL 裸列名 + `#{et.xxx}` OGNL 引用（Rename Field **不会**联动 XML 里的 `et.xxx`）。

| 行号 | 内容 | 改法 |
|---|---|---|
| L17 | 列清单 `last_seen_at, last_active_at, ... offline_at,` | 三个列名→`last_seen_time, last_active_time, ... offline_time,` |
| L18 | `consecutive_failure_count, last_failure_at, last_fallback_at,` | →`last_failure_time, last_fallback_time` |
| L56-63 | `last_seen_at = #{et.lastSeenAt}` 等 5 行 SET | 等号两侧**都改**：列名改新名，`#{et.lastSeenAt}`→`#{et.lastSeenTime}`（其余字段同理） |
| L82 | `offline_at = #{now}` | →`offline_time = #{now}` |
| L85 | `AND last_seen_at < #{cutoff}` | →`last_seen_time` |
| L98 | `WHERE (last_seen_at IS NULL OR last_seen_at < #{cutoff})` | 两处→`last_seen_time` |
| L112 | `last_failure_at = #{now}` | →`last_failure_time` |
| L126 | `last_failure_at = NULL` | →`last_failure_time` |
| L141-142 | `last_failure_at = NULL, last_fallback_at = #{now}` | →新名 |
| L161 | `AND (last_fallback_at IS NULL OR last_fallback_at < #{cooldownCutoff})` | 两处→`last_fallback_time` |
| L162 | `ORDER BY consecutive_failure_count DESC, last_failure_at ASC` | →`last_failure_time` |
| L73/91/94/120/135/153 | 注释中的旧列名 | 建议同步改，防止后人照注释写错 |

### ② `AgentDutyLeaseMapper.xml`

| 行号 | 改法 |
|---|---|
| L42 | `AND expires_at < #{cutoff}` → `expire_time` |
| L44 | `ORDER BY expires_at ASC` → `expire_time` |
| L36 | 注释同步 |

### ③ `SubTaskMapper.xml`

| 行号 | 内容 | 改法 |
|---|---|---|
| L21 | `assigned_agent = #{et.assignedAgent}` | →`assigned_agent_id = #{et.assignedAgentId}`（两侧都改） |
| L29 | `completed_at = #{et.completedAt}` | →`complete_time = #{et.completeTime}` |
| L44 | `SET assigned_agent = #{agentId}` | →`assigned_agent_id` |
| L50 | `AND (assigned_agent IS NULL OR assigned_agent = #{agentId})` | 两处→`assigned_agent_id` |
| L75 | `WHERE assigned_agent = #{agentId}` | →`assigned_agent_id` |
| L39 | 注释同步 |

### ④ `helloai-job/.../task/AgentHealthCheckTask.java`（L138-139）

UpdateWrapper 裸字符串，全项目唯一一处 Java 裸列名：

```java
// 改前
"offline_at", now.toString(),
"last_seen_at", agent.getLastSeenAt() != null ...
// 改后
"offline_time", now.toString(),
"last_seen_time", agent.getLastSeenTime() != null ...
```

## 四、自动联动、只需核对 diff 的使用点清单

Rename Field 后这些文件的调用点自动更新，提交前过一眼：

| 字段 | 使用文件 |
|---|---|
| lastSeenAt / lastActiveAt / offlineAt | `api/dto/agent/AgentResponse.java`、`core/mcp/McpMcpServer.java` |
| lastFallbackAt | `core/service/ExternalAgentFailureTracker.java`（+ 其测试） |
| nextRetryAt | `core/service/AgentCommandOutboxService.java`、`job/task/OutboxRelayTask.java`（+ 其测试） |
| confirmedAt | `core/service/AgentCommandOutboxService.java` |
| startedAt | `api/dto/duty/DutyLeaseResponse.java`、`core/service/AgentExecutionConnectivityService.java` |
| lastRenewedAt | `api/dto/duty/DutyLeaseResponse.java` |
| expiresAt（三张表） | `api/dto/credential/BindAgentApiKeyRequest.java`、`api/dto/credential/CredentialInfoResponse.java`、`api/dto/duty/DutyLeaseResponse.java`、`core/service/CredentialVaultBindingService.java`、`core/service/CredentialVaultService.java`、`core/service/AgentDutyLeaseService.java`、`core/service/McpToolService.java` |
| completedAt | `api/dto/subtask/SubTaskResponse.java` |
| assignedAgent | `api/dto/subtask/SubTaskResponse.java`、`api/dto/subtask/CreateSubTaskRequest.java`、`api/dto/SubTaskDTO.java`、`api/dto/admin/DashboardHighlights.java`、`api/controller/SubTaskController.java`、`core/agent/mqconsumer/LocalExecutionCommandConsumer.java`、`core/agent/execution/SubTaskExecutionService.java`、`core/service/SubTaskDispatchService.java`、`core/service/SubTaskService.java`、`core/service/McpToolService.java`（`common/constant/WorkMode.java` 中的命中需人工确认是否同名词） |
| reviewerAgent | `api/dto/review/ReviewResponse.java`、`api/controller/ReviewController.java`、`core/service/ReviewService.java` |
| trigger（get/setTrigger） | `api/controller/SubTaskController.java`、`core/agent/mqconsumer/ExecutionCommandMqMessage.java`、`core/agent/mqconsumer/LocalExecutionCommandConsumer.java`、`core/agent/dispatcher/ExecutionCommandPoller.java`、`core/service/AgentCommandOutboxService.java`、`core/service/AgentExecutionRecordService.java`、`job/task/OutboxRelayTask.java`，测试 ×3（ExecutionCommandServiceTest、ExecutionCommandMqPublisherTest、ExecutionCommandPollerTest） |

## 五、明确不动的：DTO 层与前端

- **DTO 字段名保持不变**（`AgentResponse.lastSeenAt`、`SubTaskResponse.assignedAgent` 等），API 契约不变 → 前端 5 个文件（`types/index.ts`、`types/duty.ts`、`SubTaskDetail.vue`、`SubTaskList.vue`、`DutyLeaseList.vue`）共 11 处引用**零改动**。
- 已确认项目无 `BeanUtils.copyProperties` 同名拷贝，entity→DTO 均为手工 set，不会有"改名后静默丢字段"的风险。改名后编译器会把所有装配点（`response.setXxx(entity.getOldName())`）以报错形式列出，逐个改成新 getter 即可——这是天然的核对清单。
- `AgentOutboxEventMapper.xml`、`TaskTimelineMapper.xml` 不引用本次任何待改列，无需修改。

## 六、验证与冒烟

```bash
# 1. 残留检查（应只剩注释，SQL 语句中无旧列名）
grep -rn "last_seen_at\|offline_at\|expires_at\|completed_at\|assigned_agent\|reviewer_agent\|patrol_agent\|last_failure_at\|last_fallback_at\|last_attempt_at\|next_retry_at\|last_sent_at\|confirmed_at\|started_at\|last_renewed_at\|read_at" helloai-core/src/main/resources/mapper/
grep -rn '"last_seen_at"\|"offline_at"' --include="*.java" .

# 2. 全量编译 + 测试
mvn clean compile && mvn test
```

启动后观察 Flyway 日志 `[V23] ... 命中数 = 20（预期 20）`，然后按以下路径冒烟（每条对应一类改名）：

| 冒烟动作 | 覆盖的改名 |
|---|---|
| agent 心跳上报一次 | `last_seen_time` / `last_active_time`（AgentMapper updateById） |
| 等一轮 AgentHealthCheckTask 或手动触发离线判定 | `offline_time`（AgentHealthCheckTask 裸字符串 + markOfflineIfStale） |
| 值班租约创建 + 续约 + 到期扫描 | `start_time` / `last_renew_time` / `expire_time` |
| 子任务分配 → 执行 → 完成 | `assigned_agent_id`、`complete_time`（SubTaskMapper 全部命中行） |
| 提交一次评审 | `reviewer_agent_id` |
| 等一轮 OutboxRelayTask | `next_retry_time` / `last_sent_time` / `confirmed_time` |
| 触发一次 DB Poller 兜底消费 | `last_attempt_time` / `trigger_type` |

## 七、规范回写（CODE_STYLE 第 8 章追加）

```markdown
### 8.x 字段命名强制规则
1. 时间字段一律 xxx_time（对齐 create_time/update_time），禁止 _at 后缀；
   日期字段（DATE 类型）一律 xxx_date。
2. 外键/ID 引用一律 xxx_id；同一 ID 多角色引用加角色前缀：assigned_agent_id、reviewer_agent_id。
3. 状态字段一律 varchar + 枚举字符串；计量字段一律 xxx_count（禁止 total_xxx 混用）。
4. 列名避开数据库关键字（trigger、order、level 等），无法避开时用 xxx_type/xxx_source 形式。
5. 新增表主键一律 BIGINT + 应用层雪花 ID，禁止新增 bigserial 自增主键。
```
